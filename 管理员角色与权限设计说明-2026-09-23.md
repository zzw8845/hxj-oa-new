# 海峡金 OA · 管理员角色与权限设计说明

| 项 | 值 |
|---|---|
| 版本 | v1.1（定稿评审稿） |
| 日期 | 2026-09-23 |
| 范围 | `sys_role` / `sys_permission` / `role_permission` / `role_data_scope` / `user_role` 五张表；`PermInterceptor` 与 `LoginUser` 鉴权链；4 处 `hasRole("ADMIN")` 硬编码；前端菜单门控 |
| 不在范围 | 流程节点指派规则的算法本身（只涉及它与权限点的边界划分）、多公司/多租户隔离 |
| 佐证方式 | 全部结论来自**实读源码 + 实跑统计**，每条给出 `file:line`。无一条来自推测。 |
| **这是一份方案，不是两份** | §3~§9 是「**阶段一 · 零 DDL**」，§10 是「**阶段二 · 主体分离**」。**同一个目标的先后两步**，顺序由 §4.1 的强制约束决定，不是两个可选方案。最终形态以 §10 为准 |

---

## 0. 结论摘要

先直接回答那个问题：**此前"admin 无部门"的设计，是不是错的？**

**设计意图不算错，实现机制是错的。**

意图是清楚的，`minimal_seed.sql:44-46` 的注释写得很明白：admin 不挂部门，正式使用前由管理员自己建部门再挂进去。这背后是"管理员 = 组织外部的配置者"这一模型（主流叫**后台运维账号**模型，泛微 `sysadmin`、致远 `system` 都这么干）。**选这个模型没错**，很多系统就是这么设计的。

错的是**实现**。本系统用 `sys_user` 一种实体承载所有人、没有任何独立后台账号表，所以它**天然只能是"管理员即组织成员"模型**（泛微 eteams、钉钉、企业微信这一路）。在这个模型里，管理员**必须**挂部门——而系统一边选了模型一（不给部门），一边又给 admin 保留了发单权（模型二才有的东西），于是必然裂开。

具体错在 **三处设计错误 + 一处空转**：

| # | 问题 | 性质 | 直接后果 |
|---|---|---|---|
| **E1** | **双轨制**：ADMIN 的"全部权限"是**声明式**的（绑 25 个权限点），而真正的管理员特权是**4 处 Java 硬编码** | 设计错误 | 权限表不是事实来源，**可审计性为零**；改权限点不改变实际行为 |
| **E2** | **无分层**：系统管理职责与业务审批职责压在同一个 `ADMIN` 角色上 | 设计错误 | 违反职责分离（SoD）；**这正是"admin 必须有部门"的根因** |
| **E3** | **无自锁护栏**：可停用自己、可清空自己的角色、可清空 `ADMIN` 角色的权限 | 设计错误 | 单管理员系统**能被自己锁死**，且只能改库恢复 |
| **N1** | **空转**：25 个权限点中 **14 个零引用**（前后端都不查；2026-09-23 前是 15，补 `document:approve` 门控后降为 14） | 不是 bug，是资产虚胖 | "给 admin 全部权限"实际给了一半装饰品，让审查者误判风险面 |

外加两条**由上述错误派生的运行期故障**（不是独立 bug，是 E1/E2 的必然产物）：

- **E2 派生** → admin 无部门 ⇒ 冷启动链（预览 NPE / 建单 500 / 自审移除 / 幽灵单）
- **E1 派生** → `FlowRuntimeService:393` 的 `|| user.hasRole("ADMIN")` 让 admin **可以代替任何人做出审批决定**（同意/驳回）。审计日志只记"谁点了"，不记"他以什么身份越过节点"。这是当前系统**最严重的权限缺陷**，也是**唯一真实存在的越权**。

**一句话定稿**：管理员应当是**分层的数据驱动角色**，不应当是"绑一堆积木 + 代码里放它一马"。

### 0.1 根因（四条症状的共同来源）

E1~E3 与 N1 不是四个独立问题，它们有一个共同的生成机制：

> **`sys_user` 一张表同时承担了「登录主体」「组织成员」「权限持有者」三个正交概念。**
> 于是"运维账号不属于组织""审批人必须是组织成员"这类不变量**在数据模型里无处表达**，
> 只能下沉成每一处调用点的纪律：4 处 `hasRole("ADMIN")`、逐个过滤候选集、自由声明的权限点字符串。

解法见 §10（主体-成员分离）：把三个概念拆回三张表，不变量就有了落点。

---

## 1. 现状取证

### 1.1 取证方法（可复现）

```bash
cd oa-backend
# ① 全项目管理员硬编码旁路
grep -rn 'hasRole(' --include='*.java' .
# ② 被真正门控过的权限点
grep -rho '@RequirePerm([^)]*)' --include='*.java' . | sort | uniq -c | sort -rn
# ③ 前端菜单/按钮门控
grep -oE "can\('[^']+'\)" ../海峡金OA审批系统-联调版.html | sort | uniq -c | sort -rn
# ④ 权限点目录
grep -E "^\('.*'," sql/minimal_seed.sql
```

### 1.2 事实一：25 个权限点中 15 个零引用

> ⚠ **2026-09-24 复核更正（数字与行号都变了，读本文时按下面这版）**
>
> - **汇总数字**：零引用 **15 → 14**，有引用 **10 → 11**。变化来自 2026-09-23 给
>   `/todos/approve`、`/batch-approve`、`/countersign` 补了 `@RequirePerm("document:approve")`，
>   `document:approve` 因此从"装饰品"进入"有引用"（本表下方该行已用删除线标注）。
>   **本文其余各处的 15 / 10 一律按 14 / 11 读**（`N1` 行、§8 证据索引、§10.5(a) 等）。
> - **行号漂移**：`FlowRuntimeService.java:393` → **`:438`**；`DocumentService.java:481` → **`:511`**
>   （补注解后方法整体下移，`AttachmentService:146/169` 未变）。
> - **全量端点级口径**（99 个操作 / 46 个有门控 / 49 个写审计 / 11 个权限点被引用，逐接口可查）见
>   `接口交接文档-2026-09-24.md` §4；交叉验证脚本 `scripts/check_perm_reachability.py` 输出与此一致。

`sys_permission` 共 25 条（`minimal_seed.sql:55-80`）。逐点统计"后端 `@RequirePerm` 引用次数"与"前端 `can()` 引用次数"：

| 分组 | code | 后端门控 | 前端门控 | 判定 |
|---|---|---|---|---|
| 菜单 | `document:menu` | 0 | 0 | **装饰品** |
| 菜单 | `todo:menu` | 0 | 0 | **装饰品** |
| 菜单 | `ledger:menu` | 0 | 0 | **装饰品** |
| 菜单 | `dashboard:menu` | 0 | 0 | **装饰品** |
| 菜单 | `admin:menu` | 0 | 0 | **装饰品** |
| 业务 | `document:create` | **0** | **0** | **装饰品**（⇒ 谁都能建单） |
| 业务 | `document:view:self` | 0 | 0 | **装饰品** |
| 业务 | `document:view:dept` | 0 | 0 | **装饰品** |
| 业务 | `document:view:company` | 0 | 0 | **装饰品** |
| 业务 | `document:export` | 1 | 1 | 有效 |
| 业务 | `document:approve` | ~~**0**~~ **3** | 0 | ✅ **有效**（2026-09-23 补门控：`/todos/approve`、`/batch-approve`、`/countersign`） |
| 业务 | `document:approve:leader` | 0 | 0 | **装饰品** |
| 业务 | `document:approve:accountant` | 0 | 0 | **装饰品** |
| 业务 | `document:approve:cashier` | 0 | 0 | **装饰品** |
| 业务 | `document:approve:seal` | 4 | 1 | 有效 |
| 业务 | `document:supplement` | 0 | 0 | **装饰品** |
| 业务 | `document:countersign` | 0 | 0 | **装饰品** |
| 系统 | `system:user` | 5(+1) | 2 | 有效 |
| 系统 | `system:role` | 5(+1) | 1 | 有效 |
| 系统 | `system:dept` | 6 | 11 | 有效 |
| 系统 | `system:flow` | 7 | 1 | 有效 |
| 系统 | `system:form` | 7 | 5 | 有效 |
| 系统 | `system:dict` | 3 | 6 | 有效 |
| 系统 | `system:audit` | 1 | 2 | 有效 |
| 系统 | `system:docType` | 3 | 5 | 有效 |

**有效 11 个，空转 14 个（56%）。**
（上表的"后端门控/前端门控"是**引用数**，随代码演进；本版已计入 2026-09-23 给
`document:approve` 补的 3 处注解。未变的是**结论**：空转率仍然过半。）

三个必须点明的推论：

1. **`document:create` 没门控** ⇒ `POST /api/documents`（`DocumentController.java:52`，只有 `@Audit`，无 `@RequirePerm`）对**任何登录用户**开放。所以"把 `document:create` 从 ADMIN 收回" **在功能上什么都不会发生**——它只是个前端没人读的字符串。
2. **`document:approve` 没门控，但 `document:approve:seal` 有** ⇒ 审批授权的唯一真实判定在 `FlowRuntimeService.assertAssignee()`（见 1.3），用印反而走权限点。**同一个审批语义，两套判定，粒度还不一致。**
3. **`document:view:*` 三兄弟是重复设计** ⇒ 行级可见性已由 `DataScopeHelper.buildClause()`（`oa-common/util/DataScopeHelper.java:43-80`）单一实现，权限点再来一遍只会产生"权限点给了但数据范围是 self"这种自相矛盾的状态。

### 1.3 事实二：真正的管理员特权 = 4 处 Java 硬编码

`hasRole("ADMIN")` 全项目 4 处调用（`LoginUser.java:47` 是定义，不计）：

| # | 位置 | 语义 | 危险度 |
|---|---|---|---|
| 1 | `oa-flow/.../FlowRuntimeService.java:393`<br>`if (uid.equals(task.getAssignee()) \|\| user.hasRole("ADMIN"))` | **审批授权旁路**：admin 可对**任意任务**做出同意/驳回决定，绕过 assignee、候选人、委托三重判定 | **极高** |
| 2 | `oa-document/.../DocumentService.java:481` | 可见性旁路：admin 看全部单据 | 中 |
| 3 | `oa-document/.../AttachmentService.java:146` | 附件下载旁路：admin 可下载任意附件 | 中 |
| 4 | `oa-document/.../AttachmentService.java:169` | 附件删除旁路：admin 可删除任意附件 | 中高 |

**而 `hasPerm(...)`（`LoginUser.java:51`）在主代码中调用次数 = 0。**

这就是 E1 的完整证据链：**接口层的门禁是数据驱动的（10 个点真的在起作用），业务层的特权判定是代码硬编码的（4 处），两者之间没有任何桥梁。** 项目里唯一"数据驱动特权"的通道 `hasPerm()` 从未被使用。

> ⚠ **更正一条此前的错判**。早前记录过"`hasRole("ADMIN")` 只是数据可见性/附件旁路，不能当权限旁路"——**这是错的**。`FlowRuntimeService.java:391-400` 是货真价实的审批授权旁路。早前之所以误判，是因为只看了 `TodoController.myTodo()`（列表按本人过滤），由"列表查不到"错误外推成"批不了"。**"看不见"不等于"做不了"。**

### 1.4 事实三：ADMIN 的双轨制结构

```
ADMIN 角色的实际能力 = 声明层 ∪ 代码层

  声明层（数据，可配置，可审计）
  ├── role_permission 25 条  ← minimal_seed.sql:83-84 全量授权
  │     └── 其中 15 条是装饰品 ⇒ 声明层有效部分仅 10 条
  └── role_data_scope = company  ← 行为正确，但也是"越宽越好"式的给予

  代码层（硬编码，不可配置，不可审计）
  └── hasRole("ADMIN") × 4 ⇒ 这 4 处才是"超管"的真实来源
```

`ADMIN` 的 `remark` 字段写着"拥有全部权限"（`minimal_seed.sql:52`）——**这句话在两层意义上都不准确**：声明层有 60% 是空转，代码层则完全没被这句话覆盖。

### 1.5 事实四：无分层 ⇒ 违反职责分离（SoD）

本系统只有**一种管理员**。`sys_role` 里 `ADMIN` 一个内置角色同时持有：

- 系统动作 8 个（`system:*`）
- 业务动作（`document:create` 等）

对照主流产品的实现（2026-09-23 查证）：

| 产品 | 是否分层 | 分层方式 |
|---|---|---|
| 泛微 e-cology | ✅ | `sysadmin` 是**独立后台账号**，不在通讯录里，只做运维；业务权限走组织架构 |
| 致远 A8 | ✅ | `system`（系统管理）/ `group-admin`（集团管理）/ `admin1`（单位管理）/ `audit-admin`（审计）**四个账号分立** |
| 泛微 eteams | ✅ | 系统管理员 + **分级管理员**，管理员本身是通讯录成员 |
| 钉钉 / 企业微信 | ✅ | 主管理员 + 子管理员（可按部门划范围） |

**分层是行业共识，本系统完全没有。**

而"未分层 + admin 有发单权"两者叠加，直接把 admin 推成了"同时是配置者和业务发起人"，这**正是"admin 需要部门"这条结论的真正来源**——它不是固有属性，是 E2 的产物。

### 1.6 事实五：无自锁护栏（E3）

| 位置 | 现状 | 风险 |
|---|---|---|
| `UserAdminService.java:186-220` `update()` | `setStatus(req.getStatus())` **无"不能改自己"判断** | admin 可把自己 `status=0` ⇒ 立即无法登录 |
| `UserAdminService.java:224` `updateRoles()` | **无"不能改自己角色"判断** | admin 可清空自己全部角色 ⇒ 权限归零 |
| `RoleAdminService.java:151` `update()` | **无 `isBuiltin` 保护**，`req.getPermCodes()` 非 null 即 `overwritePermissions` | 可把 `ADMIN` 角色的权限清空 |
| `RoleAdminService.java:180` `updatePermissions()` | **无 `isBuiltin` 保护** | 同上，且这是界面"配置权限"的直接入口 |
| `RoleAdminService.java:191` `updateDataScope()` | **无 `isBuiltin` 保护** | 可把 ADMIN 数据范围降成 `self` |
| `RoleAdminService.java:203-207` `delete()` | ✅ **有** `isBuiltin` 保护 | 参照物：这里做对了 |
| `UserAdminService.java:239-243` `delete()` | ✅ **有** `if (Objects.equals(id, UserContext.currentUserId()))` | 参照物：这里做对了 |

注意这个不对称：**"删除"路径都做了保护，"修改"路径都没做。** 结果是保护形同虚设——不能删，但能改废，效果一样。

还有一条静默通道：`DataScopeType.of()`（`oa-common/security/DataScopeType.java:35-45`）对非法值**静默返回 `SELF`**；`RoleAdminService.overwriteDataScope()` 不做枚举校验 ⇒ `PUT /api/roles/{id}/data-scope` 传个非法字符串**返回成功但降级为 self**。界面下拉选不出非法值，但**直连接口的对接方会中招**，且报错信息为零。

### 1.7 事实六：管理范围维度缺失

`UserAdminService.listWithDetail/pageWithDetail/assembleAll` 全部**只按 `company_id` 过滤**，没有任何按部门的收敛。这意味着：

- 一旦引入"部门级管理员"（分级管理员），当前实现**无法表达**"这个管理员只能管这个部门的人"。
- 不是 bug，是**能力缺席**——要做分级管理员必须新增维度。

### 1.8 事实七：前端菜单未门控

`海峡金OA审批系统-联调版.html:24` 的侧边栏 **14 个菜单项里，只有 3 个带 `v-if="can(...)"`**（实测）：

```
主数据   v-if="can('system:dept') || can('system:dict')"   ✅
表单模板 v-if="can('system:form')"                          ✅
审计日志 v-if="can('system:audit')"                         ✅
权限管理 (无 v-if)                                          ❌
流程管理 (无 v-if)                                          ❌
风险预警 (无 v-if)                                          ❌
```

后果：普通员工也能看到并点进"权限管理""流程管理"，接口返回 403，页面表现为**一片报错或空白**。这是权限体系"内外两张皮"的直观体现——**权限点在数据库里很齐全，在界面上只落实了 3 处。**

### 1.9 事实八：admin 无部门 ⇒ 冷启动链

`minimal_seed.sql:47-48` 的 `sys_user` INSERT **不含 `dept_id` 列** ⇒ admin 的 `dept_id = NULL`。

| 环节 | 后果 | 位置 |
|---|---|---|
| 发单时 `applicantDeptId` 为空 | 节点解析 `Objects.requireNonNullElse(deptId, ctx.getApplicantDeptId())` 两参皆 null ⇒ **NPE** | `AssigneeResolver.java:121` |
| 保存草稿 | `doc.setDeptId(user.getDeptId())` 直传不兜底 ⇒ **500** | `DocumentService.java:84-85` |
| 自审 | 首个节点常是"直属部门负责人"= 申请人自己 ⇒ 被静默移除 | B5 |
| 幽灵单 | `autoSkipUnassigned()` 已 `FINISHED`，`start()` 又**无条件** `publish(STARTED)` 覆盖 ⇒ status=2 但 `ended_at` 有值、`ACT_RU_TASK=0` | `FlowRuntimeService.java:120-122` |

⚠ **这条链是 E2 的必然产物，不是四个并列的 bug。** 修 admin 的部门归属，链条上游就断了。

---

## 2. 设计原则

以下五条作为本模块后续所有改动的判据。**违反任一条的改动不予合入。**

### P1 · 一切特权必须是数据，不是代码

主代码中**不得出现角色编码字符串的比较**（`hasRole("ADMIN")`、`roleCode.equals(...)`）。

- 现状违反：4 处（见 1.3）
- 通过标准：`grep -rn 'hasRole(' oa-backend --include='*.java' | grep -v 'LoginUser.java'` 输出为空

### P2 · 权限点必须有门控，否则不得入库

任何写进 `sys_permission` 的 code，**必须至少在一处被引用**（后端 `@RequirePerm` 或前端 `can()`），否则它就是"假权限"——它让审查者以为有这个控制，实际没有。

- 现状违反：15 个（见 1.2）
- 通过标准：新增校验脚本 `scripts/check_perm_reachability.py`，扫描 25 个 code 的引用数，**0 引用 = 红灯**，接入 `run_all_verify.sh`

### P3 · 职责分离：审批决定权永不给管理员

"代替他人做出同意/驳回决定"与"管理配置"是**互斥职责**。管理员需要介入流程时，只能做**流程管理动作**（转办 / 终止 / 催办）——那三件事的作用是把任务**交还给正确的人**，而不是**替正确的人签字**。

| 动作类 | 例子 | 可否给管理员 |
|---|---|---|
| 流程管理动作 | 转办、终止、催办、超时升级 | ✅ 应当给 |
| 审批决定动作 | 同意、驳回、加签、要求补料 | ❌ 永不给 |

- 现状违反：`FlowRuntimeService.java:393` 给了**审批决定权**，而**转办/终止/催办一个都没有**——**方向完全反了**

### P4 · 单一事实来源

"谁是这个节点的审批人" **只能**由流程节点指派规则（`AssigneeResolver` + `flow_node_assignee`）决定；"这个人能做什么操作"由权限点决定。两者**正交，不互相表达**。

- 依据：`document:approve:leader/accountant/cashier` 是在用权限点**重复表达**节点规则，必然产生两套真相。`AssigneeTaskListener` 按 `(flowConfigId, nodeKey)` 定位审批人（硬约束 1），说明节点规则才是权威。

### P5 · 失败必须显式

拒绝要报错；降级要提示；不允许"静默放行"或"静默降级"。

- 现状违反三处：`DataScopeType.of()` 静默降级 self（1.6）；`RoleAdminService.updateDataScope()` 不校验枚举（1.6）；`autoSkipUnassigned` 非空废人静默卡死（硬约束 16①）

---

## 3. 目标模型

### 3.1 角色分层（三层）

```
┌─ 第 0 层 · 平台管理 ─────────────────────────────────────────┐
│  SYS_ADMIN（系统管理员）                                      │
│  职责：组织架构 / 用户 / 角色权限 / 字典 / 单据类型 /           │
│        表单模板 / 流程定义 / 审计日志                          │
│  部门：必须有（挂「公司本部」根部门，语义中立）                  │
│  数据范围：company（审计需要全局视野）                          │
│  ⛔ 不含任何 document:approve*                                │
└──────────────────────────────────────────────────────────────┘
┌─ 第 1 层 · 流程运营（可选，可由 SYS_ADMIN 兼任） ──────────────┐
│  FLOW_ADMIN（流程管理员）                                     │
│  职责：流程干预 —— 转办 / 终止 / 催办 / 超时升级盘点            │
│  ⛔ 不含 document:approve*  ← P3 的落点                       │
└──────────────────────────────────────────────────────────────┘
┌─ 第 2 层 · 业务角色（已交付，不动） ──────────────────────────┐
│  EMPLOYEE / DEPT_HEAD / GM / FIN_DIRECTOR /                   │
│  ACCOUNTANT / CASHIER / INTERNAL_CTRL                         │
│  各持「发起 + 审批」能力；审批人身份来自节点规则（P4）           │
└──────────────────────────────────────────────────────────────┘
```

**与现状的差别**：现状是把第 0、1、2 层压成一个 `ADMIN`。改造**不新增表、不新增实体**，只是把 `sys_role` 拆成三个内置角色。

### 3.2 权限点目录重构（25 → 目标 17）

三种处置：**保留**（已在起作用）、**补门控**（有语义、缺落地）、**删除**（语义重复或不成立）。

| 现状 code | 处置 | 理由 / 门控落点 |
|---|---|---|
| `document:menu` | **删** | 前端菜单硬编码，无门控语义；5 个菜单点全是装饰 |
| `todo:menu` | **删** | 同上 |
| `ledger:menu` | **删** | 同上 |
| `dashboard:menu` | **删** | 同上 |
| `admin:menu` | **删** | 同上 |
| `document:create` | **补门控** | 挂 `DocumentController.java:52` `@RequirePerm("document:create")` |
| `document:view:self` | **删** | 行级可见性由 `DataScopeHelper` 单一实现（P4） |
| `document:view:dept` | **删** | 同上 |
| `document:view:company` | **删** | 同上 |
| `document:export` | **保留** | 已门控（`DocumentController.java:115`） |
| `document:approve` | **补门控** | 挂 `TodoController` 三个端点：`/approve`(:47)、`/batch-approve`(:65)、`/countersign`(:73) |
| `document:approve:leader` | **删** | 与节点规则重复（P4）；指派规则才是事实来源 |
| `document:approve:accountant` | **删** | 同上 |
| `document:approve:cashier` | **删** | 同上 |
| `document:approve:seal` | **保留** | 用印是**独立台账操作**（不是流程节点决定），已门控 4 处 |
| `document:supplement` | **删** | 与 `/approve` 同端点（`action` 区分，见 `TodoController.java:41-51`），URL 级无法单独门控；且"是不是本节点审批人"已决定资格 |
| `document:countersign` | **删除**（并入 `document:approve`） | 加签同属审批决定动作（P3），共用一个点即可 |
| `system:user` | **保留** | 已门控 5(+1) 处 |
| `system:role` | **保留** | 已门控 5(+1) 处 |
| `system:dept` | **保留** | 已门控 6 处 |
| `system:flow` | **保留** | 已门控 7 处 |
| `system:form` | **保留** | 已门控 7 处 |
| `system:dict` | **保留** | 已门控 3 处 |
| `system:audit` | **保留** | 已门控 1 处 |
| `system:docType` | **保留** | 已门控 3 处 |

**新增 3 个**（替换硬编码 + 补第 1 层能力，需随功能一起开发）：

| 新 code | 用途 | 替换目标 |
|---|---|---|
| `flow:intervene:transfer` | 转办 | 新增能力（当前缺失） |
| `flow:intervene:terminate` | 终止 | 新增能力（当前缺失） |
| `attachment:manage:any` | 附件读/删任意附件 | `AttachmentService.java:146` / `:169` 两处硬编码 |

> **`DocumentService.java:481` 的可见性旁路** 不新增权限点，**直接删除** —— admin 挂着 `scope=company` 的数范围本来就看得见，硬编码是冗余的。

**目标总数**：`document:create`、`document:export`、`document:approve`、`document:approve:seal`、`system:*` × 8、`flow:intervene:*` × 3、`attachment:manage:any` = **17 个，全部有门控**。

### 3.3 授权矩阵

| 权限点 | SYS_ADMIN | FLOW_ADMIN | EMPLOYEE | DEPT_HEAD | 业务审批角色 |
|---|---|---|---|---|---|
| `system:user` / `:role` / `:dept` | ✅ | — | — | — | — |
| `system:form` / `:docType` / `:dict` | ✅ | — | — | — | — |
| `system:flow` | ✅ | ✅ | — | — | — |
| `system:audit` | ✅ | — | — | — | — |
| `document:export` | ✅ | ✅ | — | — | 视角色 |
| `document:create` | **✅**（决策点 D8） | — | ✅ | ✅ | — |
| `document:approve` | **⛔** | **⛔** | ✅ | ✅ | ✅ |
| `document:approve:seal` | — | — | — | — | CASHIER / 印章管理 |
| `flow:intervene:*` | — | ✅ | — | — | — |
| `attachment:manage:any` | ✅ | — | — | — | — |

**数据范围**：SYS_ADMIN / FLOW_ADMIN = `company`；其余角色不变（`DEPT_HEAD=dept`、`EMPLOYEE=self`）。

**关于 `document:create`**（决策点 D8）：`SYS_ADMIN` 是否保留发单权，是本模型唯一的开放项。

- **出口 A（本期采用）**：**保留** `document:create`。理由：配完流程必须能自己跑一遍验证，这是排障刚需；代价是 admin 需要部门——所以挂**「公司本部」根部门**（语义中立，非业务科室，发出的单据不会污染业务部门统计）。
- **出口 B（稳态目标）**：**收回** `document:create`。管理员只能在"节点指派"里用预览功能验证（`/api/flows/configs/{id}/preview`），不能真发单。代价：遇到"流程能解析但一提交就报错"这类问题，管理员只能靠日志排查。

#### ⚠ D8 定稿：分期，而不是二选一

上一版我直接推荐了 A，理由是"排除成本高、收益低"。加入 §3.8 的取证后，这个理由需要**补一个时间维度**才是完整的：

| 阶段 | 选择 | 判据 |
|---|---|---|
| **交付前（现在）** | **出口 A · 保留** | 冷启动链 B1~B5 **尚未修复**，而其中三条（建单 500 / 自审移除 / 幽灵单）**只在"真提交"这条路径上暴露**。`/configs/{id}/preview` 只解析审批人，**恰好抓不到它们** ⇒ 此时收回发单权，等于砍掉管理员唯一的排障手段 |
| **交付后 / 补了模型一之后** | **出口 B · 收回** | 一旦 (a) 客户侧流程全部验证完毕，或 (b) 按 §3.8.5 引入独立运维账号表，发单权就**没有任何存在理由**——它的全部价值就是"临时排障" |

⇒ **A 不是终点，是过渡。** 把它写成"永久保留"会掩盖 D8 的真实性质：
D8 问的从来不是"要不要给管理员发单权"，而是"**要不要承认它现在其实是组织成员**"（§3.8.5）。

### 3.4 4 处硬编码收口方案（逐处）

| # | 位置 | 现状 | 改为 | 类型 |
|---|---|---|---|---|
| 1 | `oa-flow/.../FlowRuntimeService.java:393` | `if (uid.equals(task.getAssignee()) \|\| user.hasRole("ADMIN")) return;` | 删掉 `\|\| user.hasRole("ADMIN")`，**不再补任何旁路**。管理员要介入请走 `flow:intervene:transfer`（转办），这是 P3 的落点 | **行为收窄** |
| 2 | `oa-document/.../DocumentService.java:481` | `if (user.hasRole("ADMIN")) return;` | **直接删除**。可见性由 `DataScopeHelper.buildClause()` 决定；SYS_ADMIN 的 `scope=company` 天然放行 | **行为等价**（因 scope=company） |
| 3 | `oa-document/.../AttachmentService.java:146` | `... && !user.hasRole("ADMIN")` | `... && !user.hasPerm("attachment:manage:any")` | **行为等价**（ADMIN 仍持该点） |
| 4 | `oa-document/.../AttachmentService.java:169` | `... && !user.hasRole("ADMIN")` | 同上 | **行为等价** |

**关键观察**：4 处里 **3 处是行为等价替换**（只把"代码内特权"变成"数据内特权"，不改实际放行结果），**只有第 1 处是真正的权限收窄**。这意味着**这次收口对现有功能的影响面极小，风险可控**——除了管理员不能再"替人审批"。

第 1 处的收窄需要一个**过渡方案**，否则交付时管理员可能确有"救火"需求：

```
选项 甲（推荐）：只给「转办」，不给「代批」
  管理员发现单据卡在离职人员名下 → 用 flow:intervene:transfer 转给正确的人 → 由那人批
  ✅ 职责分离保持完整，审计链清晰

选项 乙：新增 flow:intervene:approve_on_behalf，默认不授予任何人
  确需时单点授予某个角色，且审批记录里标注"管理员代办"
  ⚠ 引入即弱化 P3，仅在前置配置错误导致单据无法推进时使用
```

#### 3.4.1 收尾动作：删掉 `hasRole(String)` —— 这一步才让"收口"不再是补丁

4 处改完之后，`LoginUser.hasRole(String)` 的调用者**归零**（实测：全项目生产代码只有这 4 处；
`getRoleCodes()` 另有 `PermInterceptor:77` 一处引用，是日志参数，保留）。

⇒ **把 `hasRole(String)` 方法删掉。** 第 5 处 `user.hasRole("ADMIN")` 从此**写不出来 —— 编译不过**。

| | 删方法之前 | 删方法之后 |
|---|---|---|
| 再写一处 `hasRole("ADMIN")` | 编译通过、启动通过、测试可能全绿，静默生效 | **编译失败** |
| 需要新的管理员旁路 | 只能再写一处硬编码 | 被迫落到 `@RequirePerm` / `hasPerm(...)`，即数据 |

代价：**零 DDL、零新依赖、零运行时风险**，且**不依赖测试被执行**（本项目所有构建命令都带 `-DskipTests`，见 §4.2 脚注 ⇒ 测试型护栏在本项目是假护栏）。

### 3.5 自锁护栏 R1~R3

| 编号 | 规则 | 落点 | 拒绝话术 |
|---|---|---|---|
| **R1** | 不允许把自己停用 | `UserAdminService.update()`，`Objects.equals(id, UserContext.currentUserId()) && req.getStatus()==0` | "不能停用当前登录账号" |
| **R2** | 不允许移除自己最后一个管理型角色 | `UserAdminService.updateRoles()` / `rebindRoles()`：操作后该用户需仍持有**至少一个含 `system:role` 的角色** | "不能移除自己的最后一个管理角色" |
| **R3** | **末位管理员保护**：任何操作完成后，系统中"活跃且持有管理型角色"的用户数必须 ≥ 1 | 抽成 `AdminGuard.assertNotLastAdmin(affectedUserId)`，在 4 个入口调用：`UserAdminService.update` / `updateRoles` / `delete`、`RoleAdminService.updatePermissions` / `updateDataScope` / `delete` | "这是系统中最后一个管理员，该操作会导致无人能管理系统" |

R3 是 **R1/R2 的一般化**（R1/R2 拦住最常见路径，R3 兜住其余路径，包括"两个管理员互相停用"）。**优先实现 R3**，R1/R2 作为快速失败的前置检查。

```sql
-- R3 的判据（管理型角色 = 含 system:role 的角色）
SELECT COUNT(DISTINCT u.id)
FROM sys_user u
JOIN user_role ur ON ur.user_id = u.id
JOIN sys_role r ON r.id = ur.role_id AND r.deleted = 0 AND r.status = 1
JOIN role_permission rp ON rp.role_id = r.id AND rp.perm_code = 'system:role'
WHERE u.deleted = 0 AND u.status = 1 AND u.id <> #{affectedUserId};
-- 结果 = 0 且被影响用户是活跃管理员 ⇒ 拒绝
```

**同时对内置角色加保护**（补齐 1.6 的不对称）：

- `RoleAdminService.updatePermissions()` / `updateDataScope()`：若目标是 `is_builtin=1` 的角色，**只允许变更 `remark`**，权限点与数据范围拒绝修改（"内置角色的权限由系统维护"）。
- 理由：内置角色是流程指派规则的锚点（`RoleAdminService.java:50-51` 已明确），**而且其中的 `ADMIN` 是全系统权限的根**。可维护性通过"新建自定义角色"满足，不必动内置角色。
- 注意这是**现状已声称的原则**（`delete` 已做），只是补上另外三个入口，不是新增约束。

### 3.6 部门归属规则

| 实体 | 字段 | 现状 | 目标 |
|---|---|---|---|
| `sys_user` | `dept_id` | 可空（`schema.sql` 已确认为可空） | **保持可空**（普通员工入职流程允许暂不分配），但 **SYS_ADMIN 必须有值**，由引导流程保证 |
| `document` | `dept_id` | **NOT NULL**（`schema.sql:368`） | **不动** —— 这就是 `DocumentService.java:84-85` 需要兜底的原因 |
| 根部门 | — | 不存在 | 新增「公司本部」：`parent_id=0, dept_type=1, path='/1/', leader_id=<admin>` |

**为什么 `document.dept_id` 保持 NOT NULL**：单据必须有归属部门，行级权限 `deptSubQuery` 依赖它；把它改成可空会让 `DataScopeHelper` 的 `path`/`deptId` 双空分支（`DataScopeHelper.java:97-98` 的 `AND 1 = 0`）在业务表上大面积命中，等于把 NPE 换成"所有人都看不见"。

**所以 D2 决策点的答案是：DDL 不需要改。** 要改的是行为——把 `DocumentService.java:84-85` 的**静默 500** 换成**明确报错**：

```java
// 现状（B4 根因）：直传不兜底 ⇒ dept_id NOT NULL ⇒ MySQL 500
doc.setDeptId(user.getDeptId());

// 目标：显式拒绝 + 可执行的提示（P5）
if (user.getDeptId() == null) {
    throw BizException.of("当前账号未分配部门，请先在【主数据 → 部门】中为自己设置所属部门后再发起单据");
}
doc.setDeptId(user.getDeptId());
```

### 3.7 与主流 / 等保的对照

**行业事实（2026-09-23 查证）**：OA 管理员主要负责系统搭建维护、权限分配、流程配置，**日常不参与具体业务审批**，特定情况下拥有**流程干预权**。这句话里的"流程干预权"在行业实现里明确指**转办/终止/催办**，**不含"同意/驳回"**。本系统恰好把这两类**搞反了**：给了代批权（`FlowRuntimeService:393`），没有干预权（转办/终止/催办均无实现）。

**与等保 2.0 三员分立的差距**：

| 等保要求 | 本系统 | 差距 |
|---|---|---|
| 系统管理员（用户/角色/权限/配置） | `SYS_ADMIN` | ✅ 改造后满足 |
| 安全保密管理员（安全策略、授权） | 无 | ⚠ 未分离；`system:role` 与 `system:user` 捆在一起 |
| 安全审计员（审阅审计日志、不可修改） | `system:audit` | ⚠ 有只读审计页，但审计员角色未独立；`system:audit` 目前与管理员权限捆在同一角色上 |

**本期处置**：三员分立**不作为交付阻塞项**（客户未提等保要求），但分层设计要**为它留出结构** ——
三层角色模型下，将来只需新建 `AUDIT_ADMIN` 角色并把 `system:audit` 从 `SYS_ADMIN` 移过去，**不需要再改代码**。
若不做分层，将来做等保必须重写鉴权链。

---

### 3.8 "系统管理员有部门"的到底是谁 —— 逐产品取证与一次自我修正

#### 3.8.1 真正分界不是产品，是**交付形态**

之前我给的"主流对照"只到产品粒度，这不够。查到数据库表结构之后，分界线清楚了：

> **分界 = 管理员是「一种账号类型」，还是「组织成员身上的一个角色」。**

| 系统 | 交付形态 | 管理员的存储位置 | 属组织架构 | 有部门 |
|---|---|---|---|---|
| **泛微 e-cology**（E9） | 私有化 | **`HrmResourceManager`**（系统管理员信息表） | ❌ 不在 | **❌ 没有** |
| **致远 A8** | 私有化 | `ORG_PRINCIPAL` 里的预置账号 `system` / `group-admin` / `admin1` / `audit-admin` | ❌ 不在 | **❌ 没有** |
| **泛微 eteams**（云） | SaaS | 通讯录成员的**管理角色成员** | ✅ 在 | **✅ 有** |
| **钉钉** | SaaS | 企业通讯录成员 + 管理组 | ✅ 在 | **✅ 有** |

#### 3.8.2 私有化阵营：管理员是**独立的第二张账号表**（硬证据）

**泛微 e-cology 的表结构直接证实了模型一**：

| 表名 | 中文说明 | 归属 |
|---|---|---|
| `HrmCompany` | 总部 | 组织五级模型 |
| `HrmSubCompany` | 分部 | 组织五级模型 |
| `HrmDepartment` | 部门 | 组织五级模型 |
| `hrmJobTitles` | 岗位 | 组织五级模型 |
| **`HrmResource`** | **人力资源基本信息表（人员主表）** | 组织五级模型 |
| **`HrmResourceManager`** | **系统管理员信息表** | **系统级，独立** |

官方二次开发资料把 `HrmResourceManager` 定义为"**系统级管理员元数据表**，承载超级管理员、安全管理员、流程管理员等角色权限的底层映射"；
密码重置也是直接 `UPDATE HrmResourceManager SET password = ... WHERE loginid = 'sysadmin'` ——
**它和人员主表 `HrmResource` 是两张表，且不参与"组织-单位-部门-岗位-人员"五级模型。**
⇒ 泛微 `sysadmin` **没有部门**，这是设计如此，不是缺失。
（顺带：`sysadmin` 默认密码是 `1`，E10 还有 `weaveradmin / Weaver@2001` —— 弱口令问题在成熟产品里同样存在。）

**致远 A8 同理**：`org_member` 是人员档案表，而 `system`（系统管理员）/ `group-admin`（集团管理员）/
`admin1`（单位管理员）/ `audit-admin`（审计管理员）四个**预置账号**存在 `ORG_PRINCIPAL` 里，ID 是 25/26/27/600 这种低位值。
⇒ 它是**四员分立 + 预置账号**，且**不在人员档案里**。这恰好印证了 §3.7 的三员分立对照。

#### 3.8.3 云/SaaS 阵营：管理员就是**通讯录里的人**

- **泛微 eteams**（官方帮助文档原文）：管理员身份在"后台管理中心 → 权限管理中心 → **角色设置**"里配置，
  "**角色成员**"从成员中选择；系统管理员可把权限下放给**分级管理员**；
  并明确写着"**下级角色的功能权限只能在上级角色的权限范围内勾选**"、"**系统管理员默认拥有所有模块的功能管理权限，不可取消的**"。
  ⇒ **管理员 = 通讯录成员 + 一个管理角色**（这正是本系统的形态）。
- **钉钉**（官方文档原文）：添加子管理员的路径是"成员 → 管理范围 → 分配权限"，
  成员一步的操作是"**从企业通讯录选择**"；并可为子管理员设置"**管理范围**（所辖部门 / 自定义范围）"。
  ⇒ **子管理员就是通讯录成员，有部门**，且"管理范围"本身就是一个部门维度。

#### 3.8.4 ⚠ 修正我此前的一个说法

我在上一轮说过："本系统天然只能走模型二（管理员 = 组织成员），**按主流就应当有部门**。"
**前半句成立，后半句的推理链不成立**，需要更正：

**本项目是私有化交付**（自建服务器、客户机房部署），而私有化阵营（泛微、致远）**清一色是模型一，管理员没有部门**。
所以"admin 无部门"这个选择**在交付形态上是有大量先例的、甚至是更主流的**——
它并不是"抄错了"，而是**只抄了一半**：

```
模型一的完整形态 = 独立账号表（HrmResourceManager） + 不进组织架构 + 不参与任何业务
        ↑                          ↑                        ↑
    本系统缺的                                  本系统缺的
        ↓
本系统实际做的 = 把 admin 塞进 sys_user（那张"人员主表"） + 不给部门 + 却给了发单权

⇒ 结果：形态上模仿模型一（不给部门），机制上做不到模型一（没有第二张表做隔离），
        于是 admin 必然被"组织成员"的那套规则约束 —— 要部门、会被当成审批人候选、单据必须有归属部门。
```

#### 3.8.5 因此，"admin 有部门"在本项目里是**补偿性设计**，不是抄主流

| 方案 | 性质 | 评价 |
|---|---|---|
| admin 挂「公司本部」根部门 | **补偿性设计** —— 用"语义中立的根部门"去补"没有独立账号表"的窟窿 | ✅ 本期正解（成本最低） |
| 收回 `document:create` | 让 admin **不参与**业务，而不只是"不参与业务部门" | ⏳ **稳态目标**（见 §3.3 D8 分期：交付前必须保留，否则砍掉排障手段） |
| **新增独立运维账号表**（模型一） | **形态性修正** —— 让 `sysadmin` 真正独立于 `sys_user` | ✅ **方案见 §10（阶段二）；何时做见 §10.7 触发条件**，而非"看不清怎么做" |

**结论（定稿）**：
1. 本期按补偿性设计走 —— **admin 挂根部门**（+ 交付前保留发单权用于排障），这是"在没有独立账号表的前提下最接近模型一的姿态"。
2. 若客户明确要求"管理员不得出现在组织架构里"，则**唯一正确的做法是补模型一**（独立运维账号表 + 独立登录入口），
   而不是把 admin 的 `dept_id` 置空 —— 置空只是把架构缺口换成运行期 NPE（见 §1.9 冷启动链）。
3. 反过来说：**如果补了模型一，`document:create` 就可以光明正大地收回**，D8 的纠结自然消失。
   ⇒ D8 的本质不是"要不要给管理员发单权"，而是"**要不要承认它现在其实是组织成员**"。

---

## 4. 落地顺序（强制，不可调换）


### 4.1 为什么顺序不能换

**核心风险**：4 处旁路是**按角色编码字符串**判定的。如果先动角色（比如把 `ADMIN` 改名成 `SYS_ADMIN`），
这 4 处判定会**全部静默失效** —— 既不报错，也不告警，表现为"管理员突然什么都做不了了"或"附件下载 403"。
而如果先收口 4 处，再动角色，每一步都能验证。

```
❌ 错误顺序：改角色编码 → 收口硬编码
   ↑ 中间态：ADMIN 已不存在，4 处判定变成死代码，
     管理员权限静默丢失，且此时还没有 SYS_ADMIN 来配权限 —— 系统锁死

✅ 正确顺序：收口硬编码 → 建新角色 → 改种子 → 清旧权限 → 补门控 → 前端
```

### 4.2 六步执行表

| 步 | 动作 | 判据（可执行） | 回滚 |
|---|---|---|---|
| **S1** | 收口 4 处硬编码（§3.4）：第 3/4 处用 `attachment:manage:any` 替换，第 2 处直删，第 1 处删旁路；**然后删掉 `LoginUser.hasRole(String)` 方法**（§3.4.1） | `grep -rnE 'hasRole\(' oa-*/src/main` **无输出**（连定义都没有）；且能编译通过 | `git revert <commit>`；无 DDL |
| **S2** | 建 `SYS_ADMIN` / `FLOW_ADMIN` 角色 + 新权限点（`flow:intervene:*` / `attachment:manage:any`）。**纯数据操作，可走应用界面** | `GET /api/permissions` 返回 17 个点；`GET /api/roles` 有 3 个内置角色 | 删角色（`DELETE /api/roles/{id}`，此时成员为空可删） |
| **S3** | 改种子 `minimal_seed.sql`（§5.1）：建根部门、admin 挂根部门、ADMIN 改为按 `SYS_ADMIN` 清单授权 | 灌入临时库 → `SELECT dept_id FROM sys_user WHERE id=1` 非空；`SELECT COUNT(*) FROM role_permission WHERE role_id=1` = SYS_ADMIN 的点数 | 恢复 SQL 文件；临时库 DROP |
| **S4** | 从旧 `ADMIN` 清掉 12 个业务权限点（只留系统层 + `document:create` + `document:export`） | 登录后 `me.permCodes` 不含 `document:approve*` | 界面重新勾选（**必须重新登录生效**，见硬约束 2） |
| **S5** | 补门控：`document:create` → `POST /api/documents`；`document:approve` → `/todos/approve`、`/batch-approve`、`/countersign` | 用普通员工 token 调 `POST /api/documents` → 若该员工无 `document:create` 应 **403**；用 admin token → 200 | 删除注解即可（P2 的红灯会提醒） |
| **S6** | 前端菜单门控补齐（权限管理 / 流程管理 / 风险预警加 `v-if="can(...)"`）+ 14 个空转点在目录中删除 | 用 EMPLOYEE 登录，侧边栏不出现"权限管理"；`scripts/check_perm_reachability.py` 全绿 | `git revert`；前端改动不影响后端 |

**S5 必须在 S4 之前或同时**：如果先把权限点从 ADMIN 摘掉、却还没给端点上锁，那么"收权"是**假的**（管理员仍能调通）。

**S3 必须在 S4 之前**：否则 admin 在新种子下没有部门，冷启动链依旧。

> **⚠ 关于"护栏写在哪"（2026-09-23 实测修正）**
> 本项目**所有构建入口都带 `-DskipTests`**（`docker-test/deploy.sh:88`、`启动联调版.command:140`、
> `oa-backend/README.md:47`、`scripts/init_and_run.sh:58`）⇒ **写在 `src/test` 里的护栏在交付构建中根本不会执行，
> 是假护栏。** 因此 S1 的收尾必须是**删方法**（编译期），不能只靠 ArchUnit / 单元测试。
> 判据也要相应改：`grep -rnE 'hasRole\(' oa-*/src/main` —— **连定义都不该有**，而不是"除定义外无输出"。

---

## 5. 种子与交付物改动清单

### 5.1 `oa-backend/sql/minimal_seed.sql`（逐项）

| 行 | 现状 | 改为 |
|---|---|---|
| — | 无部门 | **新增**：`INSERT INTO department (id,company_id,parent_id,code,name,dept_type,level,path,leader_id,sort_no) VALUES (1,1,0,'D000','公司本部',1,1,'/1/',1,10);` |
| 47-48 | `sys_user` INSERT **不含 `dept_id` 列** | 列清单加入 `dept_id`，值填 `1` |
| 51-52 | `sys_role` 仅 `ADMIN`（remark "拥有全部权限"） | 新增 `SYS_ADMIN` / `FLOW_ADMIN` 两行；`ADMIN` 的 `remark` 改为准确的职责描述（禁止"全部权限"这类无信息量的表述） |
| 83-84 | `SELECT 1, code FROM sys_permission`（全量 25 点） | 改为**显式列举** SYS_ADMIN 的点（不写 `SELECT *`）—— 显式列举让"给了什么"一眼可读，也避免新增权限点时被静默全量授予 |
| 87-88 | `role_data_scope` ADMIN=`company` | `SYS_ADMIN`=`company`、`FLOW_ADMIN`=`company` |
| 94-99 | 核对用的"灌完后数据库状态"注释 | 按新结构更新；**`department` 不再是 0** |
| 44-46 | 注释"不挂部门……请先建部门并挂进去" | 改写为：admin 挂「公司本部」根部门；解释为何根部门而非业务科室（§3.6） |

**同时修正 `seed_data.sql` 的口径冲突**（硬约束 8①）：`seed_data.sql:68` 把 admin 放在 `dept_id=6`（综合管理中心，负责人是 user 8），
新种子放在根部门 —— 两者必须统一为**根部门**，否则同一产品在不同种子下行为不同（一个是 admin 参与综合管理中心审批，一个不参与）。

### 5.2 交付文档口径统一

`项目交接说明.md` §9.2 当前写的是 `seed_data.sql`（187 行演示数据，含张总/李财务等假账号）。
本设计定稿后，**交付口径只能是 `minimal_seed.sql`**（+ 本设计要求的根部门改动）。`seed_data.sql` 降级为**离线自测基准，不得随交付物给出**。

---

## 6. 验收断言（可直接执行）

```bash
# A1 · P1 通过：主代码零角色编码比较
grep -rn 'hasRole(' oa-backend --include='*.java' | grep -v 'common/security/LoginUser.java'   # 期望：空

# A2 · P2 通过：无空转权限点
python3 scripts/check_perm_reachability.py    # 期望：17 个点全部有引用，退出码 0

# A3 · 审批决定权不外泄（P3 核心）
#    用 admin 登录，对自己的审批任务调 /api/todos/approve
#    期望：403「无权限执行该操作（需要权限点：document:approve）」

# A4 · 自锁护栏 R1
#    admin 对自己调 PUT /api/users/1 {status:0}
#    期望：400「不能停用当前登录账号」

# A5 · 自锁护栏 R3（末位管理员）
#    用第二个管理员停用第一个（仅剩一个活跃管理员时）
#    期望：400「这是系统中最后一个管理员…」

# A6 · 冷启动链断绝
#    灌入新 minimal_seed.sql → 用 admin 打开发起单据对话框、建单、提交
#    期望：全部 200；`SELECT status,ended_at FROM document ...` 不含"status=2 且 ended_at 非空"的幽灵单

# A7 · 数据范围显式失败（P5）
curl -X PUT /api/roles/{id}/data-scope -d '{"scopeType":"DEPT"}'
#    期望：400（非法枚举），而不是 200 + 静默降级 self
```

---

## 7. 未决决策点

| 编号 | 决策 | 选项 | 建议 |
|---|---|---|---|
| **D8** | `SYS_ADMIN` 是否保留 `document:create` | A 保留（挂根部门） / B 收回（只能靠预览验证） | **分期**：交付前 **A**（冷启动链未修，预览抓不到建单路径的 bug）→ 交付后/补了模型一 **B**。详见 §3.3 |
| **D2** | `document.dept_id` / `sys_user.dept_id` 是否改 DDL | 改 / 不改 | **不改**（§3.6 已论证；只需把 500 换成明确提示） |
| **D7** | 是否本期就做三层角色，还是先只做 `SYS_ADMIN` + `FLOW_ADMIN` | 完整三层 / 先两层 | **先两层**（第三层业务角色已存在，不动；`AUDIT_ADMIN` 留到客户提等保要求时再加） |
| **新** | `FLOW_ADMIN` 的转办/终止/催办是否本期开发 | 本期 / 下期 | 若本期只收窄 `FlowRuntimeService:393` 而不提供转办，管理员将失去所有救济手段。**建议本期至少做"转办"** |
| **新** | 是否保留 `flow:intervene:approve_on_behalf`（§3.4 选项乙） | 保留（默认不授予） / 不保留 | **不保留**。P3 一旦开口子，实际使用中必然被常开 |
| **新** | `role_data_scope` 非法枚举是否改为显式报错 | 是 / 否 | **是**（P5）；当前静默降级 self 是"功能都能点、数据对不上"这类问题的源头 |

---

## 8. 附：证据索引

| 结论 | 证据位置 |
|---|---|
| 25 点中 14 点零引用（原记 15，见 1.2 更正） | `sys_permission` 全表（`minimal_seed.sql:55-80`）× 全项目 `@RequirePerm` grep × 前端 `can()` grep × `scripts/check_perm_reachability.py` |
| 建单接口无门控 | `oa-document/.../DocumentController.java:52-56`（仅 `@Audit`） |
| ~~审批接口无门控~~ **已修**（2026-09-23 补门控） | `oa-document/.../TodoController.java:48-51`、`:67-70`、`:76-79` —— 三处均为 `@RequirePerm("document:approve")`；**本行原记为"无门控"，已作废** |
| 4 处硬编码 | `FlowRuntimeService.java:438`（原记 393）、`DocumentService.java:511`（原记 481）、`AttachmentService.java:146`、`:169` |
| `hasPerm` 零调用 | `LoginUser.java:51`（定义）；全项目 grep 无其他命中 |
| ADMIN 自锁无护栏 | `UserAdminService.java:186-220`（`update`，无 self 判断）vs `:239-243`（`delete`，有） |
| 内置角色修改无护栏 | `RoleAdminService.java:151/180/191`（无 `isBuiltin` 检查）vs `:203-207`（`delete`，有） |
| 数据范围静默降级 | `DataScopeType.java:35-45`（`of()` 返 `SELF`）；`RoleAdminService.java:282-304`（不校验枚举） |
| admin 无部门 | `minimal_seed.sql:47-48`（INSERT 无 `dept_id` 列） |
| `document.dept_id` NOT NULL | `oa-backend/sql/schema.sql:368` |
| 行级权限单一实现 | `oa-common/util/DataScopeHelper.java:43-101` |
| 前端菜单只门控 3 项 | `海峡金OA审批系统-联调版.html:24` |
| 前端 `can()` 定义 | `海峡金OA审批系统-联调版.html:904-907` |
| 冷启动链四处 | `AssigneeResolver.java:121`、`DocumentService.java:84-85`、`FlowRuntimeService.java:120-122`、B5 自审移除 |
| 主流产品分层实现 | 2026-09-23 查证：泛微 e-cology / 致远 A8 / 泛微 eteams / 钉钉·企业微信（详见 `修复与交付待办-2026-09-23.md` §13.7） |
| **"管理员有无部门"的分界线** | §3.8。**泛微 e-cology** `HrmResourceManager`（系统管理员信息表）≠ `HrmResource`（人员主表）——官方定义为"系统级管理员元数据表"；密码重置语句 `UPDATE HrmResourceManager ... WHERE loginid='sysadmin'`。**致远 A8** `org_member`（人员档案）vs `ORG_PRINCIPAL` 里的预置账号 `system`/`group-admin`/`admin1`/`audit-admin`。**泛微 eteams** 官方帮助「权限管理中心」原文（管理员=角色成员、分级管理员、下级权限受上级范围限制）。**钉钉** 官方文档「设置管理员权限」原文（子管理员从企业通讯录选择 + 管理范围） |

---

## 9. 一页纸结论（给决策者）

1. **`ADMIN` 的"全部权限"是假的**：25 个权限点里 15 个没有任何地方在读，真正的管理员特权藏在 4 处 Java 硬编码里。
2. **不是"admin 需要不需要部门"的问题，是"管理员该不该同时是审批人"的问题。** 该拆开。拆开后 admin 自然需要一个语义中立的根部门。
3. **4 处硬编码中有 3 处替换后行为完全等价**，风险集中在第 1 处 —— 它当前让管理员能替任何人做审批决定，这是系统里唯一真实的越权。
4. **先收口代码，再动角色编码**，顺序颠倒会导致管理员权限静默丢失。
5. 改造**不新增表、不新增实体、不改 DDL**，只是把 1 个角色拆成 2 个、把 14 个装饰权限点删掉、把 4 处硬编码变成 4 个权限点。
6. **"系统管理员有部门"的不是私有化产品，是云产品。** 泛微 e-cology 的 `sysadmin` 存在独立表 `HrmResourceManager`、致远 A8 的四个预置管理员账号存在 `ORG_PRINCIPAL` —— **都不在组织架构里、都没有部门**；有部门的是泛微 eteams、钉钉这类**管理员就是通讯录成员**的云产品。
7. 本项目是**私有化交付，却没有第二张账号表** ⇒ 它复制了"不给部门"的形态，复制不了"不进组织架构"的机制。所以 **admin 挂根部门是补偿，不是模仿**；长期正解是补模型一（独立运维账号表），届时发单权可以光明正大收回。

---

## 10. 根因重构设计（Root Design）—— 把不变量放进结构，而不是放进每一处调用点

### 10.0 阶段一为什么不能收尾

§3（拆角色 + 挂根部门 + 收口 4 处硬编码 + 加护栏）是**阶段一**：零 DDL、可立即执行，但它是**在现有结构上做防御**，
没有改变"下次还会长出同类 bug"的条件：

| 阶段一的做法 | 为什么还会复发 |
|---|---|
| admin 挂根部门 | 下一次有人把某个用户的 `dept_id` 置空，NPE 照旧 |
| 逐个过滤 candidateIds | 新增一个"选人"查询就会漏掉过滤（**和"15 个零引用权限点"是同一个失效模式**） |
| `check_perm_reachability.py` 脚本拦截 | 靠事后扫描，不写脚本的环境照样长 |
| 4 处 `hasRole` 改权限点 | 第 5 处会被写出来（现在已经 4 处，说明"写一次"的门槛很低） |

**共同特征：都把不变量放在"每一处调用点的纪律"里。** 本项目自己的硬约束 4 早就总结过这个教训
——"行级权限只准一份实现 `DataScopeHelper.buildClause()`；**新增'可见吗'判断禁止再写 switch**（曾有 IDOR）"。
同一个道理：**不变量必须进结构，不能进纪律。**

### 10.1 根因：`sys_user` 一张表干了三件事

```
sys_user 实际承担了三个正交概念：

  ① 登录主体（Principal）    account / password / pwd_reset_flag / last_login_at
  ② 组织成员（Member）       company_id / job_no / dept_id / post_id / status
  ③ 权限持有者（Authority）  被 user_role / user_post 指向
```

**三个概念的正交性现在是靠巧合维持的**：
"能被解析为审批人的人必须是组织成员"——这条不变量**从来没有被表达过**，
它只是"恰好所有 `sys_user` 都是组织成员"这个事实的副产品。
而 `minimal_seed.sql` 把 admin 塞进 `sys_user` 却不给部门，**第一次打破了这个巧合**，
于是必然炸出一串（B1 预览 NPE / B2·B4 建单 500 / B5 自审移除 / B4 幽灵单）。

同理，4 处 `hasRole("ADMIN")` 是因为**"超管"这个概念在数据里没有位置**——
`sys_role` 只能表达"这个角色有哪些权限点"，表达不了"这个主体是运维账号、
不属于组织、不参与业务"，所以只能写进代码。

**⇒ 一句话根因：系统缺少 `Principal`（登录主体）这一层抽象。**

### 10.2 核心改造：主体-成员分离

```sql
-- 新增：登录主体（所有人都在这里，含运维账号）
sys_principal (
  id              BIGINT PK AUTO_INCREMENT,
  company_id      BIGINT NOT NULL,
  account         VARCHAR(64)  NOT NULL,
  password        VARCHAR(100) NOT NULL,
  principal_type  VARCHAR(16)  NOT NULL,   -- 'MEMBER' | 'OPS'
  status          TINYINT      NOT NULL,   -- 1 启用 0 停用
  pwd_reset_flag  TINYINT      NOT NULL,
  last_login_at   DATETIME     NULL,
  created_by, updated_by, created_at, updated_at, deleted,
  UNIQUE KEY uk_principal_account (account, deleted)      -- 沿用 uk_user_account 语义
);

-- 改造：人员档案（组织成员的"扩展表"，一对一）
sys_user (
  principal_id    BIGINT PK,                -- ← 主键改为指向 sys_principal
  company_id      BIGINT NOT NULL,
  job_no          VARCHAR(32)  NOT NULL,
  real_name       VARCHAR(32)  NOT NULL,
  phone, email,
  dept_id         BIGINT NOT NULL,          -- ← 从 NULL 改为 NOT NULL（见 10.3）
  post_id         BIGINT NULL,
  status          TINYINT NOT NULL,
  created_by, updated_by, created_at, updated_at, deleted,
  UNIQUE KEY uk_user_company_jobno (company_id, job_no, deleted)
);
-- account / password / pwd_reset_flag / last_login_at 从 sys_user 移除
```

**运维账号（OPS）只有 `sys_principal` 行，没有 `sys_user` 行。** 仅此一条，就同时解决四件事：

| 问题 | 为什么结构性消失 |
|---|---|
| admin 需要部门吗 | **不需要，而且是合法终态** —— OPS 不是组织成员，`dept_id` 无从谈起 |
| 运维账号被解析成审批人（含硬约束 16① 的"废人卡死"） | 所有候选集查询都要 `JOIN sys_user` ⇒ **join 不上，不是"过滤掉"** |
| 运维账号出现在选人下拉/人员列表 | 同上，人员列表天然只来自 `sys_user` |
| 冷启动链 B1/B2/B5 | admin 不建单、不做审批人 ⇒ 这三条**触发路径不存在**（B4 幽灵单是独立竞态 bug，仍须按 §十二补 0-4 修） |

### 10.3 关键设计取舍：`dept_id` 改成 `NOT NULL`，而不是"应用层记得判空"

现在 `document.dept_id` 是 `NOT NULL`，而 `sys_user.dept_id` 可空 —— **这个错配就是 B4 建单 500 的根因**
（`DocumentService.java:84-85` 把 `user.getDeptId()` 原样写进一个 NOT NULL 列）。
两种修法：

| 修法 | 性质 |
|---|---|
| 应用层判空 + 抛业务提示（§3.6 的方案） | 非法状态仍可入库，每次写码都靠人记得检查 |
| `sys_user.dept_id` 改 `NOT NULL` | 非法状态**写不进去**，错误在"创建员工时"暴露，而不是在"发单时 500" |

⇒ 取后者。同一个原则套用：`sys_principal.principal_type` 用 `NOT NULL` + 枚举（不用可空的 `role_code` 暗示）。
—— 两者不是"快慢两个方案"，是同一件事的两个位置：前者治症状，后者是同一不变量在**结构**上的落点。

### 10.4 迁移代价：**业务表零改动**（这是能下决心的关键）

因为 `sys_principal.id` **直接复用旧 `sys_user.id` 的值**，所有指向用户的外键列**数值不变**：

| 指向用户的列 | 全库出现次数 | 迁移动作 |
|---|---|---|
| `created_by` / `updated_by`（审计字段） | 18 + 14 = **32** | **不动**（数值语义不变） |
| `user_role.user_id` / `user_post.user_id` | 7 | **不动** |
| `department.leader_id` | 1 | **不动**（`verify_assignee.sql` 无需改） |
| `document.applicant_id` / `flow_*` `assignee_id` | 4 | **不动** |
| `notification.receiver_id` / `flow_delegation.delegator_id`·`delegate_id` / `attachment.uploader_id` | 5 | **不动** |

**⇒ 需要动的只有：1 张新表 + 1 张表改造 + Java 侧查询口径。业务表 DDL 与业务数据完全不动。**
这就是为什么"主体分离"在这套代码里**比听起来小得多** —— 它的耦合面只有登录入口和极少数查询。

**Java 侧改动面（逐一列清，便于估工）**：

| 模块 | 改动 |
|---|---|
| `AuthService.login()` | 查 `sys_principal` 校验密码；`MEMBER` 再 join `sys_user` 取部门/岗位 |
| `LoginUser` | 新增 `principalType`；`deptId/deptName/deptPath` 改为 **OPS 时显式 null 合法** |
| `SysUserMapper` 3 个查询 | 加 `JOIN sys_principal p ... AND p.principal_type='MEMBER'`（同时修掉硬约束 16① 的 `leader_id` 校验缺失） |
| `UserAdminService` | 人员的增删改作用在 `sys_principal` + `sys_user`（一个事务写两张） |
| `DocumentService.create` | 去掉"没部门"的分支 —— **因为写不进去** |
| 新增 `OPS` 管理入口 | 建/停用运维账号（独立于人员管理） |

### 10.5 配套：把"3 个失效模式"一起结构性消灭

根因修完之后，还有三类**同源**问题（都是"靠纪律不靠结构"），一并处理才叫彻底：

**(a) 权限点声明与落地脱节（15/25 零引用）**
- 后端：`PermCode` 枚举 + `@RequirePerm(PermCode.X)`；
  **启动时**双向校验 `sys_permission` ↔ `PermCode`：**库里多一个 = 启动失败；枚举多一个 = 启动失败。**
- 前端：不硬编码 code，由 `/api/permissions`（**该接口已存在**）下发 + 构建期生成常量模块。
- ⇒ "装饰品权限点"从"事后 grep 才发现"变成"**服务起不来**"。这比 `check_perm_reachability.py`（事后扫描）强一个量级。

**(b) 特权判定散落（4 处 `hasRole`；基线 5 处 → 现 4 处，`TodoService:210` 那处已随一次修复消失）**
- 删 4 处：3 处等价替换为 `hasPerm("attachment:manage:any")`，1 处直删（§3.4）；
- **然后删掉 `LoginUser.hasRole(String)` 方法**（§3.4.1）—— 调用者归零后方法不存在 ⇒ 第 5 处**编译不过**。
- ⚠ **不用 ArchUnit**：一是要加新依赖（本项目 `mvn -o` 离线构建），二是**测试型护栏在本项目根本不执行**
  —— 全部构建入口都带 `-DskipTests`（§4.2 脚注）。删方法才是真护栏，且与测试无关。
- ⇒ 从"要求人记得"变成"**编译不过**"。

**(c) 管理范围维度缺失（做不了分级管理员）**
- `DataScope`（看多少数据）保留；**新增 `AdminScope`（管哪些部门的人）**，与前者正交。
- 这正是钉钉的"管理范围"、eteams 的"下级权限只能在上级范围内勾选"那一维。
- ⇒ 分级管理员从"要改架构"变成"**加一列配置**"。

### 10.6 迁移路径（5 步，每步可回滚，不中断服务）

| 步 | 动作 | 判据 | 回滚 |
|---|---|---|---|
| **M1** | 建 `sys_principal`，**双写**：`UserAdminService` 与登录同时维护新旧两处；跑数据比对（`principal` 与 `sys_user` 的 account/password 逐行一致） | 比对脚本零差异 | 删表（无外部引用） |
| **M2** | 登录切到 `sys_principal` 读密码，`sys_user` 仅作归档；`LoginUser` 加 `principalType` | 登录 E2E 全绿；审计字段落库正常 | 切回 `sys_user` 读 |
| **M3** | 为每个 `sys_user` 建 `principal_id` 映射（**id 复用以保证业务表零改**）；`user_role`/`user_post`/`leader_id` 语义切到 principal 空间 | 审批人解析用例全绿；`verify_assignee.sql` 原样通过 | 保留旧列，双写期不删 |
| **M4** | 去掉 `sys_user` 的 `account/password/pwd_reset_flag/last_login_at`；`dept_id` 改 `NOT NULL`；建 OPS 账号并**回收 admin 的组织成员身份** | 新库冷启动全链路通（建单→审批→办结，无幽灵单） | 加回列（数据在 principal 里，不丢） |
| **M5** | 上 `PermCode` 枚举 + 启动校验 + `AdminScope`（`hasRole` 已在阶段一删除，此处只补枚举与范围维度） | 服务启动即校验；分级管理员可配 | 分步开关，可单独回退 |

**⚠ M4 与 M5 的强制顺序不变（沿用 §4.1）**：**必须先把 4 处 `hasRole` 收口完，才允许回收 admin 的角色编码** ——
否则 `ADMIN` 改名/撤角色会让那 4 处判定**静默失效**。

### 10.7 成本与收益

| | 阶段一 · 零 DDL（§3） | 阶段二 · 主体分离（§10） |
|---|---|---|
| 改动量 | 约 30 行 + 种子若干（含删 `hasRole` 方法） | 1 张新表 + 1 张表改造 + 5 个模块的 Java 改动 |
| 业务表 DDL / 业务数据 | 不动 | **同样不动**（id 复用） |
| 风险集中点 | 几乎无 | **登录链路**（唯一高风险处），用 M1 双写 + M2 切换分阶段降到可控 |
| 冷启动 4 个症状 | 逐个修 | **3 个结构性消失**，第 4 个（幽灵单）仍需单独修 |
| 越权路径（代他人审批） | 靠删一处硬编码 | **路径不存在**（OPS 不是成员 ⇒ 不是 assignee） |
| 下次再长出同类 bug | **会** | **结构上不会** |
| 分级管理员 / 等保三员分立 | 做不到 / 要改架构 | 加配置即可 / 拆角色即可 |

**触发条件（不是二选一，是先后）**：阶段一**交付必须**，任何情况下都做。
阶段二在下列条件任一成立时就该排进来 —— 在此之前它就是"已设计、待触发"：

- 这套系统要**再交给下一家客户**（每多一家就多一次"重新踩同类坑"）；
- 要过**等保**（三员分立要求"系统管理员/安全管理员/审计员"职责分离，现结构里表达不了）；
- 要在客户端启用**部门级/分级管理员**（需要 `AdminScope` 这一维）；
- 允许**运维账号代他人审批**这件事被审计方追问（阶段一只删了这一条路径，阶段二让它不存在）。

### 10.8 与第 3 章的关系（不冲突，是两段）

```
阶段一（§3~§9）   零 DDL：删 4 处硬编码、加自锁护栏、补权限点门控、挂根部门
阶段二（§10）     主体-成员分离 + 不变量结构化
```

两段做的是同一件事的两层：阶段一把**现有结构下的错误行为**清掉，
阶段二让**那类错误无法再被表达**。顺序由 §4.1 / §10.6 的同一条强制约束决定
（先收口代码，再动角色编码），不是两个可选路线。

**⇒ §3.8.5 里写的"长期正解是补模型一，本期不做"，现在更正为：正解已给出（本章），
是否本期做取决于 §10.7 的触发条件，而不是"技术上看不清怎么做"。**
