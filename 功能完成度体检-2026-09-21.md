# 功能与业务完成度体检（2026-09-21）

> 起因：用户要求"检查完成错误的或者未完成的功能和业务"。
> 本文只写**查证过的**结论，每条都附证据（文件行号 / 命令 / 数据）。
> 顺带交付了可重复运行的 `scripts/audit_api_surface.py`（接口面交叉核对）。

---

## 一、结论摘要

| # | 类型 | 问题 | 证据 | 影响 |
|---|---|---|---|---|
| 1 | **缺陷** | 顶栏通知角标写死 `:value="3"` | `oa.html` 顶栏 `♧` 图标；同一页面的侧栏两个角标却是正确绑定（`:value="unreadCount"`、`:value="todos.length"`） | 角标永远是 3，与实际未读数无关。用户会以为有 3 条未读，或以为角标坏了 |
| 2 | **未完成** | 审计日志没有界面入口 | 后端有 `GET /api/audit-logs`（`AuditLogController`，只有 `system:audit` 能读）；`oa.html` 里"审计"出现 **0** 次；单据详情页签只有「全流程 / 附件资料 / 关联单据 / 申请表信息」 | 原型文档承诺"谁、何时、对哪条单据做了什么"，**后端在写、接口能查，但界面上看不到** |
| 3 | **未完成** | 动态表单（表单模板）前端完全未落地 | 后端有 `GET /api/forms/schema`、`GET /api/forms/templates`、`POST /api/forms/validate`；这三个路径在 `oa.html` 里出现 **0** 次 | 原型 §9 把「表单模板驱动 + 特殊表单插槽」列为**关键架构决策**，目前只做了后端 |
| 4 | **未完成** | 岗位管理前端未接 | 今天新加的 4 个 `/api/posts*` 接口在 `oa.html` 里出现 0 次 | 已知，属本轮后续（部门/岗位/字典管理界面） |
| 5 | **语义缺口** | 登出**不吊销 token** | 前端 `logout()` 只做 `setToken('')` 等本地清理，**不调** `POST /api/auth/logout`（该路径在 HTML 里 0 次）；而后端 `AuthController.logout()` 本身是**空实现**（只回"已登出"） | 登出后旧 token 在 12 小时（720 分钟）内**仍然有效**。共用电脑上"退出"了但别人拿到过就能继续用 |
| 6 | **待你定** | 菜单写死在前端，`GET /api/auth/menus` 无人调用 | 该路径 0 次出现；侧栏 9 项是硬编码的 | 是"刻意取舍"还是"未完成"，文档里没写，需要你定 |
| 7 | **数据卫生** | 演示库遗留早期 E2E 造的岗位 | `post` 表有 `POST_001 测试岗`、`POST_002 UI测试岗` | 演示数据里混着两个不属于业务的岗位 |

## 二、已核对为「正常」的（免得被误以为没查）

- **菜单 9 项 ↔ 页面分支 9 个，一一对应**，没有"点进去是空白页"的菜单。
  （模板里那个 `<el-empty :description="pageNames[page]+'功能区'">` 兜底目前是死代码。）
- **前端调用的 43 个接口路径，后端全部存在** —— 0 个"点了必然 404"的风险。
- `pageNames` 覆盖全部 9 个页面，标题/面包屑不会出现 `undefined`。
- 没有 `TODO / FIXME / 待实现 / 未实现` 标记（只有一处无害的"模板兼容占位"注释）。
- 抽样检查了 `newFlow / editFlow / refreshRisks / removeAttachment` 等处理函数，
  **都是真实实现，不是"只弹个提示"的桩**。
- 曾怀疑的"通知角标"问题已定位为**只有顶栏那一个**是写死的，侧栏两个是对的。

## 三、本次体检**没有**覆盖的（诚实声明）

- **业务规则本身的正确性**：例如审批链是否符合海峡金的实际制度、金额阈值是否合理。
  这需要领域知识，不是我能"自证"的，得由业务方确认。
- **点击级交互**：由 277 条 E2E 覆盖，但它只能覆盖**已经写出来的用例** ——
  没写用例的地方它一样不查。
- **权限矩阵是否合理**：当前只有 `admin` 拥有 `system:dept` / `system:dict` / `system:audit`，
  其余 8 个账号都没有。这是"现状"还是"配置缺失"，需要你确认。

## 四、建议的修复顺序

| 优先级 | 事项 | 成本 | 理由 |
|---|---|---|---|
| P0 | 顶栏写死角标 | 1 行 | 用户直接看得见，且是纯粹的疏漏 |
| P1 | 登出吊销 token | 前后端各一处 | "看着登出了其实没登出"属于安全问题，且改动小 |
| P2 | 审计日志界面 | 中（后端已就绪） | 用户拿不到"可审、可查"的价值；后端与 E2E 都已就位，只差 UI |
| P3 | 岗位/部门/字典管理界面 | 大（本轮已选） | 你已确定要做，做完就能摆脱"改库" |
| P4 | 动态表单前端 | 大 | 需先确认是否还要按原型做动态表单；不做的话建议把三个接口标注为"保留未用" |
| — | 演示库两个测试岗位 | 1 条 SQL | 顺手清掉 |

## 五、怎么重复跑这次体检

```bash
# 接口面交叉核对（前端调的路径 vs 后端提供的路径）
python3 scripts/audit_api_surface.py
```

⚠ 这个脚本本身**踩过三次假报**（把 `/api/users/` + id 归一成 `/api/users`、
漏掉"变量在末尾"的拼接、把查询串当路径段）。三次都记在脚本注释里了。
**它的"没报错"不等于"都没问题"**：它只比路径形状，不校验参数语义与业务规则。
