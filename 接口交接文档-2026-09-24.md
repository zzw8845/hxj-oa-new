# 海峡金 OA · 接口交接文档

> ## ⚠ 本文档是**内部备查底稿，不发给公司前端**
>
> 2026-09-24 拍板：**接口文档改由源码注释承载**，通过 IDEA 的 **Apifox Helper** 插件同步生成。
> 前端同事看的是 **Apifox 上的接口**，不是本文。所以：
>
> - **要改接口说明，改源码里的 Javadoc**（类注释 / 方法注释 / `@param` / 字段注释），别改本文。
> - 同步规则见工程根目录的 `.apifox-helper.properties`；同步后的效果预览见
>   `接口契约/apifox-preview.html`（按 Apifox 解析规则做的静态模拟，含覆盖率与剩余缺口）。
>
> **保留本文的理由**：它记录的是**代码注释里给不出来的**东西 ——
> §0「自动生成文档有哪 4 处在骗人」、§2 错误语义表、§4 端点 × 权限门控/审计矩阵、
> §7 已知缺口。这些属于**评审与验收证据**，与"给前端看的字段说明"是两件事，刻意不重复。
>
> ---
>
> 面向**接手对接的同事**。接口本身已完成并跑通，本文只讲"怎么接、哪些地方会踩"。
> 配套文件：`接口契约/openapi.json`（机器可读的权威结构，75 path / 99 操作 / 125 schema）。
> **先读 §0 与 §1 再动手** —— §0 讲的是"生成出来的那份文档哪几处在骗你"。

**基准** —— 每个数字都标了来源，别混用：

- **§1 / §2 / §3**（认证、错误语义、五个坑）：2026-09-24 在本机 `127.0.0.1:8080`（默认 profile、`haixiajin_oa` 库）**实调实测**所得。
- **§4 端点清单**：**源码静态扫描**（口径写在 §4 开头）+ 与**离线契约**双向核对；权限点引用数另与 `scripts/check_perm_reachability.py` 交叉验证。
- **§5 枚举 / §7 缺口**：从源码常量与注解读出，标了文件行号，可复核。
后端：Spring Boot 3.2.5 + Flowable 7 + MyBatis-Plus + MySQL 8 + JDK 17，包名 `com.hxj.oa`。

---

## §0 ⚠ 先看这个：自动生成的接口文档有 4 处不可信

把 `OA_OPENAPI_ENABLED=true` 打开就能得到一份 OpenAPI 3 文档。它能用，但**下面 4 处会把人带沟里**，凡是与本文冲突的，以本文为准。

| # | 生成文档里的样子 | 真实情况 | 后果 |
|---|---|---|---|
| 1 | 几乎每个接口的 responses 只声明 `400` / **`500`** | 业务失败是 **HTTP 200 + body `code≠0`**（`BizException` 的 handler 没有 `@ResponseStatus`，于是 springdoc 按默认给了 500） | 你会把"业务校验失败"当成**服务端崩溃**去报障；或者按 HTTP status 判失败，**永远判不出来** |
| 2 | `GET /api/documents` 的参数是 `{"name":"query","in":"query","required":true}` | Spring 把 query 绑定到 `DocumentQuery` 对象，springdoc 展不开字段 ⇒ 生成一个**假的 `query` 参数** | 你在 swagger-ui 上**看不到真正要传哪些参数**。真实参数见 §4 |
| 3 | spec 里**没有 `securitySchemes`** | 所有 `/api/**`（除 `auth/login`、`ping`）都要 `Authorization: Bearer <token>` | 你会以为接口不需要认证，调通第一个就 401 |
| 4 | 99 个操作**全部没有 summary/description** | 源码里 `@Operation`/`@Tag` 注解数为 **0** | 只有路径名可读，语义全靠猜；本文 §3 §4 补这部分 |

**它可信的部分**：路径、HTTP 方法、请求/响应 DTO 的结构、schema 名、字段类型、必填标记。这些直接照抄没问题。

---

## §1 认证与三个通用约定

**登录**：`POST /api/auth/login`，body `{account, password}` → `data.token`（JWT，HS512）。

- 有效期 **12 小时**（`expiresIn: 43200` 秒）。⚠ **没有 refresh 接口**：到期必须让用户重新登录，前端要处理"用着用着 401"。
- 之后每个请求带 `Authorization: Bearer <token>`。缺 token / token 失效 → **HTTP 401 + `code:401`** → 前端应清本地并跳登录页。
- **登出** `POST /api/auth/logout`：服务端把当前 token 的 `jti` 拉黑（不是只清前端）。注意与"前端清 localStorage"分开处理。
- 当前用户信息 `GET /api/auth/me`；菜单 `GET /api/auth/menus`；权限点列表 `GET /api/auth/permissions`（前端 `can()` 判断用它）。

⚠ **最容易被忽略的一条**：**角色与部门是"登录时刻的快照"**。JWT 里装的是整个 `LoginUser`（含 `roleCodes`/`permCodes`/`deptId`）。
⇒ 管理员改了某人的角色或部门后，**那个人必须重新登录**才生效；**没有"刷新权限"的接口**。
前端如果做"改完角色立刻生效"的交互，是做不到的，别把它当 bug 追。

**统一响应包装**（所有接口一致）：

```json
{ "code": 0, "msg": "success", "data": { ... }, "timestamp": 1790229970695 }
```

`code=0` 为成功。**分页**统一返回 `data: { records: [...], total: n, ... }`（schema 名形如 `PageResultDocument`），请求参数是 `pageNum` / `pageSize`。

---

## §2 错误语义（照抄这张表判错）

| 情形 | HTTP | `body.code` | 前端应该怎么做 |
|---|---|---|---|
| 成功 | 200 | 0 | 正常取 `data` |
| **业务规则失败**（金额超限、状态不允许、找不到单据…） | **200** | ≠0（多为 400 / 404 / 409） | **弹 `msg`**。这是最主要的一类失败 |
| 参数校验失败（`@Valid`） | 400 | 400 | 表单在校验前先自己拦一遍 |
| 路径/参数类型错（`/api/users/abc`、日期格式错） | 400 | 400 | 属于调用方写错，开发期就该发现 |
| 请求体不是合法 JSON / 字段类型不符 | 400 | 400 | 同上 |
| 未登录 / token 失效 / 已登出 | 401 | 401 | 跳登录 |
| 已登录但无权限 | 403 | 403 | 提示"无权限"，别跳登录 |
| 真正的服务端异常 | 500 | 500 | 这才叫服务故障，看后端日志 |

**一句话**：**判失败看 `body.code`，不要看 HTTP status**；HTTP status 只用来区分 401 / 403 这两种"路由级别"的情况。

⚠ 已知的一处**有意的不一致**：业务失败（HTTP 200 + `code:400`）与参数类失败（HTTP 400 + `code:400`）**`code` 相同而 HTTP status 不同**。
对接时**统一按 `code` 分支**即可，不要去猜 HTTP status。

---

## §3 五个最容易踩的契约（都是实测出来的，不是推测）

### 3.1 `formData`：请求是对象、响应是字符串

- 请求（`POST /api/documents` 的 `formData`）：**`Map<String,Object>` 对象**。
- 响应（`document` 里的 `formData`）：**字符串**（JSON 文本）。

⇒ 对接时 **`JSON.parse` 一次**才能拿到字段。按请求形态去读响应会静默拿到 `undefined`（不报错，只是表单字段全是空）。

### 3.2 `POST /api/todos/approve` 的 `action` 只接受两个值：`approve` / `reject`

- 传其它值（含拼错的 `aprove`、语义不符的 `withdraw`）→ 返回 `code≠0` + `msg`「不支持的审批动作：xxx」。
- ⚠ **不要把它和 `DocumentDetailVO.availableActions` 混为一谈**：后者的取值是 `approve` / `reject` / `withdraw` / `countersign` / `supplement`，**是"这个单据对当前用户开放哪些动作"的列表，不是可以直接塞给 `/todos/approve` 的值**。`withdraw` 对应的是 `POST /api/documents/{id}/withdraw`。

`action` 的取值 → 接口映射：

| 想做的事 | 接口 | 参数 |
|---|---|---|
| 通过 | `POST /api/todos/approve` | `action: "approve"` |
| 驳回 | `POST /api/todos/approve` | `action: "reject"` + `rejectTo` |
| 加签 | `POST /api/todos/countersign` | `taskId`, `userId` |
| 批量通过 | `POST /api/todos/batch-approve` | 只支持通过；驳回必须逐条 |
| 撤回（申请人） | `POST /api/documents/{id}/withdraw` | 单据级，不是任务级 |

### 3.3 `rejectTo` 的语义（⚠ 文档别写"上一节点"）

| 传值 | 实际行为 |
|---|---|
| 不传 / 空 / `"START"` | **终止当前实例，退回发起人**，发起人改完需重新提交 |
| 具体 `nodeKey`（如 `"n3"`） | 跳回该节点重审（`changeState`） |
| `"PREV"` | ⚠ **当前与 `START` 等价**（实现里把两者一起排除出"跳回指定节点"），**不是"上一个节点"**。需要退回上一节点请传具体 `nodeKey` |

发起人可选的 `rejectTo` 建议由前端按**该单据实际在跑的那一版流程**的节点动态生成，不要写死。

### 3.4 `DocumentDetailVO.flowVersion` 不是流程版本

它的来源是 `document.form_template_ver` —— **表单模板的版本号**（`DocumentService` 里 `vo.setFlowVersion(doc.getFormTemplateVer())`）。
⇒ 想定位"这张单跑的是哪一版流程"，**不要拿它去比对流程配置的版本**；用 `flowName`（流程名）匹配流程配置更可靠。

### 3.5 `POST /api/flows/configs/{id}/preview` 的返回

- 返回的是**完整配置链路**（条件分支网关的**两侧目标节点都在**），**不会按金额裁剪**。⇒ 不要把它当作"这张单将会走的路径"展示，它是"配置上可能的路径"。
- 每个节点上，**解析出来的审批人字段是 `candidateNames`**（姓名数组）/ `candidateIds`；另有 `hitRules`（命中了哪条指派规则）、`selfSkipped`（与发起人同一人而自动跳过）、`unresolved`（解析不出人）。
- ⚠ 没有 `assignees` 字段。（`GET /api/flows/configs/{id}` 返回的 `assignees` 是**另一个东西**：按 `nodeId` 索引的**指派规则表**，不是审批人。）这两个名字很像，混淆后不会报错，只会安静地把"已解析出人"显示成"没解析出人"。

---

## §4 端点清单（99 个操作 / 20 个控制器；其中 46 个有接口门控、49 个写审计日志）

> **这一节的每个数字都能追到源码，不是估的**：
>
> 1. 逐方法解析源码：取「本条 `@XxxMapping` 之后 → 下一条 Mapping 或方法签名之前」这个窗口里的
>    `@RequirePerm` / `@Audit`。⚠ 用「注解后取 N 个字符」的窗口会**取到下一个方法的注解** ——
>    本项目因此先后算出过 63 / 64 / 76 三组错数，全部作废，以下是修正后的值。
> 2. 与离线契约 `接口契约/openapi.json` **双向核对**：契约 75 path / 99 操作 ↔ 扫描 99 操作，**路径集合零差异**。
> 3. 与 `scripts/check_perm_reachability.py` 交叉验证：该脚本独立统计出「声明 25 个权限点、有引用 11 个」，
>    与本节「46 个接口共引用 47 次、涉及 11 个权限点」（`GET /api/permissions` 一个接口引用 2 个）**完全吻合**。

**权限列**：接口门禁层要求的权限点。`—` = **没有** `@RequirePerm`，**不等于“谁都能干”**（见本节末尾）。
**审计列**：是否写 `audit_log`（`@Audit` 的 `action` 名；49 个动作名互不重复）。

**注解形态**（读源码时容易看漏）：`@RequirePerm` 支持数组 + 逻辑组合 ——
`@RequirePerm({"a","b"})` 要求**全部具备**（默认 AND）；`(value = {...}, logic = RequirePerm.Logic.OR)` 是**任一即可**。
当前只有 `GET /api/permissions` 用了 OR 形式。另外：`@Audit` 只能标在方法上（`@Target(METHOD)`）；
`@RequirePerm` 方法与类都允许，但本项目目前**没有类级用法**。

> 附：`GET /`（`PageEntryController`）是**页面入口**，302 跳 `/oa.html`，不在 `/api/**` 下、不需要认证，
> 也不在 OpenAPI 契约里 —— 所以**不在下面这张清单里**。

### PingController（1）

| 方法 | 路径 | 权限点 | 审计 |
|---|---|---|---|
| GET | `/api/ping` | — | — |

### AttachmentController（5）

| 方法 | 路径 | 权限点 | 审计 |
|---|---|---|---|
| POST | `/api/attachments` | — | `uploadAttachment` |
| GET | `/api/attachments` | — | — |
| GET | `/api/attachments/{id}/download` | — | — |
| GET | `/api/attachments/{id}/preview` | — | — |
| DELETE | `/api/attachments/{id}` | — | `deleteAttachment` |

### AuditLogController（1）

| 方法 | 路径 | 权限点 | 审计 |
|---|---|---|---|
| GET | `/api/audit-logs` | `system:audit` | — |

### DocumentController（9）

| 方法 | 路径 | 权限点 | 审计 |
|---|---|---|---|
| GET | `/api/documents/types` | — | — |
| POST | `/api/documents` | — | `createDraft` |
| PUT | `/api/documents/{id}` | — | `updateDraft` |
| POST | `/api/documents/{id}/submit` | — | `submit` |
| POST | `/api/documents/{id}/withdraw` | — | `withdraw` |
| GET | `/api/documents/{id}` | — | — |
| GET | `/api/documents` | — | — |
| GET | `/api/documents/stats` | — | — |
| GET | `/api/documents/export` | `document:export` | `exportLedger` |

### DocumentTypeAdminController（6）

| 方法 | 路径 | 权限点 | 审计 |
|---|---|---|---|
| GET | `/api/document-types` | — | — |
| GET | `/api/document-types/enabled` | — | — |
| GET | `/api/document-types/categories` | — | — |
| POST | `/api/document-types` | `system:docType` | `createDocType` |
| PUT | `/api/document-types/{id}` | `system:docType` | `updateDocType` |
| DELETE | `/api/document-types/{id}` | `system:docType` | `deleteDocType` |

### EscalationController（2）

| 方法 | 路径 | 权限点 | 审计 |
|---|---|---|---|
| POST | `/api/flows/escalation/run` | `system:flow` | `runEscalation` |
| GET | `/api/flows/escalation/by-document/{documentId}` | `system:flow` | — |

### FormTemplateController（10）

| 方法 | 路径 | 权限点 | 审计 |
|---|---|---|---|
| GET | `/api/forms/schema` | — | — |
| GET | `/api/forms/templates` | — | — |
| POST | `/api/forms/validate` | — | — |
| GET | `/api/forms/templates/versions` | `system:form` | — |
| GET | `/api/forms/templates/{id}` | `system:form` | — |
| POST | `/api/forms/templates` | `system:form` | `createFormTemplate` |
| PUT | `/api/forms/templates/{id}` | `system:form` | `updateFormTemplate` |
| POST | `/api/forms/templates/{id}/activate` | `system:form` | `activateFormTemplate` |
| PUT | `/api/forms/templates/{id}/field-permissions` | `system:form` | `updateFormFieldPerm` |
| DELETE | `/api/forms/templates/{id}` | `system:form` | `deleteFormTemplate` |

### NotificationController（4）

| 方法 | 路径 | 权限点 | 审计 |
|---|---|---|---|
| GET | `/api/notifications` | — | — |
| GET | `/api/notifications/unread-count` | — | — |
| POST | `/api/notifications/{id}/read` | — | — |
| POST | `/api/notifications/read-all` | — | — |

### RiskController（1）

| 方法 | 路径 | 权限点 | 审计 |
|---|---|---|---|
| GET | `/api/risks` | — | — |

### SealController（4）

| 方法 | 路径 | 权限点 | 审计 |
|---|---|---|---|
| GET | `/api/seals` | `document:approve:seal` | — |
| GET | `/api/seals/by-document/{documentId}` | `document:approve:seal` | — |
| POST | `/api/seals/use` | `document:approve:seal` | `useSeal` |
| POST | `/api/seals/return` | `document:approve:seal` | `returnSeal` |

### TodoController（5）

| 方法 | 路径 | 权限点 | 审计 |
|---|---|---|---|
| GET | `/api/todos` | — | — |
| GET | `/api/todos/done` | — | — |
| POST | `/api/todos/approve` | `document:approve` | `approve` |
| POST | `/api/todos/batch-approve` | `document:approve` | `batchApprove` |
| POST | `/api/todos/countersign` | `document:approve` | `countersign` |

### DelegationController（4）

| 方法 | 路径 | 权限点 | 审计 |
|---|---|---|---|
| GET | `/api/delegations/mine` | — | — |
| GET | `/api/delegations/to-me` | — | — |
| POST | `/api/delegations` | — | `createDelegation` |
| DELETE | `/api/delegations/{id}` | — | `cancelDelegation` |

### FlowConfigController（11）

| 方法 | 路径 | 权限点 | 审计 |
|---|---|---|---|
| GET | `/api/flows/configs` | — | — |
| GET | `/api/flows/configs/{id}` | — | — |
| GET | `/api/flows/configs/{id}/bpmn` | — | — |
| POST | `/api/flows/configs/{id}/preview` | — | — |
| GET | `/api/flows/configs/{id}/assignees` | — | — |
| GET | `/api/flows/node-templates` | — | — |
| POST | `/api/flows/configs` | `system:flow` | `createConfig` |
| PUT | `/api/flows/configs/{id}` | `system:flow` | `updateConfig` |
| PUT | `/api/flows/configs/{id}/assignees` | `system:flow` | `updateAssignees` |
| POST | `/api/flows/configs/{id}/deploy` | `system:flow` | `deploy` |
| DELETE | `/api/flows/configs/{id}` | `system:flow` | `retireConfig` |

### AuthController（6）

| 方法 | 路径 | 权限点 | 审计 |
|---|---|---|---|
| POST | `/api/auth/login` | — | `login` |
| GET | `/api/auth/me` | — | — |
| PUT | `/api/auth/password` | — | `changePassword` |
| GET | `/api/auth/menus` | — | — |
| GET | `/api/auth/permissions` | — | — |
| POST | `/api/auth/logout` | — | `logout` |

### DepartmentController（5）

| 方法 | 路径 | 权限点 | 审计 |
|---|---|---|---|
| GET | `/api/depts/tree` | — | — |
| GET | `/api/depts` | — | — |
| POST | `/api/depts` | `system:dept` | `createDept` |
| PUT | `/api/depts/{id}` | `system:dept` | `updateDept` |
| DELETE | `/api/depts/{id}` | `system:dept` | `deleteDept` |

### DictController（5）

| 方法 | 路径 | 权限点 | 审计 |
|---|---|---|---|
| GET | `/api/dicts` | — | — |
| GET | `/api/dicts/{dictType}` | — | — |
| POST | `/api/dicts` | `system:dict` | `createDict` |
| PUT | `/api/dicts/{id}` | `system:dict` | `updateDict` |
| DELETE | `/api/dicts/{id}` | `system:dict` | `deleteDict` |

### PermissionController（1）

| 方法 | 路径 | 权限点 | 审计 |
|---|---|---|---|
| GET | `/api/permissions` | （任一）`system:role` `system:user` | — |

### PostController（4）

| 方法 | 路径 | 权限点 | 审计 |
|---|---|---|---|
| GET | `/api/posts` | — | — |
| POST | `/api/posts` | `system:dept` | `createPost` |
| PUT | `/api/posts/{id}` | `system:dept` | `updatePost` |
| DELETE | `/api/posts/{id}` | `system:dept` | `deletePost` |

### RoleController（7）

| 方法 | 路径 | 权限点 | 审计 |
|---|---|---|---|
| GET | `/api/roles` | — | — |
| GET | `/api/roles/{id}` | — | — |
| POST | `/api/roles` | `system:role` | `createRole` |
| PUT | `/api/roles/{id}` | `system:role` | `updateRole` |
| PUT | `/api/roles/{id}/permissions` | `system:role` | `grantPerm` |
| PUT | `/api/roles/{id}/data-scope` | `system:role` | `dataScope` |
| DELETE | `/api/roles/{id}` | `system:role` | `deleteRole` |

### UserController（8）

| 方法 | 路径 | 权限点 | 审计 |
|---|---|---|---|
| GET | `/api/users` | — | — |
| GET | `/api/users/page` | `system:user` | — |
| GET | `/api/users/{id}` | — | — |
| GET | `/api/users/roles` | — | — |
| POST | `/api/users` | `system:user` | `createUser` |
| PUT | `/api/users/{id}` | `system:user` | `updateUser` |
| PUT | `/api/users/{id}/roles` | `system:user` | `assignRoles` |
| DELETE | `/api/users/{id}` | `system:user` | `deleteUser` |

### 权限点字典（声明 25 个 / 真正生效 11 个 / 零引用 14 个）

`@RequirePerm` 里写的 code 必须能在 `sys_permission` 里被分配出去，否则**没人拿得到它、接口永久 403**。
下表 `后端引用` = 有多少个接口门控用了这个 code（**0 = 声明了但没有任何接口用它**，前端别指望它控制住任何按钮）：

| code | 名称 | 类型 | 后端引用 |
|---|---|---|---|
| `document:menu` | 单据中心 | 菜单 | 0 ⚠ 零引用 |
| `todo:menu` | 我的待办 | 菜单 | 0 ⚠ 零引用 |
| `ledger:menu` | 表单台账 | 菜单 | 0 ⚠ 零引用 |
| `dashboard:menu` | 数据看板 | 菜单 | 0 ⚠ 零引用 |
| `admin:menu` | 系统管理 | 菜单 | 0 ⚠ 零引用 |
| `document:create` | 发起单据 | 按钮 | 0 ⚠ 零引用 |
| `document:view:self` | 查看本人单据 | 接口 | 0 ⚠ 零引用 |
| `document:view:dept` | 查看本部门单据 | 接口 | 0 ⚠ 零引用 |
| `document:view:company` | 查看全公司单据 | 接口 | 0 ⚠ 零引用 |
| `document:export` | 导出台账 | 按钮 | **1** |
| `document:approve` | 审批单据 | 接口 | **3** |
| `document:approve:leader` | 领导审批 | 接口 | 0 ⚠ 零引用 |
| `document:approve:accountant` | 会计审批 | 接口 | 0 ⚠ 零引用 |
| `document:approve:cashier` | 出纳付款办理 | 接口 | 0 ⚠ 零引用 |
| `document:approve:seal` | 用印办理 | 接口 | **4** |
| `document:supplement` | 要求补充材料 | 按钮 | 0 ⚠ 零引用 |
| `document:countersign` | 加签 | 按钮 | 0 ⚠ 零引用 |
| `system:user` | 用户管理 | 按钮 | **6** |
| `system:role` | 角色管理 | 按钮 | **6** |
| `system:dept` | 部门管理 | 按钮 | **6** |
| `system:flow` | 流程配置 | 按钮 | **7** |
| `system:form` | 表单配置 | 按钮 | **7** |
| `system:dict` | 字典管理 | 按钮 | **3** |
| `system:audit` | 审计日志 | 按钮 | **1** |
| `system:docType` | 单据类型管理 | 按钮 | **3** |

⚠ **14 个零引用里，有 6 个是「本该生效却没接上」的**：`document:create`（发起单据）、`document:supplement`（要求补料）、
`document:countersign`（加签）这三个按钮级权限点**后端完全没有拿它做门控**；再加上
`document:approve:leader` / `:accountant` / `:cashier` 三个「按审批角色分权」的点 ——
**后端的审批权实际来自流程节点的指派规则，不是这三个 code**。
另有 5 个 `*:menu` 是纯菜单展示用（前端 `can()` 读，后端无引用属正常），余下 3 个 `document:view:*` 由行级数据范围代替。

⇒ 准确的说法是：**权限体系目前只覆盖「模块级管理权」，还没有覆盖「单据级 / 审批级」。**
完整设计见 `管理员角色与权限设计说明-2026-09-23.md`；`scripts/check_perm_reachability.py` 是这件事的棘轮（只允许缩短，不允许新增零引用点）。

### 关于「没有门控」的 53 个接口 —— 千万不要误读

53 = 99 − 46。它们**不是「忘了加」**，但也**不能一律当成「设计如此」**。分四类，请对号入座：

**① 本来就不该有（7 个）** —— 登录前 / 服务探活 / 「当前登录人自己」：
`POST /api/auth/login`、`POST /api/auth/logout`、`GET /api/auth/me`、`GET /api/auth/menus`、
`GET /api/auth/permissions`、`PUT /api/auth/password`、`GET /api/ping`。
（`PingController` 源码注释写明「刻意不加」。）

**②「每个人自己的东西」（11 个）** —— 内部按**当前登录人**过滤，加权限点反而会把普通员工挡在门外：
- 我的待办：`GET /api/todos`、`GET /api/todos/done`（service 入参就是 `UserContext.require()`）
- 我的通知：`/api/notifications` 4 个（`NotificationController` 源码注释写明「刻意不加」）
- 我的委托：`GET /api/delegations/mine`、`GET /api/delegations/to-me`
- 走**行级数据范围**的读：`GET /api/documents`、`GET /api/documents/stats`、`GET /api/risks`
  （三者加上台账导出，都在同一条 `DataScopeHelper.buildClause()` 上 —— 源码位置 `DocumentService:293`（列表）、
  `:320`（统计）、`:538`（单张可见性）、`RiskService:151`、`LedgerExportService:80`；`RiskController` 注释写明「不加」的理由）

**③ 权限藏在业务层（30 个）—— 必须逐个看，不能只看这张表**：
- **单据类 6 个**（`GET /api/documents/types`、`POST /api/documents`、`PUT /api/documents/{id}`、`POST /{id}/submit`、`POST /{id}/withdraw`、`GET /api/documents/{id}`）：
  放行逻辑是「**你是不是这张单的发起人**」+ 状态机校验；`GET /api/documents/{id}` 走 `assertVisible()`（行级范围，并给「这张单的承办人」额外放行）。
- **附件 5 个**：`DELETE /api/attachments/{id}` 内部判「上传者本人 **或** `user.hasRole("ADMIN")`」——
  ⚠ **这是硬编码的角色字符串，不在权限点体系里**：也就是说「谁是超管」目前有**两套判据**（权限点 / 角色码），
  改角色编码会**静默改变**这几处的行为。全项目共 **4 处**这类硬编码（`grep -rnE "hasRole\(" */src/main` 可复现）：

  | 位置 | 代码在做什么 | 后果 |
  |---|---|---|
  | `AttachmentService.java:146`（`load()`，下载与预览共用） | 单据附件走 `assertVisible`；**尚未挂单据的草稿附件**只有上传者本人可见，且 ADMIN 例外 | 低（ADMIN 能看到别人还没提交的草稿附件） |
  | `AttachmentService.java:169`（`delete()`） | 「只能删本人上传的」，ADMIN 例外 | 中（ADMIN 能删别人的附件；状态校验仍在） |
  | `DocumentService.java:511`（`assertVisible()`） | ADMIN **直接绕过行级数据范围** | 中（ADMIN 能看到全公司单据，包括别人部门的） |
  | `FlowRuntimeService.java:438`（`approve()`） | 非任务处理人但 ADMIN → **可通过/驳回任意单据** | ⚠ **全项目唯一的真实越权点** —— 它绕过的是"审批决定权"，而不只是"可见性" |

  对照：`LoginUser.hasPerm()` 在主代码里**调用次数为 0** —— 也就是说**业务层的判权完全不看权限点**，硬编码角色码是事实上的第二套特权体系。
- **流程配置 6 个**（`GET /api/flows/configs`、`/{id}`、`/{id}/bpmn`、`/{id}/assignees`、`GET /api/flows/node-templates`、`POST /api/flows/configs/{id}/preview`）：
  该控制器**写**接口都有 `system:flow` 门控，**读**接口都没有（读配置不改数据）。
- **表单 3 个**（`GET /api/forms/schema`、`GET /api/forms/templates`、`POST /api/forms/validate`）：预览 / 校验类，前端渲染动态表单要用。
- **部门·岗位·字典·单据类型 8 个读接口**：`GET /api/depts`、`/depts/tree`、`/posts`、`/dicts`、`/dicts/{dictType}`、
  `/document-types`、`/document-types/enabled`、`/document-types/categories`（选择器/下拉要用的主数据）。
- **委托写 2 个**：`POST /api/delegations`、`DELETE /api/delegations/{id}`（只能动自己的委托，内部判定）。

**④ ⚠ 残余敞口：管理类「读」接口无门控（5 个）—— 这是缺口，不是设计**：
`GET /api/users`、`GET /api/users/{id}`、`GET /api/users/roles`、`GET /api/roles`、`GET /api/roles/{id}`。
其中 `GET /api/users` **确属有意为之**（源码注释：选人下拉要在客户端对全量做模糊搜索，所以另开 `GET /api/users/page` 做服务端分页）；
但 `GET /api/users/{id}`、`GET /api/users/roles`、`GET /api/roles*` 属于**待收口** —— 任何登录用户都能拉到角色清单和任意同事的档案。
已列入待办，**尚未修**，别当成既有行为去依赖。

⇒ **结论**：判断「某人能不能调这个接口」**只看这张表是不成立的**，还要看接口内部的数据范围 / 角色判断 / 状态机。
需要时直接问接口负责人；§7 也列了已知缺口。

---

## §5 枚举字典（⚠ 只有 1 个进了 spec，其余必须手抄）

`dataScope` 是唯一被自动生成进文档的枚举（`SELF` / `DEPT` / `CENTER` / `CUSTOM_DEPT` / `COMPANY`），其余都是**裸整数**，文档里看不出来：

**单据状态** `document.status`

| 值 | 含义 |
|---|---|
| 0 | 草稿 |
| 1 | 待审批 |
| 2 | 审批中 ⚠ **不等于办结** —— 单据侧看 **`document.closedAt`** 是否有值（`endedAt` 是 `flow_instance` 的字段，不在单据响应里，写 `document.endedAt` 只会拿到 `undefined`） |
| 3 | 已通过 |
| 4 | 已驳回 |
| 5 | 已撤回 |
| 6 | 已归档 |

**流程节点状态** `flow_instance_node.status`（也是 `TodoVO.nodeStatus` 的取值）

| 值 | 含义 |
|---|---|
| 0 | 待处理 |
| 1 | 处理中 |
| 2 | 已通过 |
| 3 | 已驳回 |
| 4 | 已跳过（如"与发起人是同一人"自动跳过） |
| 5 | 已抄送 |
| 6 | 已撤回 |

**节点类型** `nodeType`

| 值 | 含义 |
|---|---|
| 1 | 审批 |
| 2 | 抄送 |
| 3 | 条件分支（网关，不是人审节点） |
| 4 | 办理 |
| 5 | 发起 |

**会签模式** `signMode`：`1` 或签 / `2` 会签（同时签）。**指派规则类型** `ruleType`：`initiator_leader`（发起人部门负责人）、`dept_role`（某部门某角色）、`biztype_role`（某业务类型某角色）、`role`、`user`、`condition`。

---

## §6 需要重出一份契约时怎么做（⚠ 别在服务器上开）

生成脚本是**离线**的（只在本机跑）：

```bash
# 1) 构建
cd oa-backend && JAVA_HOME="$(/usr/libexec/java_home -v 17)" \
  PATH="$(/usr/libexec/java_home -v 17)/bin:$PATH" mvn -o -q -pl oa-boot -am install -DskipTests

# 2) 本机临时打开 OpenAPI（默认 false，不要改配置文件，用环境变量）
cd .. && OA_OPENAPI_ENABLED=true NO_OPEN=1 bash 启动联调版.command

# 3) 抓取权威结构
curl -s http://127.0.0.1:8080/v3/api-docs -o 接口契约/openapi.json

# 4) 抓完立刻关回去，并确认已回到 404
#    停服务 → NO_OPEN=1 bash 启动联调版.command
curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8080/v3/api-docs   # 必须是 404
```

**为什么必须离线、绝对不能在测试服务器/生产上开**：

`/v3/api-docs`、`/swagger-ui/**`、`/actuator/**` 这几个路径**不在 `/api/` 前缀下**，
而认证拦截器是 `addPathPatterns("/api/**")` ⇒ 它们**根本不经过认证拦截器**，
白名单里有没有都一样是**未认证可读**。

实测打开后 `/v3/api-docs` 一次吐 **117,038 字节（≈114KB）**：全部路径 + 参数 + **125 个 DTO 的字段结构**。
这等于给任何访客一份**完整的攻击面地图**（其中包含尚未加门控的写接口）。
项目此前已因此栽过两次（一次默认 profile、一次 test profile 把默认值写成了 `true`）。

⇒ **交付形态**：**保持线上 `springdoc.*.enabled=false`**，用离线生成的 `openapi.json` + 本文作为交接材料。
如果确实需要"在线可点的 swagger"，那需要**先给 `/v3/api-docs/**` 加鉴权**（改鉴权链，另立任务），不能直接开。

---

## §7 已知缺口（诚实清单，别在联调时才发现）

| 缺口 | 影响 |
|---|---|
| **管理类「读」接口无门控（5 个）** | `GET /api/users/{id}`、`/api/users/roles`、`GET /api/roles`、`/api/roles/{id}` 对**任何登录用户**开放（能看到角色清单、任意同事档案）；`GET /api/users` 是有意为之。详见 §4 ④ |
| **14 个权限点零引用**（占 25 个的 56%） | 其中 6 个是「本该生效却没接上」：`document:create` / `:supplement` / `:countersign` / `approve:leader` / `:accountant` / `:cashier`。**按权限点给前端做按钮控制时，这 6 个点不起作用**。详见 §4 权限点字典 |
| **业务层判权用硬编码角色码，不看权限点** | `hasRole("ADMIN")` 共 4 处（其中 `FlowRuntimeService:438` 是唯一真实越权点）；`hasPerm()` 调用数为 0。详见 §4 ③ |
| 99 个操作无 summary/description | 语义靠本文 §3/§4 补；后续补 `@Operation` 注解即可自动改善 |
| spec 缺 `securitySchemes` | 生成文档不体现 Bearer 认证；需补 `@SecurityScheme` |
| `GET /api/documents` 等"POJO 绑 query"的接口参数展不开 | swagger-ui 上只能看到一个假的 `query` 参数，需手工补 `@Parameter` 或改用 `@RequestParam` 平铺 |
| 错误语义无机器可读表达 | 只有本文 §2 这张表；若前端想按错误码做分支，需补错误码字典 |
| `company` 无接口 | 公司主体目前只能改数据库；已列入待办 |
