# 交付数据清单 —— 系统交付给客户时，库里到底有什么

> 核实方式：**实测**。另建临时库把 `init_database.sql` + 两个种子脚本分别灌了一遍，
> 逐表 `COUNT(*)`。临时库与临时文件已全部删除，演示库 `haixiajin_oa` 未触碰。
> 日期：2026-09-23

---

## 一、结论一句话

**交付态 = 70 张空表 + 1 个公司壳 + 1 个 admin 账号 + 25 个权限点。**
没有部门、没有岗位、没有字典、没有单据类型、没有表单模板、没有流程、没有单据。

客户拿到手的是一个 **"能登录、但一张单都发不起来"** 的空系统，全部要靠配。

---

## 二、第一个脚本：`init_database.sql`（只有结构）

| 项 | 实测 |
|---|---|
| 库名（脚本自建） | **`hxj-oa`**（注意是连字符，不是下划线） |
| 表数 | **70**（29 张业务表 + 41 张 Flowable `ACT_*`/`FLW_*`） |
| 业务数据 | **0 行** |
| 那 16 条 INSERT 是什么 | 全是 Flowable 自己的版本标记：`ACT_GE_PROPERTY` 15 条 + `ACT_ID_PROPERTY` 1 条。**不是种子数据** |

---

## 三、第二个脚本：**两个候选，口径不一致** ⚠️

这是本次最需要拍板的地方。**两份文档指向不同的脚本**：

| | `minimal_seed.sql`（今天新建） | `seed_data.sql`（交接文档 §9.2 指定的） |
|---|---|---|
| 自称 | "客户从零开始使用的起点" | 演示/联调数据 |
| 公司 | 1（福建海峡金投资有限公司） | 1（同上） |
| 部门 | **0** | **8**（总经理室/财务中心/财务核算部/资金结算部/内控合规中心/综合管理中心/行政管理部/业务一部） |
| 岗位 | **0** | 8 |
| 用户 | **1**（admin） | **9**（admin/张总/李财务/王会计/赵出纳/陈内控/林经理/周综合/黄小明） |
| 角色 | **1**（ADMIN 超级管理员） | 8 |
| 权限点 | 25 | 25 |
| 角色权限绑定 | 25 | 76 |
| 数据范围 | 1（ADMIN = company） | 8 |
| 字典项 | **0** | 17 |
| 单据类型 | **0** | 3（日常付款申请 / 员工报销 / 用印申请） |
| 表单模板 | **0** | 3 |
| 字段权限 | **0** | 4 |
| 流程定义 | **0** | 3（日常付款 / 员工报销 / 用印申请） |
| 流程节点 | **0** | 18 |
| 指派规则 | **0** | 12 |
| 业务单据 `document` | 0 | **0**（脚本不含） |
| 通知 `notification` | 0 | **0**（脚本不含） |
| **合计业务行** | **32 行** | **约 187 行** |

### `minimal_seed.sql` 的 32 行明细（实测全文）

| 表 | 行数 | 内容 |
|---|---|---|
| `company` | 1 | 福建海峡金投资有限公司 / HXJ |
| `sys_user` | 1 | `admin` / 系统管理员 / **dept_id=NULL** / post_id=NULL |
| `sys_role` | 1 | `ADMIN` 超级管理员（is_builtin=1） |
| `role_permission` | 25 | ADMIN → 全部 25 个权限点 |
| `role_data_scope` | 1 | ADMIN → `company`（全公司） |
| `user_role` | 1 | admin → ADMIN |
| `sys_permission` | 25 | 5 个菜单 + 20 个按钮（见下） |

25 个权限点（这就是客户登录后能看到的**全部菜单来源**）：

```
菜单：单据中心 / 我的待办 / 表单台账 / 数据看板 / 系统管理
单据：发起单据 / 查看本人·本部门·全公司单据 / 导出台账
审批：审批单据 / 领导审批 / 会计审批 / 出纳付款办理 / 用印办理 / 要求补充材料 / 加签
系统：用户管理 / 角色管理 / 部门管理 / 流程配置 / 表单配置 / 字典管理 / 审计日志 / 单据类型管理
```

---

## 四、三处交付硬伤（实测发现）

### 硬伤 ①：`init_database.sql` 重复执行会失败，与自身文档承诺相反

脚本头部第 13 行原文：

> 【幂等】全部为 `CREATE DATABASE/TABLE IF NOT EXISTS`，重复执行不会报错，也不会动已有数据。

**实测为假**：70 条建表语句里只有 **29 条**带 `IF NOT EXISTS`，**41 张 Flowable 表全是裸 `create table`**
（第 740 行 `create table ACT_GE_PROPERTY` 是第一处）。第二次执行：

```
ERROR 1050 (42S01) at line 740: Table 'ACT_GE_PROPERTY' already exists
退出码 1 → 脚本从此处中断
```

**影响**：
- 自动化部署脚本（`set -e`）会直接失败；
- 运维按文档"可重复执行"重跑建库，会拿到一个非 0 退出码 + 中断；
- 修法：给那 41 条补 `IF NOT EXISTS`，或改文档措辞。

### 硬伤 ②：`seed_data.sql` 的流程是"未部署"状态

实测 `flow_config` 3 条：`proc_def_key = NULL`、`deploy_status = 0`。

也就是说灌完 `seed_data.sql` 后，**3 条流程都还没部署**，必须再进「流程管理」页手动部署一次。
否则表现就是 MEMORY 里记的那条：**"能登录、能建单，一提交就报错"**，且报错不指向建库脚本。

这就是交接文档 §9.2"空库上线三层"里第 ③ 步存在的原因 —— 但它只写在文档里，脚本没有自检提示。

### 硬伤 ③：脚本自建库名 `hxj-oa`，容易被灌错地方

`init_database.sql` 内含 `CREATE DATABASE IF NOT EXISTS \`hxj-oa\`` + `USE`。
所以 `mysql -uroot 某个库 < init_database.sql` 是**无效**的 —— 表会静默建到 `hxj-oa` 去，
目标库里 **0 张表**（本次实测踩到，第一次导入后 `COUNT(*)=0` 才反应过来）。

另外本地默认库名与部署库名不一致：

| 场景 | 库名 |
|---|---|
| `init_database.sql` 自建 | `hxj-oa` |
| `docker-test/.env`（部署） | `hxj-oa` ✅ 一致 |
| `application.yml` 本地默认 | `haixiajin_oa` ⚠️ 不一致 |
| `application-test.yml` | `hxj-oa` ✅ |

**灌库时不要带库名参数**，直接 `mysql -uroot < init_database.sql`。

---

## 五、交付前必须拍板的两件事

### ① 灌哪个种子？

- 如果客户**就是**海峡金 ⇒ `seed_data.sql` 可以直接用，但它带 9 个假账号（张总/李财务/…）、
  8 个部门、3 个假单据类型和 3 条假流程，**上线前必须逐条核对是否符合客户真实组织**。
- 如果客户**不是**海峡金，或希望干净起步 ⇒ 用 `minimal_seed.sql`，
  从"只有 admin"开始，在界面上建组织和业务（这条路径已实测跑通，见 `零数据冷启动体检-2026-09-23.md`）。

> ⚠️ `项目交接说明.md` §9.2 目前写的是 `seed_data.sql`，与 `minimal_seed.sql` 的自我定位冲突。
> **交付前必须把两份文档改成一个口径。**

### ② 9 个演示账号的密码

`seed_data.sql` 里的账号若共用弱密码（如 123456），
在测试服务器还挂着公网域名的情况下是**最大敞口**（此项在 MEMORY 的"剩余待做"里已挂多时）。

---

## 六、附：不确定项

- 测试服务器 `hxj-oa` 库当前是**哪一层**状态，本次未连服务器核实（本地无服务器凭据）。
  按交接文档 §9.2 + §394-395，应为「70 张表 + 主数据（9 用户/8 角色/25 权限点/3 单据类型/3 流程配置）
  + 流程已部署」。**建议交付前实地核一次** `deploy_status` 与 `document` 行数。
