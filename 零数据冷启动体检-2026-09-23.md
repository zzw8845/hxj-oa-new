# 零数据冷启动体检 —— 「只有一个 admin 从零开始」能不能跑通

> 实测方式：另起一个**隔离实例**（port 8081）+ **全新空库** `hxj_oa_oneadmin_test`，
> 全程只调 HTTP 接口（等价于点界面），**完全不碰演示库 `haixiajin_oa`**。
> 实测环境已回收（实例已停、临时库已 DROP），本报告是唯一产物。

---

## 一、先回答你的问题

> 「新系统开始用，是不是要把基本数据建好呢，人员、部门、权限、角色啥的，然后进行表格流程啥的这些业务呢」

**顺序完全正确**，这就是标准冷启动路径：

```
① 建公司/组织            （company）
② 建部门树 + 每部门指定负责人   （department.leader_id）   ← 最容易漏，也是本次踩坑点
③ 建人员账号 + 归属部门 + 岗位   （sys_user）
④ 建角色 + 给角色配权限点 + 数据范围（sys_role / role_permission / role_data_scope）
⑤ 给人员分配角色          （user_role）
⑥ 建表单模板（可视化设计器，出 schema JSON）
⑦ 建流程：节点 + 条件分支 + 每个节点配「谁审」→ 部署
⑧ 建单据类型，把「表单模板 + 流程」绑到一起   ← 三层配置的枢纽
⑨ 提单 → 审批 → 办结
```

①②③④⑤ **全部可在界面上做**（部门树/岗位/字典、用户管理、角色权限都有页面），
⑥⑦⑧⑨ 也都实测在界面上走通了（单据最终到 `status=3 已通过`）。

**但是**：当前代码有 **2 个阻断性 Bug**，让「只留一个 admin」这个前提直接卡死。
不是配置顺序问题，是代码问题。下面是实测证据。

---

## 二、阻断性 Bug（必须先修，否则 200 次验证第 1 次就挂）

### Bug ①：`initiator_leader` 规则不校验「负责人是否可用」

**位置**：`oa-backend/oa-system/.../mapper/SysUserMapper.java` → `selectLeaderIdByDeptId`

```sql
SELECT d.leader_id FROM department d
WHERE d.id = #{deptId} AND d.deleted = 0 AND d.leader_id IS NOT NULL
--                                            ↑ 只判非空，不判「这个人还在不在、还启不启用」
```

对照：同文件的 `selectUserIdsByRole` / `selectUserIdsByDeptAndRole`
**都带了** `u.status = 1 AND u.deleted = 0`，**唯独负责人这条 SQL 漏了**。

下游 `AssigneeResolver.byInitiatorLeader()`（第 120-133 行）：

```java
Long leaderId = departmentService.leaderIdOf(deptId);
if (leaderId != null) { return List.of(leaderId); }   // 0 也照样返回
```

**实测后果（隔离库真实数据）**：

| 单据 | 部门负责人配置 | 引擎任务 `ACT_RU_TASK.ASSIGNEE_` | 结果 |
|---|---|---|---|
| FK…0001 | dept6.leader_id=**8**（周综合，已停用 status=0） | `8` | 任务挂在停用账号上，**谁登录都看不到、审不了** |
| FK…0002 | dept8.leader_id=**0**（未设负责人） | `0` | 指派给一个**不存在的用户 id=0**，永久卡死 |

**为什么"解析不出人"反而不卡、"解析出个废人"却卡死**：看 `AssigneeTaskListener` 第 69-75 行 ——

```java
if (result.getCandidateIds().size() == 1) {
    task.setAssignee(String.valueOf(result.getCandidateIds().get(0)));   // [0] 也会被设成 assignee="0"
} else {
    for (Long uid : result.getCandidateIds()) { task.addCandidateUser(...); }   // 空列表走这里 = 无人 → auto_skip
}
```

`autoSkipUnassigned()` 的跳过条件是 `task.getAssignee()` 为空。
所以「一个人都没解析出来」→ 空列表 → 无 assignee → **会被自动跳过**（有兜底）；
而「解析出了一个停用的人 / id=0」→ assignee 非空 → **永远不跳过，也不报错，静默卡死**。

> 这是本次最值得记的一条：**静默失败比显式失败危险得多**。
> 客户在界面上看到的是"提交成功了"，然后这张单就消失了。

**修法（两步）**：
1. 治本 —— 改 SQL，join 用户表校验可用性 + 排除哨兵值 0：
   ```sql
   SELECT d.leader_id FROM department d
     JOIN sys_user u ON u.id = d.leader_id AND u.deleted = 0 AND u.status = 1
   WHERE d.id = #{deptId} AND d.deleted = 0 AND d.leader_id > 0
   ```
2. 兜底 —— 在 `AssigneeResolver.resolve()` 出口统一过滤一遍 candidateIds
   （存在 && status=1 && deleted=0），一次覆盖所有规则，
   包括 `byUser` 直接指定了已删用户、`condition` 里写死的 userIds。

第 2 步是**防御纵深**，建议一定要加 —— 否则以后每加一种规则都要重新记得校验一遍。

---

### Bug ②：全节点被跳过时，单据状态被 STARTED 事件覆盖（死单）

**位置**：`FlowRuntimeService.start()` 第 120-125 行

```java
autoSkipUnassigned(pi.getId(), inst);          // ← 内部可能已发 FINISHED（单据置 3 已通过）

eventPublisher.publishEvent(new FlowLifecycleEvent(this, Stage.STARTED, ...));  // ← 又发 STARTED，覆盖回 2 审批中
```

`autoSkipUnassigned()` 第 314-321 行：跳完最后一个节点、引擎实例已结束时，
自己会 `publish(FINISHED)` → `FlowLifecycleListener` 把 `document.status` 置为 `3 已通过`。
紧接着 `start()` 第 122 行**无条件**再发一个 `STARTED` → 又被置回 `2 审批中`。

**实测后果**：

| 单据 | `flow_instance` | `document.status` | 引擎任务 | 界面表现 |
|---|---|---|---|---|
| FK…0003（申请人=审批人） | status=2 已结束，`ended_at` 有值 | **2 审批中** | 0 条 | 待办里没有、已办里没有、跟踪里显示"审批中"→ **幽灵单** |
| FK…0004（删干净后提单） | status=2 已结束，`ended_at` 有值 | **2 审批中** | 0 条 | 同上 |

即：**流程实际已经结束了（该置"已通过"），单据却停在"审批中"，且没有任何待办入口**。
这单就永远躺在那里了。

**修法（择一）**：
- `autoSkipUnassigned()` 返回一个 boolean（"是否已办结"），`start()` 里据此决定要不要发 STARTED；
- 或 `start()` 发 STARTED 前先查一次 `runtimeService.createProcessInstanceQuery()...singleResult()`，
  为 null（实例已结束）就跳过 STARTED。

推荐第一种 —— 判定条件就写在事件产生的地方，不用查两次库。

> 这两个 Bug 在**多人在线的正常公司**里不容易暴露（有人、有负责人），
> 但在你设想的**"全新系统、只有一个 admin"**场景下是 **100% 必踩**：
> 流程第一个审批节点几乎都是"直属部门负责人"，
> 而此时要么部门还没设负责人（leader_id=0 → Bug①），
> 要么负责人就是 admin 自己（申请人=审批人 → 全跳过 → Bug②）。

---

## 三、非阻断缺口（不卡流程，但影响"好不好操作"）

按影响排序：

| # | 问题 | 现场表现 | 建议 |
|---|---|---|---|
| 1 | **自创节点名静默跳过** | 节点名不在 15 个预置模板里时，自动配不到审批人 → 提单后 `auto_skip`，用户完全不知道为什么 | 保存流程时校验节点名，不在模板库就**明确提示**"该节点无法自动置审批人，请手动指定" |
| 2 | **流程保存不可逆** | 保存即生效，无二次确认；表单模板有 `activate`/回退，流程没有 | 加二次确认 + 流程版本回退入口（节点结构变了必须新建版本，这是硬约束） |
| 3 | **权限点只读** | `PermissionController` 只有 GET，界面上无法新增权限点 | 要么补 CRUD，要么在界面上写明"权限点由系统预置" |
| 4 | **业务大类写死 4 个** | DAILY/BIZ/REIMBURSE/SEAL 是枚举，客户加不了新大类 | 若客户业务超出 4 类，需要改成字典表 |
| 5 | **读接口偏松** | 普通员工可读到全部角色列表、流程配置、用户名册 | 按最小权限收紧（列表类接口也要过权限点） |
| 6 | **菜单未按权限隐藏** | `权限管理`/`流程管理`/`风险预警` 菜单没有 `v-if` 权限判断，只是进去后接口报 403 | 菜单加权限门槛，避免"点进去才发现没权限" |
| 7 | **swagger/actuator 默认关** | 新前端对接时没有在线接口文档 | 内网/测试环境可临时打开 `springdoc.api-docs.enabled`，给对接方一份在线文档 |

> 第 7 条对你的第 2 个问题（**其他前端人员好不好对接**）很关键：
> 后端有 swagger 但**默认关闭**，现在给对接方的只有这份 `项目交接说明.md`。
> 建议测试环境开一个只读的接口文档页，对接效率会差一个量级。

---

## 四、建议的验证路径（200 次怎么跑才有意义）

先修 Bug ①②，然后按这个顺序建：

```
第 1 轮：开荒（把骨架搭起来，1 套）
  建 1 个公司 → 建 3~4 个部门 + 设负责人 → 建 5~8 个账号
  → 建 3 个角色（部门负责人/财务/出纳）+ 配权限
  → 建 1 个表单模板 → 建 1 条流程（用预置节点名）→ 建 1 个单据类型
  → 走通 提单 → 审批 → 办结

第 2~N 轮：换场景（复用上面骨架，只换变量）
  变量轴：
    金额（触不触发条件分支：< 门槛 / ≥ 门槛）
    单据类型（付款 / 报销 / 用印 / 自建）
    申请人（不同部门、不同角色）
    审批动作（通过 / 驳回 / 驳回到指定节点 / 撤回）
    审批人形态（单人 / 多人候选 / 会签）
  每条单据一个断言：最终 status、当前节点、待办归属、通知条数
```

**关键**：第 1 轮必须把「部门负责人」全部设上 —— 这是 Bug① 的触发点，
也是真实客户上线时最容易漏的一步。**建议在部署文档里把它列为强制检查项。**

---

## 五、环境与基线核实（本轮无污染）

- 隔离实例：`127.0.0.1:8081`，库 `hxj_oa_oneadmin_test`，独立 JWT 密钥/存储/日志目录 → **已停、已 DROP**
- 演示库 `haixiajin_oa` 基线核对：

| 项 | 基线 | 实测 | 结论 |
|---|---|---|---|
| document | 46 | 46 | ✅ |
| post | 8 | 8 | ✅ |
| seal_apply | 9 | 9 | ✅ |
| seal_record | 0 | 0 | ✅ |
| delegation | 0 | 0 | ✅ |
| escalation | 0 | 0 | ✅ |
| ACT_RU_TASK | 5 | 5 | ✅ |
| notification | 84 | 84 | ✅ |
| form_template | 3 | **5** | ⚠️ 见下 |
| document_type | 3 | **5** | ⚠️ 见下 |

`form_template` / `document_type` 各多 2 条，**不是本轮产物**：

```
document_type id=74  TEST     测试        created 2026-09-23 09:35:13   created_by=1
document_type id=99  TEST_01  测试001     created 2026-09-23 10:18:28   created_by=1
form_template id=82  测试单              created 2026-09-23 09:36:07   created_by=1
form_template id=104 测试001              created 2026-09-23 10:19:00   created_by=1
```

created_by=1（admin）、时间戳集中在 09:35~10:19，是**在演示库界面上手工试的**。
本轮实验全程在 `hxj_oa_oneadmin_test` 里，未触碰演示库。**如需恢复基线，删这 4 条即可。**
