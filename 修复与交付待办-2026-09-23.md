# 修复与交付待办 —— 基于 2026-09-23 全链路审查

> 来源：`项目审查报告-2026-09-21.md`、`后端差距报告-2026-09-22.md`、`功能完成度体检-2026-09-21.md`、
> `零数据冷启动体检-2026-09-23.md`、`自助配置闭环演练-2026-09-23.md`、`交付数据清单-2026-09-23.md`、
> `系统评估与审查-2026-09-23.md`
> 本文是**唯一执行入口**。编号一旦分配不再变更，后续引用直接用编号。
>
> 🧭 **一份方案，两个阶段**（完整设计见 `管理员角色与权限设计说明-2026-09-23.md` §10）：
> - **阶段一**（本文正文 §一~§十三）：零 DDL，交付必须。解决"现在能不能交"。
> - **阶段二**（§十四）：主体-成员分离 + 不变量结构化。解决"下次还会不会长同类 bug"，
>   是"能交给下一家客户 / 过等保 / 分级管理员"的前提。触发条件见设计说明 §10.7。
>   两阶段共用同一条强制顺序：**先收口代码，再动角色编码**。

---

## 〇、总原则（用户 2026-09-23 拍板）

> **除「引导必需的数据」可以种子写入数据库外，其他一切必须走应用。**

这条原则同时**改变优先级**，因为它把"冷启动期只有 admin 一个人"变成了**标准形态**（而不是边缘场景）：

**引导集边界（已核实，逐条必需）：**

| 表 | 行数 | 为什么必须走种子（不可替代） |
|---|---|---|
| `company` | 1 | **无任何写接口**（无 `api/companies` / `CompanyController`），前端也无界面 ⇒ 只能种子 |
| `sys_user` | 1 | 引导死锁：建号需登录、登录需有号；且**全后端 0 处** `CommandLineRunner`/`DataInitializer` 自动初始化 |
| `sys_role` | 1 | 建角色需 `system:role` 权限 ⇒ 无角色即死锁 |
| `sys_permission` | 25 | `PermissionController` **只有 `@GetMapping`**（只读），前端无界面 ⇒ 只能种子 |
| `role_permission` | 25 | 缺则每个接口 403，连"去哪加权限"都找不到入口 |
| `role_data_scope` | 1 | 缺则 `DataScopeHelper:52` 静默降级 `SELF`（只看自己单据） |
| `user_role` | 1 | 缺则 admin 没有任何权限 |

⇒ **`minimal_seed.sql` 的 32 行全部是引导必需，一行都不能省。**
⇒ **真正该退役的是 `seed_data.sql`（187 行演示数据）** —— 它的 8 部门 / 9 用户 / 8 角色 / 3 单据类型 /
3 表单模板 / 3 流程 / 17 字典项**全部都能走应用创建**（界面齐备，冷启动演练已证明）。

**由原则直接推导出的两个新待办：**
- 公司信息**没有编辑入口** ⇒ 交付后客户想改公司名只能进库，**违反原则**（见 N1）
- 权限点不可创建 ⇒ 需明确决定"预置不可变"还是补 CRUD（见 N2）

---

## 一、⚠ 关键路径分析：冷启动期会**串联**踩到 4 个坑

按总原则，种子**不带业务数据** ⇒ 客户第一次用系统时是"只有 admin 一个人"。
这条路径不是并列的四个 bug，而是**一条链**：

```
① admin 无部门（minimal_seed 刻意不给，见下方 ⚠ 口径提醒）
     ↓ 撞
② B1 流程预览 500（NPE）/ B4 建单 500（dept_id NOT NULL 无默认值）
     ↓ 修好前两个后，admin 必然"建部门 + 把 leader 设成自己"
③ 提单人是 admin，n2「直属部门负责人」解析出 admin 本人 → 自审被移除（C1）
   → 其余节点（会计/总经理/出纳角色）在单 admin 下全部解析为空
     ↓ 撞
④ 全节点被跳过 → autoSkipUnassigned 发 FINISHED → start() 又发 STARTED 覆盖
   → 单据停在「审批中 2」且**没有任何待办入口**（B-幽灵单，体检报告 Bug②）
```

**⚠ 口径提醒（2026-09-23 补查，解释"为什么这条链长期没被发现"）**

两份种子的 admin 归属**不一致**，而且**能跑的那份恰是不能交付的那份**：

| 种子 | 用途 | admin 的 `dept_id` | 后果 |
|---|---|---|---|
| `seed_data.sql:68` | 演示 / 交接文档 §9.2 指定 | **6 = 综合管理中心**（一级部门，负责人是 user 8 而非 admin 本人） | **发单不 500、不会自审** ⇒ 演示环境全程正常 |
| `minimal_seed.sql:47` | 交付起点 | **列定义里根本没有 `dept_id`** ⇒ NULL | 触发上述整条链 |

⇒ 开发/联调/演示**全程用 `seed_data`，一切正常**，这条链只在"换成交付种子"时才出现。这就是它被漏掉至今的原因。
另有两点注意：① `minimal_seed.sql:44-46` 的注释**已经写明**"不挂部门……正式使用前请先在界面上建部门并把自己挂进去"——即设计者知道，但**只写在注释里，客户看不到**（这正是 N3 的出处）；② 演示种子里 admin 挂的一级部门负责人是别人，所以 admin 发单走的是"正常上级审批"，**不是**自审路径。

**结论：B1 · B4 · C1 · B-幽灵单 必须作为同一个「冷启动可用性」批次一起修。**
只修前两个，客户立刻撞上后两个，而且第 ④ 个是**静默失败**（界面显示"审批中"，单据就消失了）。

---

## 二、必修 V：先修「验证可信度」（几行代码，但要排在功能之前）

| # | 位置 | 问题 | 改法 |
|---|---|---|---|
| **V1** | `scripts/verify_admin_api.py` / `scripts/verify_attachment_api.py` | 两个文件**通篇无 `sys.exit`/`SystemExit`/`raise`** ⇒ 只打印失败清单、从不设退出码；而 `run_all_verify.sh` 判据就是"退出码非 0" ⇒ **93 条断言（接口侧 299 条的 31%）无论对错永远 ✓**。实测现场：`✓ verify_admin_api 结果：通过 45 项，失败 1 项` | 末尾补 `raise SystemExit(1 if FAIL else 0)`，并补 `EXPECTED_TOTAL` 闸门 |
| **V2** | `scripts/run_all_verify.sh` | `verify_flow_assignee_admin.py`（18 条、**有**闸门）**没挂进用例清单** ⇒ 标准回归永不跑它（而节点指派是核心功能） | 挂进去 |

> **为什么修测试要排在修功能之前**：一份"永远不会红"的回归网**比没有更危险** ——
> 没有会让人警惕，**假绿会让人放心**。且本次要改的是核心链路（B1/B4），更需要真网。

**顺带修掉三个红灯的性质**（都不是被测功能坏了，是测试资产假设过期）：
- `flow_branch_admin`：**硬编码**"演示库 3 单据类型 / 3 流程"，实际 5/5 ⇒ 改为按基线函数动态取值，别写死数字
- `flow_interaction`：夹具待办**承办人为空**，脚本假设"待办必有承办人" ⇒ 夹具要断言自己造出了承办人
- `escalation_api`：`scanned=0`，根因未定；与 `flow_interaction` 的 `escalated=0` 同源，**需单独定位**

---

## 三、必修 N：种子边界与首登引导（由总原则推导）

| # | 事项 | 为什么 | 做法 |
|---|---|---|---|
| **N1** | **公司信息编辑入口** | 公司无写接口 ⇒ 客户想改公司名/简称只能进库，违反原则。且 `init_database.sql` 里公司名是**种子里写死的**，客户大概率要改 | 补 `PUT /api/company`（或公司设置页）+ 界面；权限点归 `system:*` 或单开 |
| **N2** | **权限点：明确"预置不可变"还是补 CRUD** | 现在是"只读且无界面"，属**没表态**。客户若问"我想加一个审批按钮权限"会卡住 | 建议：界面写明"权限点由系统预置"，并在文档里列为设计边界；若客户要求可配置则补 CRUD |
| **N3** | **首登引导**（种子不带业务数据后的必然产物） | 种子只给账号 ⇒ admin 首登时**既无部门、也无单据类型/表单/流程**。现在这条"下一步做什么"只写在 `minimal_seed.sql` 的注释里，**客户看不到**。实测全前端 `引导/向导/onboard/wizard/setup` **0 命中**，唯一的"引导"是发单 500 —— 报错不等于引导 | 复用**已有模式** `mustChangePassword`：后端 `AuthService.login` 探测后置 `LoginResp.onboarding`，前端在 `doLogin`/`switchAccount` 两处照抄 `if (r.mustChangePassword===true) openPwd(true)` 的写法。**必须含第 ④ 步"重新登录"**（见 §三补 1）。**依赖 B1/B4 先修好**，否则引导把人直接引到 500 |

### 三补 1、N3 落地细节与「JWT 快照陷阱」（2026-09-23 新查证，本次唯一新增的技术发现）

**已核实的可复用资产（不用从零写）**

| 位置 | 现状 | 结论 |
|---|---|---|
| `oa-system/.../dto/LoginResp.java:17` | `mustChangePassword` 布尔标志 | 引导照抄这个字段形态即可，前端已有消费代码 |
| `海峡金OA审批系统-联调版.html:1233 / 1251` | `if (r.mustChangePassword===true) openPwd(true)` | **首登强制弹窗的现成范式**：登录响应带标志 → 前端不可跳过弹窗 |
| `LoginUser`（`oa-common/.../security/LoginUser.java`） | 已含 `deptId / deptName / deptPath / companyId` | 前端**无需新增接口**就能判断"没部门"，后端口径也不缺字段 |
| `海峡金OA审批系统-联调版.html:2676` | `if (!newPerson.deptId){ warning('请选择所属部门'); return; }` | ① 引导第 ③ 步**纯跳转即可，无新功能**；② **UI 根本建不出无部门用户** ⇒ 交付态 `admin.dept_id=NULL` 是种子/直写产物，UI 永远造不出这个形状 |
| `主数据 → 部门`（`page==='masterdata'`，行 33/41） | 已有 `＋ 新增一级部门 / ＋子部门 / 编辑(可设负责人 leaderId)` | 引导第 ② 步与"设负责人"**零新功能** |
| 节点指派对话框（行 59） | `initiator_leader` 规则已有"兜底角色 fallbackRoleCode"下拉 | 引导"软步骤"也**零新功能** |

**⚠ JWT 快照陷阱（本次新发现，N3 最容易漏的一环）**

- `JwtUtils` 只做一件事：`claim(CLAIM_USER, payload)`，payload 是**整个 `LoginUser` 的 JSON**；解析时反序列化回来（见 `JwtUtils.java:43 / 70`）。
- ⇒ `deptId / deptName / permCodes` 全部是**登录那一刻的快照**。发单时 `DocumentService.java:84-85` 写 `doc.setDeptId(user.getDeptId())`，取的正是 token 里的旧值。
- 全项目 **无 refresh / 重签 token 接口**（`grep -n refresh oa-system/.../controller/` = 0 命中）。
- **后果**：admin 建完部门、把自己挂进去之后，**当前会话仍是 `deptId=null`**，发单继续 500。客户会得出"这个系统设了部门也没用"的结论 —— 而这正是前面"我明明改了还是报错"的真实成因之一。
- **两个解法（二选一，D6）**：
  - **D6-a（推荐，零后端改动）**：引导把"重新登录"作为显式第 ④ 步，文案写"配置已保存，请重新登录使其生效"，按钮直接 `switchAccount`/`logout`。
  - **D6-b**：补 `POST /api/auth/reissue`（持有效 token 换新 token，内部 `buildLoginUser(userMapper.selectById(uid))`）。好处是以后**任何**改部门/改角色的场景都不用重登 —— 顺带缓解硬约束 #2「权限是登录时刻快照」带来的长期体验问题（本次只做部门，权限仍要求重登）。

**引导文案与分步（可直接进实现）**

- 硬步骤（不配就不能用 → 强制，不可跳过）
  1. 建一级部门 → `主数据 / 部门`（`newDept(null)`）
  2. 把自己挂进该部门 → `权限管理 / 编辑本人`（`editPerson`）
  3. **重新登录**（D6-a）→ 否则 `deptId` 仍是旧快照
- 软步骤（能用，但配好不踩坑 → 首页常驻清单，可跳过、可回访、带完成度）
  4. 给该部门设负责人 → `主数据 / 部门 / 编辑`（`leaderId`）；不设 ⇒ `initiator_leader` 解析不到人
  5. 节点指派加兜底角色 → `流程管理 / 节点指派`；不加 ⇒ 部门无负责人时走到 500（B2）
  6. 建单据类型 → 绑表单 → 绑流程 → **页面部署流程**（§P1 第③步，缺了"能登录能建单、一提交就报错"）

**为什么"检查清单"比"强制向导"更适合本系统**：只有第 1~3 步是"不配就没法用"；第 4~6 步配错了分别对应可观测的不同报错（500 / 人工兜底 / 部署报错），做成强制向导会让客户在配第 6 步（最重的一步）时被迫中断。**强制只放硬步骤**，这也是行业通行做法（GitLab/Jira 首登向导只锁"建组织+建项目"，其余进 checklist）。
| **N4** | ~~`seed_data.sql` 退役~~ | ~~按原则，演示数据也应走应用；它同时扮演"演示数据"和"文档指定的交付种子"两个角色，是 P3 冲突的根源~~ | ✅ **2026-09-24 已完成**（用户拍板「minimal_seed 要的，seed_data 演示的那个不要 —— 直接写库本身就不合理」，比原方案更彻底：**不留 `sql/demo/`**）：① `oa-backend/sql/seed_data.sql` **移入废纸篓**；② 替代品 = `oa-backend/scripts/bootstrap_via_api.py`（**全程走接口**、幂等、`--dry-run`/`--verify`（可提单性自检）/`--smoke`（真实提单））；③ 联动引用全部修正：`scripts/init_and_run.sh:44` 改灌 `minimal_seed.sql`、`oa-backend/README.md`（部署 6/7 步 + 文件树）、`项目交接说明.md`（§三 + 部署三步）、`verify_form_template_api.py` 注释；④ **实测通过**：空库（70 表 + 7 张引导表）→ 脚本 65 项**全新建** → 三种单据「模板生效 + 流程已部署（各 6 节点）」→ 建单提交后待办**精确落到「林经理 · 直属部门负责人」** |
| **N5** | **给引导集加一条契约检查** | 防止以后有人往 `minimal_seed.sql` 里加业务数据，慢慢长回 `seed_data` 的样子 | 加 `scripts/check_seed_scope.py`：断言 `minimal_seed.sql` 只写入白名单 7 张表、且行数不超过阈值（仿照已有的 `check_html_template_balance.py`，挂进 `run_all_verify.sh` 前置自检） |

---

## 四、必修 B：冷启动可用性批次（一条链，一起修）

| # | 位置 | 问题 | 改法 | 改动量 |
|---|---|---|---|---|
| **B1** | `oa-flow/.../AssigneeResolver.java:121` | `Objects.requireNonNullElse(toLong(param,"deptId"), ctx.getApplicantDeptId())` 两参皆 null ⇒ **NPE**，且在 `fallbackRoleCode` 兜底**之前**炸 ⇒ 无部门者流程预览失败 | 改 null-safe 两段取值：`if (deptId == null) deptId = ctx.getApplicantDeptId();`（要的是 `firstNonNull`，不是 `requireNonNullElse`——`require` 是在断言不变量，不是给分支）。**1 行**，全后端仅此 1 处 |
| **B1补** | 触发点与真实表现（2026-09-23 补查，原先描述偏了） | **触发点是"打开发起单据对话框"，不是配流程页**：前端 `openSubmit` 里 `api.flowPreview(fc.id, {bizCategory, amount})`（`HTML:1659`）**不传 applicantId** ⇒ 后端 `FlowConfigController:82` 回落成当前登录用户当申请人。⚠ **且前端 `.catch(function(){})` 把异常静默吞掉** ⇒ 客户看到的**不是报错弹窗**，而是"审批流程那一栏空着"。**这是静默失败，比 500 更难发现**，B1 的定级要按静默失败看 | 修法同上（1 行）；另外**前端不能吞**：`.catch` 里给出可读提示（"未取得流程预览，请检查申请部门是否已设置"），或后端返回可读业务错误而非 NPE |
| **B2** | `sql/init_database.sql:412` + `DocumentService.java:84` | `document.dept_id`/`dept_name` 均 `NOT NULL` 且无默认值，写入方 `doc.setDeptId(user.getDeptId())` **直传不兜底** ⇒ 无部门者建单 500 | 见决策点 **D2**（必填 or 兜底）。**至少要做的**：把裸 SQL 异常换成明确业务提示（"您还未归属部门…"），别抛 500 | 1 处校验 + 1 处赋值 |
| **B3** | `oa-system/.../mapper/SysUserMapper.java` → `selectLeaderIdByDeptId` | SQL 不 join `sys_user` 校验 `status/deleted`，也不排 `leader_id = 0` ⇒ 指派给停用者；而 `autoSkip` 只跳过 assignee 为空的 ⇒ **静默卡死** | ① SQL 加 `JOIN sys_user u ON u.id=d.leader_id AND u.deleted=0 AND u.status=1` + `AND d.leader_id > 0`<br>② **防御纵深**：`AssigneeResolver.resolve()` **出口统一过滤** candidateIds，一次覆盖 `byUser` 已删用户、`condition` 写死 userIds 等所有规则 | 1 条 SQL + 出口 1 处 |
| **B4** | `oa-flow/.../FlowRuntimeService.java:120-125` | `autoSkipUnassigned()` 内部可能已发 `FINISHED`（置 3 已通过），`start()` 紧接着**无条件**再发 `STARTED` ⇒ 覆盖回「2 审批中」⇒ **幽灵单**（状态错且无待办入口） | `autoSkipUnassigned()` 返回 `boolean`（是否已办结），`start()` 据此决定要不要发 `STARTED` | 1 个返回值 + 1 处判断 |
| **B5** | `AssigneeResolver.java:96-100` | 自审**只移除**：`ids.remove(applicantId)` + 标记 `selfSkipped`，**没有"升到上级"这一层**。唯一候选被移除即滑到自动跳过 = **静默少一环审批**（原 C1，因冷启动期 100% 触发而升入必修） | 实现「自审策略」三选一（跳过 / 升上级部门负责人 / 转人工），见 **D3**；并在流程记录里**显式留痕** | 1 个策略分支 + 留痕 |

> **B5 是本批次的关键**：单 admin 冷启动时，"申请人=审批人"是**必然**，
> 而它现在的结果是"静默少一环"，比报错更危险。

---

## 五、必修 P：交付级（纯脚本/文档，零代码风险）

| # | 位置 | 问题 | 改法 |
|---|---|---|---|
| **P1** | `sql/init_database.sql` | 头部承诺"全部 `CREATE ... IF NOT EXISTS`，重复执行不会报错"**为假** —— 70 条建表只有 **29** 条带，41 张 Flowable 表是裸 `create table`（第 **740** 行 `ACT_GE_PROPERTY` 首处）⇒ 二次执行 `ERROR 1050` + **exit 1 中断** | 补 `IF NOT EXISTS`；**注：该文件由 `sql/gen_init_database.sh` 生成，要改生成器，不可手改产物** |
| **P2** | `sql/seed_data.sql` | 3 条流程 `deploy_status=0`（**未部署**），脚本不提示 ⇒ 漏第③步就"能登录能建单、一提交就报错" | 脚本末尾加自检提示（见 N4：该文件本身将退役） |
| **P3** | `项目交接说明.md` §9.2 | **口径冲突**：交接文档让灌 `seed_data`，`minimal_seed` 自称从零起点 ⇒ 执行的人会灌错 | 按总原则统一为：**`minimal_seed` + 首登引导步骤**（N3） |
| **P4** | 测试服务器 | 9 个演示账号弱口令 + 挂公网域名 ⇒ **目前最大敞口** | 改强密码；交付前实地核 `deploy_status` 与 `document` 行数 |
| **P5** | `application.yml:32` vs `docker-test/.env` | 库名不一致（本地 `haixiajin_oa` / 部署 `hxj-oa`）；且 `init_database.sql` 内含 `CREATE DATABASE hxj-oa` + `USE` ⇒ **灌库不能带库名参数**（带了会静默建到 `hxj-oa`，目标库 0 张表） | 文档写死"不带库名参数"；本地默认库名对齐 |

---

## 六、该修 C：正确性与合规（客户会挑，不阻断）

| # | 位置 | 问题 | 改法 |
|---|---|---|---|
| **C1** | `FlowNodeTemplate.java:85,87,132` | 3 个预置节点出厂自带 `{"fallbackRoleCode":"DEPT_HEAD"}`；而 `selectUserIdsByRole` 返回**所有**持该角色者，`AssigneeTaskListener:69` 是 `size>1` 就进**候选池抢单** ⇒ 「我的上级审批」退化为「**任意部门负责人可审**」= **权限放宽，不是降级** | 见 **D4**：换成唯一职责角色（总经理/财务总监），或默认留空=转人工。原则：降级目标必须比上一级**更收敛** |
| **C2** | `AssigneeResolver.resolve()` 出口 + 前端节点指派对话框 | 降级/兜底**不可观测**（只有 `log.info`）；自创节点名解析不出人时**静默跳过** | `hitRules`/降级原因落 `flow_instance_node`，界面标注"该节点由兜底规则指派"；保存流程时对"解析不出人"给明确提示 |
| **C3** | `UserAdminService.java:80,97` | 管理接口只按 `company_id` 过滤，**无 dept/DataScope 过滤** ⇒ 谁拿到 `system:user` 就能管全公司人。**「分级管理员」缺失** | 见 **D5**：需要则引入"管理范围"（与 `DataScopeHelper` 的"数据可见范围"**分开**，别混用） |
| **C4** | `seed_data.sql` role_permission | ADMIN 持有全部 25 个权限点，**含 `document:approve:*`** ⇒ **系统管理员兼业务审批人**，违反职责分离(SoD)。实测构成：**系统层 8 个 + 菜单 5 个 + 纯业务动作 12 个**（`document:create` / `view:self\|dept\|company` / `export` / `approve` / `approve:leader\|accountant\|cashier\|seal` / `supplement` / `countersign`）—— **近一半权限点与"管系统"无关** | 见 **D7**：拆一个纯管理角色（只给 `admin:menu` + `system:*`），业务角色单独建。这也是 admin "需要部门"的根因（详见 §三补 1） |
| **C5** | `scripts/` | 8 个 `patch_frontend_*.py` 一次性补丁脚本仍在库里，前端"源"被脚本改过多次 ⇒ 维护时极易改错文件 | 归档到 `scripts/archive/` 并在 README 标明"历史补丁，勿再执行" |
| **C6** | `RoleAdminService.java:180,191` + `UserAdminService.java:224` | **层级隔离缺失 ⇒ 可自我提权**。`delete` 已保护内置角色（205-207）与"不能删自己"（`UserAdminService:241`），但：① `updatePermissions` / `updateDataScope` **对 `is_builtin=1` 的角色无任何保护**，只 `requireRole(id)` 校验存在；② `updateRoles` **没有"不能改自己的角色"**。⇒ 任何拿到 `system:role` 或 `system:user` 的账号都能**给自己挂上 ADMIN 角色**，或**直接改掉 ADMIN 角色的权限点**。这正是主流 OA 用「子管理员无权设置子管理员」堵住的那条线 | ① `updatePermissions/updateDataScope/update` 拒绝改 `is_builtin=1`（照抄 `delete` 里已有判断）；② `updateRoles` 加"不能修改当前登录账号自己的角色"；③ 若要真做分级管理员，再加"非超管不能给他人授予超管角色" |
| **C7** | `FlowRuntimeService.java:393` 等 4 处 + 全项目端点清点 | **干预能力方向反了：有最危险的，缺最安全的**（2026-09-23 重述，见 §13.0）。① **有**：`assertAssignee` 里 `\|\| user.hasRole("ADMIN")` ⇒ 持 ADMIN 者可**审批任意单据的任意节点**（授权旁路，绕过全部指派规则）；配合 `DocumentService:481` 可看任意单据 ⇒ **界面上就能走通"打开任意单据 → 拿到 taskId → 直接批准"，无需碰库**。② **缺**：转办/改派 无、强制终止/作废 无（`withdraw` 明确"只能撤回本人发起的"）、催办 无。③ 叠加 `#16①` 的必然卡死：`leader_id` 指向停用用户时 `setAssignee(废人)` ⇒ 非空废人**不走 auto_skip** ⇒ 待办挂在登不进系统的人名下；而"改指派规则"**只影响尚未到达该节点的单据** ⇒ **已卡死的单据只能改库** | ① 按 **§13.4** 收口 4 处硬编码：`:393` 改判显式权限点 `document:approve:any` + 强制留痕；`:481` 直接删（冗余且会静默放权）；`AttachmentService:146/169` 改判权限点。② 补**流程管理动作**：转办 / 终止 / 催办（不碰结论）。③ 与 `#16①` 一起修 |

---

## 七、该修 E：表单引擎缺口（去谈表单前必做）

| # | 位置 | 问题 | 改法 |
|---|---|---|---|
| **E1** | `海峡金OA审批系统-联调版.html:2601` `FIXED_FORM_KEYS` | 18 个字段 key 被**静默吞掉不报错**（字段凭空消失，客户会认定是 bug） | 改为**保存模板时明确报错**，或补齐渲染。**别留静默** |
| **E2** | `FormTemplateService.validate()` | `schema.rules.require`（联动必填）**存了但不执行** —— 服务端只读 `fields[].required` | 要么实现，要么**从设计器移除**。"配了不生效"比没有更伤信任 |
| **E3** | 字段类型白名单 8 种 vs 渲染 6 种 | `invoiceGroup`/`attachment` 在白名单但渲染层未实现（退化成文本框） | 补齐渲染，或从白名单移除 |
| **E4** | `FormTemplateService.validate()` | 服务端校验仅 4 类（required / min·max / money 两位小数 / select 白名单） | 按客户真实表单再定，别提前做 |
| **E5** | `form_field_permission` | 字段级权限**只有接口、无编辑界面** | 补界面即可，后端已就绪 |

> 对客户的承诺口径：先只承诺「基础字段 + 必填 + 下拉」，带逻辑的字段规则报二期。

---

## 八、决策点（不拍板会返工）

| # | 决策 | 我的建议 |
|---|---|---|
| **D1** | 交付种子 = `minimal_seed` + 首登引导（总原则已定），演示数据是否移入 `sql/demo/` | 是（N4） |
| **D2** | **两张表的 `dept_id` 要不要 NOT NULL**（原表述把两者混为一谈，2026-09-23 拆开） | **拆开处理**：① `document.dept_id` **保留 NOT NULL** —— 单据是业务凭证，无部门的单据在台账/数据范围/看板里就是残缺数据；② `sys_user.dept_id` **保持可空**（NOT NULL 会挡掉 HR 同步、批量导入、离职待归档等合法中间态，属把约束加错地方）。无论哪种，**都必须把 500 换成明确提示**（"您还未归属部门，请到主数据建部门并把自己挂进去"），并且 admin 这个账号**要挂一个语义中立的根部门**（演示种子已做对：`seed_data.sql:68` admin → `dept_id=6` 综合管理中心）|
| **D3** | 自审策略：跳过 / 升上级 / 转人工 | **升到上级部门负责人**，可配；纯跳过在合规上站不住 |
| **D4** | 兜底角色换成谁 | 唯一职责角色（总经理/财务总监），或默认留空=转人工 |
| **D5** | 客户是否金融/国企、是否需满足等保 | 是 ⇒ C3/C4 提前到必修；否 ⇒ 可后置 |
| **D6** | **引导第 ④ 步怎么做**（本次新发现，决定 N3 工作量） | **a（推荐）**：引导显式提示"重新登录生效"，零后端改动；**b**：补 `POST /api/auth/reissue` 重签 token，顺带缓解硬约束 #2「改角色要重登」的长期体验问题。**a 是必须做的，b 是可选加分** —— 不做 b 的话第 ④ 步不能省 |
| **D7** | **管理员是否分层**（对照主流 OA 后新增） | 主流做法是**四层分开**（创建者超管 / 分级管理员 / 模块管理员 / 业务角色）+ **「管理范围 × 权限」正交可配** + 「子管理员无权设置子管理员」。本系统是**压成一层**（ADMIN 全绑 25 个权限点，含 12 个纯业务动作）。**建议分两步**：① 零成本 —— 权限点目录在界面上按「系统层 / 业务层」分组展示，让客户一眼分清管理员角色与业务角色；② 按客户规模 —— 真做分级管理员（数据结构已具备：`role.dept_id` + 数据范围 + `is_builtin`，缺的是约束与界面语义）。**C4/C6 是它的安全后果，建议至少把 C6（防自我提权）立刻做掉** |
| **D8** | **admin 的发单权：保留（挂根部门）还是拿掉（纯配置）** —— 回答"admin 到底需不需要部门" | **现状"有发单权 + 无部门"是唯一自相矛盾的组合，不能保持**（能发单但一发就错）。**⚠ 权重已按 §13.7 修正：出口 A 是主线** —— 本系统只有 `sys_user` 一种用户实体（无独立后台账号表）⇒ 天然属"模型二：管理员=组织成员" ⇒ **admin 本就应该有部门**，跟发单权无关。**A · 给它一个部门**：admin 挂「公司本部」根部门，`document:create` 保留；改动 0、交付即可验证；与泛微云版/钉钉做法一致。**B · 拿掉发单权**：撤 `document:create`，只在"避免技术报错"这一层成立，从模型看是**折中而非正解**；仅在"客户不接受 admin 有部门"时采用，且采用 B 必须同时拿掉发单权。**建议：走 A** |

### 八补、决策点详解（2026-09-23 补充，供拍板用）

#### D2 · 两张表的 `dept_id` —— 结论比想象中简单：**DDL 不用动**

实测 `sql/schema.sql` 的现状**已经是我们想要的样子**：

| 表 | 现状（schema.sql 行号） | 是否符合预期 |
|---|---|---|
| `document.dept_id` | **NOT NULL**（368） | ✅ 对 |
| `sys_user.dept_id` | **NULL 可空**（107） | ✅ 对 |
| `sys_role.dept_id` | NULL 可空（147） | ✅ 对 |
| `user_post.dept_id` | NOT NULL（129） | ✅ 对 |

⇒ **D2 不需要改 DDL。** 真正要改的是两个"人"：

1. **写入方**（`DocumentService:84-85`）：`doc.setDeptId(user.getDeptId())` **直传不兜底**。用户无部门 ⇒ 往 NOT NULL 列写 null ⇒ **裸 SQL 异常 ⇒ 500**。
   → 改成写入前校验，抛可读业务提示：**"您还未归属部门，请先到「主数据 → 部门」建部门并把自己挂进去"**。
2. **校验方**（`UserSaveReq.deptId`）：**建议保持不加 `@NotNull`**。原因：加了会挡掉 HR 同步、批量导入、离职待归档这些**合法中间态**——约束加错了地方。前端已经必填（`HTML:2676`），够了。

> **所以 D2 的实质是"把 500 换成明确提示"，不是"加约束"。** 原表述把它写成二选一，是把问题复杂化了。

#### D3 · 自审策略三选一 —— 可行性已核实

现状（`AssigneeResolver.java:96-100`）：`ids.remove(applicantId)` + 标记 `selfSkipped`，**只移除、没有"升到上级"这一层**。唯一候选被移除 ⇒ 空 ⇒ 滑到 auto_skip。

三种做法的实现代价（**都只需改 `resolve()` 里那 5 行**）：

| 策略 | 实现 | 代价 | 评价 |
|---|---|---|---|
| **跳过**（现状） | 无 | 0 | 静默少一环审批。合规上站不住（金融/国企必被挑） |
| **升上级部门负责人** | 自审被移除后，若 `ids` 为空：`departmentService.getById(applicantDeptId).getParentId()` → `leaderIdOf(parent)` 加回 `ids` | **小**。`DepartmentService` 已有 `getById`(79) / `leaderIdOf`(94)，**无需新增 Mapper** | ✅ 推荐。不丢审批环节 |
| **转人工** | 自审被移除后为空 ⇒ 指派给"流程管理员"角色（需新增一个角色锚点） | 中。要新增角色 + 引导配置 | 最稳，但多一个配置项 |

**必须一起处理的边界**：申请人所在部门是**一级部门**（`parentId = 0`）时**没有上级** ⇒ "升上级"会再次落空。此时要么降级到"转人工"，要么升到**公司负责人**角色。**这条边界不处理，"升上级"会变成一个隐蔽的空转。**

#### D7 · 管理员分层 —— 拆成"零成本"和"按规模"两段

| 段 | 内容 | 代价 | 何时做 |
|---|---|---|---|
| **第一段（零成本）** | ① 权限点目录界面按「系统层 8 / 菜单 5 / 业务层 12」分组展示；② **C6 自锁保护**（见 §十二补 0-2） | 前端分组 + 后端约 15 行 | **建议现在就做**。C6 是安全/事故项，不该等 |
| **第二段（按客户规模）** | 真做分级管理员：「管理范围 × 权限」两个维度 + 「子管理员无权设置子管理员」+ 拆出纯管理角色 | 中等。**数据结构已具备**（`sys_role.dept_id` / `role_data_scope.scope_type` / `is_builtin`），缺的是约束与界面语义 | 客户明确提分级诉求时（金融/国企基本跑不掉） |

> 注意：**"拆出纯管理角色"（C4）不等价于"做分级管理员"**。前者只是把 ADMIN 的 12 个业务权限点拿出来另建一个角色，**改的是种子/配置，不是代码**；后者才需要动权限模型。**C4 可以零代码先做。**

---

## 九、执行顺序（顺序有依赖，别乱）

```
第 0 步  V1 · V2              ← 先把回归网变成"真的会响"（几行代码）
第 1 步  D2 · D3              ← 拍板两个决定 B 批次形态的决策
第 2 步  B1 · B2 · B3 · B4 · B5  ← 冷启动可用性批次，**同一条链，必须一起修**
第 3 步  N3 首登引导           ← 依赖 B 批次修好；否则提示了也还是 500
第 4 步  N1 · N2 · N4 · N5     ← 公司编辑入口 / 权限点表态 / 演示数据退役 / 种子契约检查
第 5 步  P1 · P2 · P3 · P5 · P4  ← 交付脚本与文档
第 6 步  C2 · C5               ← 可观测性、补丁脚本归档
第 7 步  E1 · E2               ← 见客户谈表单前必做
第 8 步  C3 · C4               ← 按 D5
第 9 步  E3 · E5 · E4 · C1     ← 有时间再做
第 10 步 I1 · I2               ← 实施工具链（批量导入 / 配置包迁移），按客户规模定
```

**两条不能违反的顺序理由：**
1. **V 在 B 之前** —— 没有可信回归网，改核心链路无法验证；而且"假绿"会让人误以为改对了。
2. **B 批次内部不能拆** —— 只修 B1/B2，客户会撞 B5 + 幽灵单，且幽灵单是**静默失败**（界面显示"审批中"，单据就消失了）。

---

## 十、如果只做三件事

1. **V1 + V2** —— 让回归网会说真话（几行代码，立刻能验出那 93 条里藏了多少真失败）
2. **B1 + B2 + B3 + B4 + B5** —— 冷启动可用性批次（这是"客户第一次打开能不能用"的全部）
3. **N3 + N4 + P1 + P3** —— 首登引导 + 演示数据退役 + 交付脚本幂等 + 文档统一口径

---

## 十一、建议 I：实施工具链（对照行业后新增，优先级取决于客户规模）

> 行业里"让客户走应用"这件事不是靠自觉，是靠**工具链**兜住的。本项目缺两件：

| # | 事项 | 行业通行做法 | 现状 | 建议 |
|---|---|---|---|---|
| **I1** | **组织 / 人员批量导入**（Excel/CSV） | 组织架构从 HR 系统或 Excel **批量导入**，一次灌几百人；这是实施效率的分水岭 | **无**（`MultipartFile` 的命中全是附件上传 + 台账导出，无任何组织/人员导入） | 客户人数 > 50 就是刚需，否则只能一个个建或写脚本调 API。建议做「部门 + 人员」两个导入（含模板下载、导入前校验、失败行回显） |
| **I2** | **配置包导出 / 导入**（流程 + 表单跨环境迁移） | 实施顾问在**测试环境配好 → 导出为配置包 → 生产环境导入**；配置是可版本化、可迁移的资产 | **无**。只能在目标环境**重新配一遍** | 当前"测试服务器配好 → 生产重配"必然产生偏差（且无版本记录）。建议至少支持「流程 + 表单模板 + 单据类型 + 节点指派」四件的导出/导入 |

**对照后确认「已对齐」的几项**（无需改）：
- 首登强制改密（行业标配）✅
- 默认管理员 + 内置权限点（对应别人的"内置角色/权限"）✅
- 配置即交付的四步闭环 ✅（行业方向一致，差的是上面的工具链）
- 审批人由**规则**决定而非硬编码人 ✅（这是行业正确做法）

### 十一补、管理员分层的行业事实（2026-09-23 查证，对应 D7）

| 产品 | 分层叫法 | 关键机制 |
|---|---|---|
| 钉钉 | 主管理员 / 子管理员 / 部门管理员（群主） | **「管理范围」与「管理权限」是两个正交可配的维度**；可建"管理组"（一组权限模板，≤200 人/组）；**子管理员无权设置子管理员**；有创建者标识的主管理员唯一、不可删、只能转让 |
| 企业微信 | 创建人·超级管理员 / **分级管理员** | 同上语义 |
| 简道云 | **系统管理员** / 普通管理员 | 系统层与业务层管理员分开命名 |
| 泛微 e-cology 9 | 超级管理员(admin) / 系统管理员角色 / **模块功能管理员** | RBAC+ABAC 混合；**"传统'高级角色'不再默认包含系统管理能力"**，系统管理必须显式勾选；有「组织应用分权」「组织权限转移」「矩阵组织授权」「六维权限（组织/角色/操作/模块/对象/数据）」 |
| 钉钉（考勤为例） | 企业主管理员 / 考勤打卡应用管理员 / 考勤组主负责人 / 子负责人 | **四层**，且明确"数据隔离：各部门考勤数据仅对相应管理员可见" |

**三条可直接借用的结论**
1. **分层不是"系统层 vs 业务层"两个平面，而是至少三层**（创建者超管 → 分级管理员 → 模块管理员）+ 业务角色另算。
2. **「能管什么」和「能管哪些部门」必须拆成两个独立可配项** —— 本系统 `role.dept_id` + `data_scope` 恰好是这两个维度的雏形，只是没在界面上表达出来。
3. **层级隔离是防提权的核心机制**（"子管理员无权设置子管理员"）。本系统缺这条 ⇒ 对应 **C6**。

---

## 十二、立即执行批次（2026-09-23 按投入产出比重排）

> 补入 **C6（自我提权）** 与 **C7（流程干预缺失）** 之后，§九 的顺序需要重排两处：
> ① **C6 升到最前** —— 它是安全项，不该排在功能之后，且改动量 10 行；
> ② **C7 拆成两半** —— 根因（`#16①`）放批次 0（一行 SQL），干预工具（转办/终止/催办）另列待做。

### 批次 0 · 立刻做（零决策依赖，合计 ≈ 30 行，一次构建可验证）

| # | 事项 | 改动量 | 为什么排最前 |
|---|---|---|---|
| **0-1** | **V1 · V2** —— `scripts/verify_admin_api.py` / `verify_attachment_api.py` 补 `sys.exit(1)` | 约 4 行 | **所有后续修改的前提**。当前这两个脚本**无退出码** ⇒ 93 条断言永不红，改完不知道坏没坏（结构性假绿灯） |
| **0-2** | **C6** —— `RoleAdminService.updatePermissions/updateDataScope/update` 拒绝改 `is_builtin=1`；`UserAdminService.updateRoles` 拒绝改"当前登录账号自己的角色" | 约 10 行 | 安全项。写法**仓库里已有**：照抄 `RoleAdminService.delete:205-207` 的 `isBuiltin` 判断 + `UserAdminService.delete:241` 的自我判断 |
| **0-3** | **#16①** —— `SysUserMapper.selectLeaderIdByDeptId` 加 `u.status=1 AND u.deleted=0`；`AssigneeResolver.resolve()` 出口统一过滤 candidateIds | 1 行 SQL + 约 5 行 | 一行 SQL 消掉「必然发生 + 无法人工解除」的卡死根因（C7 的另一半）。**注意这是全项目唯一不校验用户可用性的解析规则** |
| **0-4** | **#16②** —— `autoSkipUnassigned()` 返回"是否已办结"，`FlowRuntimeService.start():120-125` 据此决定是否发 STARTED | 约 10 行 | 消掉**幽灵单**（`document.status=2` 但无任何待办入口 = 静默失败，最难自查） |
| **0-5** | **新增（2026-09-23 实测发现）· 让"权限点"这个词变成事实** —— ① 新增 `scripts/check_perm_reachability.py`（扫 25 个 code 的引用数，0 引用=红灯）并挂进 `run_all_verify.sh`；② 给 `POST /api/documents` 补 `@RequirePerm("document:create")`、给 `POST /api/todos/approve` `/batch-approve` `/countersign` 补 `@RequirePerm("document:approve")` | 脚本约 30 行 + 4 行注解 | **不补这两处，后面所有"收权/分权"动作都是空转** —— 实测 25 个权限点里 **15 个零引用**，`document:create` 与 `document:approve` 都在其中（详见 §13.8）。补完后"D8 收回 admin 发单权"才第一次有效 |

**做完批次 0 立刻跑**：`bash scripts/run_all_verify.sh` + `bash oa-backend/scripts/e2e/run_all_e2e.sh`。
**这将是本项目第一次拿到"会红的回归网 + 冷启动链路已通"的真实结论。**

**验收判据（三条，缺一不算完成）**
1. `run_all_verify.sh` 的 13 套件**全部真的退出 0**（不再有"绿中有败"）；
2. 用**无部门的新用户**走 API 建单 → 返回**可读业务提示**而非 500（验证 0-3 生效）；
3. 让一个节点的候选人是**停用用户** → 该节点**不再静默永久卡死**（验证 0-3 + 0-4 叠加生效）。

> **2026-09-24 判定（逐条给证据，见下方 ④）**
> ① ✅ **15 个套件全部退出 0**（已含"从不被执行"那条），且已通过**负向验证**：注入必败断言 ⇒ 该套件变 ✗ + 整体 `exit 1`。
> ② ✅ **已修** —— 根因不是 NPE，而是"插入策略跳过 null 列 + `document.dept_id` NOT NULL 无默认值"，改为插入前显式抛出可读业务提示。
> ③ ✅ **实测达标** —— `leader_id=0` 哨兵与"已停用的负责人"都不再被当成承办人，正常落到 `fallbackRoleCode` 兜底角色。

---

### ✅ 批次 0 执行结果（2026-09-24 实施 + 实跑）

| # | 状态 | 落地内容 |
|---|---|---|
| **0-1** | ✅ | 两个脚本补 `import sys` + 末尾 `sys.exit(1 if FAIL else 0)`（其余 11 个套件本来就用 `raise SystemExit`）。**回归网从此会说真话**：原先恒绿的 2 个套件立即各暴出 1 条真实失败 |
| **0-2** | ✅ | `UserAdminService`：`update()` 拒改自己的启停状态、`updateRoles()`/`update()` 拒改自己的角色（新增 `assertNotSelfRoleChange`，**只拦"真的变了"**，改自己手机号仍可）。`RoleAdminService`：内置角色**改名/改绑部门岗位**拒绝；`updatePermissions` 拒**清空**、拒摘掉 `system:role`（全局自锁）；`updateDataScope` 拒把内置角色设为 `self`；顺带 `requireScopeType()` **收紧枚举校验**（原 `DataScopeType.of` 非法值静默降级 self） |
| **0-3** | ✅ | `selectLeaderIdByDeptId` 改 `JOIN sys_user ... u.status=1 AND u.deleted=0` + `d.leader_id > 0`（原条件放过哨兵 0，且从不校验可用性）；`AssigneeResolver` 出口新增 `selectAliveIds` 过滤 + 剔除告警；`byInitiatorLeader` 的 `requireNonNullElse` NPE 改为显式判空（**流程预览 500 的确切根因**） |
| **0-4** | ✅ | `autoSkipUnassigned` 改返回 `boolean`（`processEnded()` 以引擎为准）；`start()` 仅在未办结时发 STARTED |
| **0-4 补** | ✅ | ⚠ **实施中发现原方案只堵了一半** —— 幽灵单有**两个写入点**。`DocumentService.submit():166-170` 在 `start()` 返回后又无条件 `setStatus(STATUS_RUNNING)`，把 listener 刚写的"已通过"覆盖回"审批中"。已改为**以数据库现值判定**：`start()` 期间已办结则跟随终态、不回写状态与当前节点。**实测复现**：申请人=本部门负责人（自审移除 → 全节点自动跳过）时，修前 `document.status=2`（幽灵），修后 `=3`（已通过） |
| **0-5** | ⚠ 部分 | ✅ 新增 `scripts/check_perm_reachability.py`（棘轮：新出现零引用点即红灯、已有欠账走 ALLOWLIST）并挂进 `run_all_verify.sh`（14 个套件）；✅ `/todos/approve`、`/batch-approve`、`/countersign` 补 `@RequirePerm("document:approve")`；❌ **`POST /api/documents` 的 `document:create` 门控已撤回** —— 见下 |

**⚠ 0-5 的关键修正：`document:create` 不能贸然补门控**

实测现网授权分布：8 个角色里只有 `ADMIN` / `DEPT_HEAD` / `EMPLOYEE` 持有 `document:create`，
业务审批角色（`GM` / `FIN_DIRECTOR` / `ACCOUNTANT` / `CASHIER` / `INTERNAL_CTRL`）**都没有**，
而该接口此前**无任何门控** ⇒ 所有人本来都能发起单据。
此时补门控 = **静默收权**：出纳当场发不了单（`verify_escalation_api` / `verify_flow_interaction`
的夹具立刻因 403 崩掉，这就是它暴露出来的方式）。

同时它与 §3.3 授权矩阵（业务角色无此点）冲突 —— **"谁能发起单据"是产品决策，不是本批的事**。
⇒ 先不补，把它列入 `check_perm_reachability.py` 的 ALLOWLIST 并注明"待与 D8 一起定"。
**规则（新增，已写入技能）**：补门控前必须先查"现网所有能走到该接口的角色是否都持有该点"，
否则不是"关上一扇门"，而是"悄悄收走一批人的权限"。

**实跑结果：12 / 14 绿**（`bash scripts/run_all_verify.sh`，14 个套件）。剩 2 红，**均为既有问题、已定位根因**：

| 套件 | 根因 | 性质 |
|---|---|---|
| `verify_escalation_api` | 夹具申请人用 `zhaocs`，而他**恰是 `dept_id=4`（资金结算部）的负责人** ⇒ 首审批节点 `initiator_leader` 解析出他自己 ⇒ 自审移除 ⇒ 自动跳过（`flow_instance_node.status=4`）。夹具随后把该节点的 deadline 改到过去，但升级扫描只认 `status IN (0,1)` ⇒ `scanned=0` | 夹具前提在演示组织架构下**不成立**（与本次改动无关） |
| `verify_flow_interaction` | 同上 ⇒ 该单据**没有真实承办人**（`assignee_id` 为空）⇒ "原承办人登录"、"委托给 B" 全部连锁失败 | 同上 |

⇒ 这两个套件**从来就是红的**（本次跑前 9/14 那轮里它们同样红），本次只是**把根因查清了**（此前列在"未核实"）。
**修法（2026-09-24 已实施并跑绿）**：夹具改为**临时建一个 `dept_id=4` 的非负责人账号**当申请人
（走 `POST /api/users` 建、收尾按记下的 id 删；dept 4 是演示架构里**唯一**满足
"本部门上级部门负责人 ≠ 本部门负责人"的部门，而它在编人员只有负责人本人 zhaocs）——
这样"承办人 = 部门负责人(5 zhaocs) ≠ 申请人"、"升级目标 = 上级部门负责人(3 lifinance)"
两个前提同时成立。两个套件各补一条"首节点必须有**真实承办人**"的断言 ⇒ **前提一旦不成立立刻红**，
不再静默退化成"连锁失败"。
**结果**：`verify_escalation_api` **23/23 ✓**、`verify_flow_interaction` **31/31 ✓**。

---

### ✅ 批次 0 收尾（2026-09-24）：三件事一次跑完

#### ① 0-1 的运行时负向验证 —— 判据原文是"这一步不过，后面所有'验证通过'都不算数"

做法：往 `verify_admin_api.py` / `verify_attachment_api.py` 各注入一条**必然失败**的断言，
再跑 `bash scripts/run_all_verify.sh`，看它们能不能真的变红。

| 观察点 | 实测 |
|---|---|
| 该套件是否变 ✗ | ✅ `✗ verify_admin_api 退出码非 0` / `✗ verify_attachment_api 退出码非 0` |
| 是否打印失败清单 | ✅ 两处都打印了 `- NEGATIVE-PROBE 故意失败…` |
| 整体退出码 | ✅ `HARNESS_EXIT=1` |
| 同一次跑里其余套件 | ✅ 12 绿（含刚修好的两个夹具套件） |
| 演示库基线 | ✅ 跑前 = 跑后，12 个字段逐项一致 |

⇒ **回归网从此会说真话**：断言失败 = 红灯 = 非零退出码。探针已回滚（源码与 `__pycache__` 均无残留，已 grep 复核）。

#### ② 顺带接上一条"从不被执行"的套件

`verify_flow_assignee_admin.py`（18 条断言：验 `PUT /api/flows/configs/{id}/assignees` 的越权、
**覆盖语义**（漏传即删规则）、不新建版本、收尾原样还原）**一直没挂进 `run_all_verify.sh`** ——
单独跑是 18/18 通过，但**没有任何入口会跑它**。
⇒ 已加入 `SUITES`：**14 → 15 个套件**。不跑的验证等于没有，与 0-1 是同一件事的两半。

#### ③ ⚠ 新发现：演示库的「日常付款」已不指向交付流程（验证可信度问题）

| 事实 | 证据 |
|---|---|
| 现库 `document_type.id=1 (DAILY_PAYMENT)` 指向 **131** | `SELECT flow_config_id FROM document_type WHERE id=1` → `131` |
| 131 = 人工建的 **「测试」v3**，只有 2 个节点 | `flow_config_node` 里 131 仅 `发起人 → 直属部门负责人` |
| 该 v3 流程**已部署**，提交会真的走它 | `ACT_RE_PROCDEF` 中有 `DAILY_PAYMENT_V3` |
| **交付种子声明的是 1** | `seed_data.sql:361` `UPDATE document_type SET flow_config_id = 1 WHERE id = 1`（真实五节点：发起 → 直属部门负责人 → 会计(按部门) → 金额分支 → GM/出纳） |
| 历史实例全部跑 v1 | `flow_instance` 中 doc_type=1 的 23 条**全是** `flow_config_id=1` |

**影响**：所有以 `docTypeId=1` 建单的接口/E2E 用例，**当前跑的是那条两节点「测试」流程**。
中间三段（会计 / 金额分支 / GM）**在回归网里从未被走到**。
这是"绿灯亮着但覆盖不到"的一类问题 —— 比红更难发现，也正是本项目反复强调的假绿。
**它同时解释了"为什么很多用例只测到第一个节点就结束了"。**

**⚠ 2026-09-24 追加证据：它不是"覆盖窄"，而是已经在把验证打成红的**

`bash oa-backend/scripts/e2e/run_all_e2e.sh` 实测 **3/5 绿、2 红**，而两个红套件**全部**由这条漂移造成：

| 套件 | 结果 | 失败项指向的根因 |
|---|---|---|
| `frontend_admin_e2e` | 83/88 | 5 条全是"流程弹窗按**节点行**渲染 / 条件分支节点展开 / 分支目标列表"——实测拿到的是 `{"rows":2,"gwRows":0,"branchRows":0,"tags":["发起","审批"]}`：**2 行、0 个网关**。它期望的是五节点（含 `gw1` 条件分支）|
| `frontend_attachment_e2e` | 40/56 | 第 1 条就卡在"**单据推进到出纳付款**（办理节点，凭证必填）"—— 这条流程里**根本没有"出纳付款"节点**（V3 只有 发起→直属部门负责人）⇒ 后面 15 条凭证/必填断言全部连锁失败 |

⇒ **104 条 E2E 断言因为这条漂移而红。**

**⚠ 关键更正：这不是"污染"，也不能靠 SQL 改指向来修（已试、已撤回）**

| 事实 | 证据 |
|---|---|
| 两条「测试」版本是**人工在界面上发布的**，不是脚本残骸 | `flow_config` 里 id 130/131 的 `created_at` = **2026-09-23 10:20:41 / 10:22:52**（相隔 2 分钟，v2→v3） |
| **发布新版本本来就会改指向** | `FlowConfigAdminService.java:213` `configMapper.updateDocTypeFlowConfig(docTypeId, cfg.getId())` —— 这是「版本化闭环」的最后一步，**产品设计如此** |
| 所以 `flow_config_id=131` 是**合法当前态**，fc1(`status=2`) 是**已退役版本** | `flow_config` 里 fc1 status=2、fc131 status=1 |
| 佐证：漂移之前的 admin E2E 是全绿的 | `/tmp/e2e_admin.log`（**2026-09-23 09:49**）`管理端 UI 验证：88/88 通过` |

**试过并已撤回的做法（留证，免得后人再走一遍）**：`UPDATE document_type SET flow_config_id=1`。
- ✅ 它确实**证明了因果**：`frontend_attachment_e2e` 从 **40/56 → 56/56 全绿**（流程里有"出纳付款"节点了）。
- ❌ 但它是**错的**：运行时开始用**已退役（status=2）的版本**，而**界面仍在列"测试"版本**。
  更糟的是界面列出了同一单据类型的**全部版本**（3 张卡，最新版本在最前），却**没有任何"生效"标记**
  ⇒ 管理员照着卡片改流程会"改了没效果"。这是**用一种静默不一致换掉另一种**。
- ⇒ **已撤回**：`document_type.flow_config_id` 恢复为 `131`（环境回到我介入前的状态，本项净改动为零）。

**正确修法（必须走应用，且需要你拍板，不在本批做）**：在**流程管理**里把该单据类型的正式流程
**重新发布一版**（保存即生成新版本并自动指向，`updateDocTypeFlowConfig` 会做这件事），
或用界面上的退场动作 `DELETE /api/flows/configs/{id}`（前端 `retireFlow`）把两条「测试」版本退掉。
⇒ 之后 E2E 的节点前提重新成立，两个套件自然恢复。

**⚠ 顺带发现的产品问题（建议单独立项）**：流程管理页把同一单据类型的**所有版本**都渲染成卡片，
排序是**最新版本在前**（`orderByDesc(version)`），而运行时依据是 `document_type.flow_config_id`。
**"最新版本 ≠ 生效版本"时，界面上没有任何标记说明哪张卡才是生效的** —— 管理员极易改错版本。
建议：卡片打「生效」标记，或列表默认只列生效版本。

**⚠ 另一个值得记住的对比**：接口层 **15/15 全绿对这条漂移完全不敏感**（在 131 与 1 两种指向下各跑一次都是 15/15）。
只有 E2E 会暴露它 —— 因为只有 E2E 真的"往下走节点"。


#### ④ 验收判据 2 / 3 的实测结果（③ ✅ 达标；② 差在 500，本批已修）

用一次性诊断脚本（**不入库**，`/tmp/diag_deptless.py`）实测三个场景，每次都自建自清，
跑完 `document / sys_user / department / ACT_RU_TASK` 全部回基线。

| 场景 | 实测 | 结论 |
|---|---|---|
| **C1** 无部门用户 · 流程预览 | HTTP 200 `code=0` | ✅ NPE 已修（就是 B1 的根因） |
| **C1** 无部门用户 · **建单** | 修前 HTTP **500**「服务异常，请联系管理员」→ 修后**可读业务提示** | ✅ 本批已修（根因见下） |
| **C2** 部门 `leader_id=0`（"未设负责人"哨兵） | 首节点 `assignee_id = (空)`，task 存在（候选模式） | ✅ **不再把哨兵 0 当成承办人** |
| **C3** 部门负责人是**已停用**用户 | 首节点 `assignee_id = (空)`，task 存在（候选模式） | ✅ **不再派给已停用的人** |

C2/C3 的关键变化是**兜底路径第一次真的被走到**：修复前 SQL 返回 `0`（哨兵）或"已停用者"，
结果列表**非空** ⇒ `AssigneeTaskListener` 直接把 `assignee` 设成这个"不存在的人 / 废人"，
而且**永远走不到 `fallbackRoleCode: DEPT_HEAD`**。这种任务不出现在任何人的"我的待办"里
（待办按 `assignee_id` 过滤），也没有任何界面入口能把它拿出来 ⇒ 只能人工改库 ——
正是"必然发生 + 无法人工解除"的那一类。现在列表为空 ⇒ 正常落到兜底角色。

**判据 2 的 500：确切根因与修法（本批已修，零 DDL）**

`oa-backend/logs/haixiajin-oa-error.log` 里的原始异常：

```
org.springframework.dao.DataIntegrityViolationException:
### Error updating database. Cause: java.sql.SQLException: Field 'dept_id' doesn't have a default value
### SQL: INSERT INTO document ( doc_no, ..., applicant_name, amount, ... )    ← 这一串里没有 dept_id
    at com.hxj.oa.document.service.DocumentService.createDraft(DocumentService.java:98)
```

⇒ **不是 NPE**，而是「插入策略 × 表结构」的边界：`doc.setDeptId(user.getDeptId())` 传 null，
MyBatis-Plus 默认插入策略会**跳过 null 字段**，于是这条 INSERT 压根不带 `dept_id` 列；
而 `document.dept_id` 是 **NOT NULL 且刻意不设默认值**（它是行级数据权限 `DataScopeHelper`
与"部门负责人"解析的锚点，塞个魔法默认值只会把错误藏起来）⇒ 数据库拒绝 ⇒ 被
`GlobalExceptionHandler` 兜成「服务异常，请联系管理员」。

**修法**（`DocumentService.createDraft`，**插入之前**显式拦住）：

```java
if (user.getDeptId() == null) {
    throw BizException.of("当前账号未归属任何部门，无法发起单据。"
            + "请联系管理员在【组织管理】中为你指定部门后再试");
}
```

这就是 P5「失败必须显式」的落点：以前这条路径 100% 复现 500，而日志里只有一句"服务异常"，
**用户、运维、开发三方都不知道问题只是"这个人没挂部门"**。

⚠ 改的是 `DocumentService.createDraft`（提单主路径）⇒ 按纪律**必须整网重跑**，
见下方重新验收结果。

### 批次 1 · 需要拍板（不拍板会返工）

**D2 · D3 必须在批次 2 之前定**（它们决定 B 组的形态）；D6 / D7 可与批次 2 并行。

### 批次 2 · B 组冷启动可用性（**一条链，不可拆**）

B1 流程预览 500 · B2 建单 500 · B3（500 → 明确提示）· B5 自审静默移除（依赖 **D3**）。
> B4 幽灵单已并入 0-4，此处不再重复。

### 批次 3 及以后

沿用 §九 顺序：**N3 首登引导 → N1/N2/N4/N5 → P 组 → C 组其余 → E 组表单 → I 组工具链**。
C7 的干预工具（转办/终止/催办）**不阻塞交付**，按客户运维需要排期。

### 十二补、批次 0 逐项行级改法（2026-09-23 读码确认，未动手）

#### 0-1 · V1/V2：让回归网会说真话

- **实测**：`verify_admin_api.py` 的 import 段（8-12 行）**没有 `sys`**；`verify_attachment_api.py`（17-24 行）**也没有**。两个脚本结尾都是 `print('='*72)` 收场，**从不调用 `sys.exit`**。
- **判定链**：`run_all_verify.sh:95` 是 `if (cd "$ROOT" && "$PY" "scripts/$s.py") > "$LOG" 2>&1; then` —— **纯粹靠退出码**。
- ⇒ 两个脚本**永远退出 0**，永远显示 `✓`。之前实测看到的「`✓ verify_admin_api 结果：通过 45 项，失败 1 项`」就是这么来的：**红的东西被打了绿勾**。
- **改法（各 2 处）**：

```python
# 文件头 import 段加一行
import sys

# 文件末尾 print('='*72) 之后加一行
sys.exit(1 if FAIL else 0)
```

- **验收**：故意把某条断言改成必然失败，跑 `run_all_verify.sh` 必须看到该套件变 `✗`、整体 exit 1。**这一步不过，后面所有改动的"验证通过"都不算数。**

#### 0-2 · C6：管理员自锁（含一处认知修正）

> **先修正我之前的说法**：本系统**只有一层管理员**，所以"防外部提权"的实际价值有限——能调 `system:role` 的人本来就是超管级别。**真正现实的风险是管理员把自己锁在门外**，而 `PUT /api/users/{id}` 恰好提供了三条自锁路径。

- **实测**（`UserAdminService.update:186-218`）：`upd.setStatus(req.getStatus() == null ? exist.getStatus() : req.getStatus())` —— **没有"不能改自己"的判断**。对比同文件 `delete:241` **明确有** `不能删除当前登录账号`。⇒ 同一类风险，`delete` 防了，`update` 没防。
- **三条自锁路径**：
  1. **停用自己**：`PUT /api/users/1 {status:0}` ⇒ `AuthService.login` 命中 `status != 1` 拒绝（"该账号已停用"）⇒ 系统唯一管理员进不去 ⇒ **只能改库**。
  2. **清空自己的角色**：`req.getRoleCodes != null` ⇒ `rebindRoles` ⇒ 自己丢掉 ADMIN ⇒ 重登后 0 权限点 ⇒ 全接口 403。
  3. **改自己的角色** ⇒ 反方向也能用（自我提权）。
- **改法**：
  - `UserAdminService.update` / `updateRoles`：**不要整条禁掉**（改自己手机号是合理需求），只对敏感字段收紧——操作者 == 目标时，`status` 与角色相关字段必须与原值一致，否则拒绝：
    ```java
    if (Objects.equals(id, UserContext.currentUserId())) {
        throw BizException.of("不能修改当前登录账号自己的角色或状态，请用其他管理员账号操作");
    }
    ```
    （加在 `updateRoles` 开头即可整条拒；`update` 里需改为"仅敏感字段"判断）
  - `RoleAdminService.updatePermissions / updateDataScope / update`：拒绝改 `is_builtin=1` 的角色（照抄 `delete:205-207`），**并额外保证内置角色的权限点数不得降到 0**。
  - **为什么需要第二层**：即使加了"不能改自己"，若系统有**两个**管理员账号，A 仍可把 ADMIN 角色权限清空，B 一起废 —— 这是**全局自锁**，比个人自锁更严重。

#### 0-3 · `#16①` 审批人可用性（同时修掉 B1 根因）

- **现状 SQL**（`SysUserMapper.java:37-40`）：

```java
@Select("""
        SELECT d.leader_id FROM department d
        WHERE d.id = #{deptId} AND d.deleted = 0 AND d.leader_id IS NOT NULL
        """)
Long selectLeaderIdByDeptId(@Param("deptId") Long deptId);
```

- **两处问题**：① 只查 `department`，**从不 join `sys_user`** ⇒ leader 停用/删除后照样返回；② `d.leader_id IS NOT NULL` 放过了 **0**（"未设负责人"哨兵值）⇒ 返回 0 ⇒ `List.of(0L)` ⇒ `setAssignee("0")`。
- **对照**：同文件的 `selectUserIdsByRole` / `selectUserIdsByDeptAndRole` **都带 `u.status=1 AND u.deleted=0`** —— **只有这一条漏了**。
- **改法**：

```sql
SELECT u.id FROM department d
JOIN sys_user u ON u.id = d.leader_id AND u.deleted = 0 AND u.status = 1
WHERE d.id = #{deptId} AND d.deleted = 0 AND d.leader_id > 0
```

（JOIN 一次干掉"停用/删除/0 哨兵"三种；`> 0` 冗余但可读）

- **出口防御**（`AssigneeResolver.java:101` 之前，紧接自审移除那段）—— `userMapper` **已注入**，无需新依赖：

```java
// 防御纵深：解析出的人必须可用。空列表有 auto_skip 兜底，非空废人会被静默永久卡死
if (!ids.isEmpty()) {
    ids.retainAll(userMapper.selectAliveIds(ids));
}
```

- 需在 `SysUserMapper` 新增：

```java
@Select({"<script>",
  "SELECT id FROM sys_user WHERE deleted = 0 AND status = 1 AND id IN",
  "<foreach collection='ids' item='i' open='(' separator=',' close=')'>#{i}</foreach>",
  "</script>"})
List<Long> selectAliveIds(@Param("ids") Collection<Long> ids);
```

- **顺带修一行（这是 B1「流程预览 500」的确切根因）**，同文件 `byInitiatorLeader:121`：

```java
// 原：两参皆 null 时 Objects.requireNonNullElse 直接抛 NPE
Long deptId = Objects.requireNonNullElse(JsonColumn.toLong(param, "deptId"), ctx.getApplicantDeptId());
// 改：
Long deptId = JsonColumn.toLong(param, "deptId");
if (deptId == null) deptId = ctx.getApplicantDeptId();
```

#### 0-4 · `#16②` 幽灵单

- `autoSkipUnassigned`（`FlowRuntimeService.java:282`）是 **`void`**。它内部在跳过最后一个节点时**已经做对了**：`inst.setStatus(2)` + `endedAt` + `publish(FINISHED)`（321-327 行）。
- **问题只在 `start()`**：第 120 行调完 autoSkip，**第 122 行无条件** `publish(STARTED)` ⇒ 把单据状态从"已通过"**覆盖回"审批中"**。
- **改法**：

```java
// autoSkipUnassigned 改为返回 boolean（是否已办结）
boolean finished = autoSkipUnassigned(pi.getId(), inst);
if (!finished) {
    eventPublisher.publishEvent(new FlowLifecycleEvent(this, /* ... STARTED ... */));
}
```

（方法内 `return;` → `return true/false;`）

- ⚠ **`approve()` 里的调用点（第 183 行）不用改**：它先发 `APPROVED`（181 行）再调 autoSkip，**顺序本来就是对的**。第 172-173 行那条注释「先发 APPROVED 再发 FINISHED，顺序不能反」说明作者清楚这个规则，**只是漏了 `start()` 这条路**。

#### 0-5 · 让"权限点"变成事实（2026-09-23 实测新增）

**背景**：§13.8 的实测结论 —— 25 个权限点里 **15 个零引用**。其中 `document:create` 与 `document:approve`
这两条最要紧的"业务资格点"，**在前后端都没有任何地方在读**。这意味着：
任何"把权限点从角色上摘掉"的操作，**唯一效果是前端少个按钮（而且前端也没读它）**，后端一律照放。

**改动 A · 补门控（4 处注解，零风险）**

```java
// oa-document/.../DocumentController.java:52  —— 现状：只有 @Audit
@PostMapping
@RequirePerm("document:create")          // ← 新增
@Audit(module = "document", action = "createDraft")
public R<Document> create(@Valid @RequestBody DocumentCreateRequest req) {

// oa-document/.../TodoController.java:47 / :65 / :73 —— 三处均无 @RequirePerm
@PostMapping("/approve")
@RequirePerm("document:approve")         // ← 新增
@Audit(module = "flow", action = "approve")

@PostMapping("/batch-approve")
@RequirePerm("document:approve")         // ← 新增（批量审批不放宽授权，见功能不变量）

@PostMapping("/countersign")
@RequirePerm("document:approve")         // ← 新增（加签同属审批决定动作）
```

⚠ **顺序约束**：这 4 处注解**必须与种子/权限点同步上线**。若先加注解而角色还没绑点，
**所有人**建单/审批立刻 403 —— 这是"看起来功能全崩"级别的事故。做法：
先确认 `ADMIN`（或 `SYS_ADMIN`）已持 `document:create` / `document:approve`，
**再**加注解。

> **⚠ 2026-09-24 更正（我上一轮写错了）**：上一轮此处写「GM / FIN_DIRECTOR / ACCOUNTANT / CASHIER / INTERNAL_CTRL 只有 `document:approve:*` 变体，没有基础点 ⇒ 5 个内置角色会立刻批不了单」。
> **实测证伪**：`seed_data.sql:133-165` 里 **8 个角色全部显式授了基础点 `document:approve`**；线上库直查也一致：
> ```
> ADMIN,GM,FIN_DIRECTOR,ACCOUNTANT,CASHIER,INTERNAL_CTRL,DEPT_HEAD,EMPLOYEE   ← document:approve 的持有者
> ADMIN,DEPT_HEAD,EMPLOYEE                                                    ← document:create 的持有者
> ```
> ⇒ 给 `/todos/approve` 等三处补 `@RequirePerm("document:approve")` **不会收走任何人的审批权**（这正是它被实测跑绿的原因）。
> **真正会收权的是 `document:create`**（只有 3 个角色有），所以那一处已撤回 —— 两处注解的命运不同，判据是同一句话：
> **补门控之前先查"现网所有能走到该接口的角色是否都持有该点"。**


**改动 B · 权限点目录瘦身（纯数据 + 前端，与 §3.2 表一致）**

删 14 个：5 个菜单点 + `document:view:self/:dept/:company` + `document:approve:leader/:accountant/:cashier`
+ `document:supplement` + `document:countersign`（`document:approve:seal` **保留**，它真的在用）。

⚠ **删除前必须确认前端没有引用**：实测 `can()` 只用了 10 个 code，与保留集合一致 ⇒ **安全**。
但 `document:approve:leader` 等三兄弟**可能被 `seed_data.sql` 的角色绑定引用**（135-165 行确实绑了）
⇒ 删点时要连带清理 `role_permission` 中的对应行，否则**批准不了也删不掉**（外键/悬空引用）。

**改动 C · 新增 `scripts/check_perm_reachability.py`（防回归）**

```python
# 对每个 sys_permission.code，统计它在
#   ① oa-backend/**/*.java 的 @RequirePerm(...) 中出现的次数
#   ② 海峡金OA审批系统-联调版.html 的 can('...') 中出现的次数
# 两者皆为 0 ⇒ 打印红灯并 sys.exit(1)
# 白名单：document:approve:seal 等已知"仅后端门控"的点允许前端为 0
```

挂进 `scripts/run_all_verify.sh`。**没有这条闸门，"装饰品权限点"会再长回来** ——
本次能发现 15 个是运气好（逐个 grep），不是流程保证。

---

## 十三、管理员模型定稿（2026-09-23，含一次重要更正）

> 📄 **本节已扩展为完整设计规格**：`管理员角色与权限设计说明-2026-09-23.md`（v1.0 定稿评审稿）。
> 该文档含：8 项现状取证（每条带 file:line）、5 条设计原则、三层角色模型、权限点三类处置表、
> 4 处硬编码收口方案、自锁护栏 R1~R3、S1~S6 落地顺序 + 7 条验收断言、6 个未决决策点。
> **本节保留为工单内的摘要**；两者冲突时以设计说明为准。

### 13.0 ⚠ 先更正上一轮的一个错误结论

上一轮我说「代他人审批 **无**」——**错的**。当时只看了 `TodoController.myTodo`（列表确实只查本人），漏看了授权侧。实测：

```java
// FlowRuntimeService.java:391-395
private void assertAssignee(Task task, LoginUser user) {
    String uid = String.valueOf(user.getUserId());
    if (uid.equals(task.getAssignee()) || user.hasRole("ADMIN")) {   // ← 硬编码旁路
        return;
    }
```

⇒ **持 `ADMIN` 角色编码者可审批系统中任意一张单据的任意节点**，绕过全部指派规则与候选人判定。
配合 `DocumentService:481` 让 ADMIN 可看任意单据详情，**从界面就能走通"打开任意单据 → 拿到 taskId → 直接批准"，不需要碰数据库**。

**准确结论**：本系统不是"缺干预手段"，而是**方向反了** —— 它拥有最危险的那一种（代替他人做审批决定），却缺最安全的那些（转办 / 终止 / 催办）。**C7 按此重述。**

### 13.1 现状：两套授权机制并存（实测）

| 层 | 机制 | 判定依据 | 可配置 | 可审计 | 结论 |
|---|---|---|---|---|---|
| 接口门禁 | `PermInterceptor` + `@RequirePerm`（`oa-boot/.../security/PermInterceptor.java:23-81`） | JWT `permCodes` ← `role_permission × sys_permission` | ✅ | ✅ | **正确** |
| 业务内部特权 | `user.hasRole("ADMIN")` | **硬编码角色编码** | ❌ | ❌ | **必须收口** |

**主代码中 `hasRole("ADMIN")` 共 4 处**（全项目清点，测试代码不计）：

| # | 位置 | 作用 |
|---|---|---|
| 1 | `FlowRuntimeService.java:393` | **审批授权旁路** —— 可批任意任务（最严重） |
| 2 | `DocumentService.java:481` | `assertVisible` 跳过数据范围，可看任意单据 |
| 3 | `AttachmentService.java:146` | 附件访问旁路 |
| 4 | `AttachmentService.java:169` | 附件删除旁路 |

> **`hasPerm(...)` 在主代码中 0 处调用**（仅 `LoginUser.java:51` 的定义）。⇒ 业务层的细粒度特权判定**完全没走权限点**，全部由角色编码硬编码替代。

### 13.2 设计原则（三条，按优先级）

1. **一切特权必须是数据，不是代码。** 代码里不允许出现任何角色编码字符串判断 —— `"ADMIN"` 也不行。
2. **管理员只碰「定义」，不碰「实例」的结论。** 改系统定义 → 管理员；决定某张单批不批 → 业务人员。
3. **管理员不能管理管理员，也不能锁死自己。** 层级隔离 + 自锁保护。

### 13.3 目标模型：三层管理员 + 一条隔离线

| 层 | 角色编码 | 权限点 | 数据范围 | 必需性 |
|---|---|---|---|---|
| 系统管理员 | `SYS_ADMIN` | `admin:menu` + 8×`system:*` + 5×`*:menu` | `company` | ✅ 必需 |
| 流程管理员 | `FLOW_ADMIN` | `admin:menu` + `system:flow`/`form`/`docType` | `company` | 建议（把流程配置从 IT 手里分出去） |
| 部门管理员 | `DEPT_ADMIN` | `system:user`（+ `system:dept` 只读） | `dept` | 按客户规模 |
| ——— 隔离线 ——— | | | | |
| 业务角色 | `DEPT_HEAD` / `ACCOUNTANT` / `CASHIER` / `FIN_DIRECTOR` / `INTERNAL_CTRL` / `EMPLOYEE` | `document:*` | 按岗位 | ✅（种子已有，8 个内置角色） |

**权限点分配定稿**（✓ 持有 / ✗ 绝不 / △ 建议只读）

| 权限点 | SYS_ADMIN | FLOW_ADMIN | DEPT_ADMIN | 业务角色 |
|---|---|---|---|---|
| `admin:menu` | ✓ | ✓ | ✓ | ✗ |
| `system:role` | ✓ | ✗ | ✗ | ✗ |
| `system:user` | ✓ | ✗ | ✓（仅本部门） | ✗ |
| `system:dept` | ✓ | ✗ | △ 只读 | ✗ |
| `system:flow` / `system:form` / `system:docType` | ✓ | ✓ | ✗ | ✗ |
| `system:dict` / `system:audit` | ✓ | ✗ | ✗ | ✗ |
| `document:create` | ✓（排障必需） | ✗ | ✗ | ✓ |
| `document:view:*` / `document:export` | ✓ | ✓（排障） | ✗ | ✓ |
| **`document:approve` / `document:approve:*`** | **✗** | **✗** | **✗** | ✓ |

**三个「绝不」**
1. 管理员角色**绝不**持有 `document:approve*` → 修 **C4**
2. 任何管理员**绝不**能修改自己的状态或角色 → 修 **C6**
3. 任何管理员**绝不**能改 `is_builtin=1` 角色的权限点，且内置角色权限点数**不得降到 0** → 防**全局自锁**

### 13.4 四个硬编码旁路的收口方案（本次设计的核心动作）

| 位置 | 改成 | 为什么 |
|---|---|---|
| `FlowRuntimeService:393` | **删掉 `|| user.hasRole("ADMIN")`**，改判显式权限点 `document:approve:any`；通过时**强制审计留痕**（detail 写「代 XXX 办理」） | 这是**授权旁路**。管理员代他人审批这件事可以存在，但必须是**显式、可配置、可撤销、可审计**的一项能力，不能藏在 `assertAssignee` 里的一个 `\|\|` |
| `DocumentService:481` | **直接删** | **冗余**：ADMIN 的角色数据范围本就是 `company`，`inDataScope` 已涵盖。留着的副作用是——有人把 ADMIN 数据范围改成 `dept` 后，这个旁路会**静默放权**，制造"改了范围却不生效"的假象 |
| `AttachmentService:146` | 改判 `document:view:company` | 附件可见性应与**单据可见性同源** |
| `AttachmentService:169` | 改判 `attachment:delete:any`（或 `system:audit`） | 删除是写操作，必须与只读分开 |

### 13.5 落地顺序（前两步零代码）

| 步 | 动作 | 代码量 |
|---|---|---|
| 1 | 建 `SYS_ADMIN` / `FLOW_ADMIN` 两个角色，按 13.3 表分权限点 | **0**（走应用配置） |
| 2 | 把 `ADMIN` 角色改成"系统管理员"语义：清掉 12 个业务权限点（修 C4） | **0**（改配置） |
| 3 | 收口 4 处 `hasRole("ADMIN")`（13.4） | ~20 行 |
| 4 | C6 自锁保护三处 | ~15 行 |
| 5 | 权限点界面按「系统层 / 业务层」分组 | 前端分组 |
| 6 | `DEPT_ADMIN` 真分级（需先补 C3 管理范围） | 中等，按客户规模 |

> ### ✅ 第 3 步已完成（2026-09-24，提交 `503e0d9`）
>
> **做法比 13.4 的设计更保守，此处记下偏差原因**：13.4 写的是「删掉 `|| user.hasRole("ADMIN")`，改判显式权限点 `document:approve:any` + 强制审计留痕」—— 即**保留**"管理员可代审"这一能力，只是把它变成显式可审计。本次**没有新增该权限点，直接删除旁路**。
>
> 理由：① P3「审批决定权永不给管理员」是设计原则，不是权宜；② 收权比新增授权路径安全，且**可逆**——真有客户要"代审"，把它做成显式权限点 + 审计是**新增**，而恢复旁路是**倒退**；③ 新增权限点会牵动 `sys_permission` 25→26、`role_permission`、棘轮 ALLOWLIST，属另一件事（D5 `FLOW_ADMIN` 转办/终止/催办才是它的归处）。
> ⚠ **若客户后续要求"管理员代审"，正确落点是新增显式权限点 + 强制审计，不要恢复这行旁路。**
>
> 实质影响评估（收口前已核）：admin 的 `role_data_scope` = `company`，演示库 46 张单据全部 `company_id=1` ⇒ **读路径零变化**，只收掉"写"（代签 / 删他人附件）。
>
> 新增 `scripts/verify_admin_no_bypass.py`（24 断言，已挂进 `run_all_verify.sh`）；**该用例做过负向验证**：旁路装回 ⇒ 5 条变红。

### 13.6 ⚠ 一个反直觉的强制顺序（读码后新增，必须遵守）

> **不要"废弃 `ADMIN`、新建 `SYS_ADMIN`"来改角色编码。**

因为那 4 处旁路是按**角色编码字符串**判定的 —— 把 `ADMIN` 改成别的编码，会**静默解除**这 4 处旁路，其中包含**审批授权旁路**。行为变化不可预期，而且没有任何报错。

**必须先做第 3 步（收口硬编码），才允许改角色编码**（如果那时还想改）。**顺序反了 = 无人察觉的权限收缩或放开。**

### 13.7 主流实现对照（2026-09-23 查证）—— "管理员该不该有部门"的行业答案

主流里**两种模型都存在**，分界线很清楚：

| 产品 | 管理员形态 | 是否属组织架构 | 模型 |
|---|---|---|---|
| 泛微 e-cology | `sysadmin` **独立后台账号**（默认密码 `1`） | **不在** | 一 · 后台账号独立于组织 |
| 致远 A8 | `system` / `group-admin` / `admin1` / **`audit-admin`** 四个独立账号 | **不在** | 一，且**按职责分离**（系统/集团/单位/审计四员） |
| 泛微 eteams（云版） | **系统管理员 + 分级管理员**，管理员从通讯录成员里配 | **在** | 二 · 管理员就是组织成员 |
| 钉钉 / 企业微信 | 主管理员 / 子管理员（管理组） | **在** | 二，且「管理范围 × 权限」正交 |

**判据（这是最有价值的一条）**：
- **后台运维型**账号（能管系统参数、数据库、服务）→ 独立于组织，**不需要部门**
- **组织管理型**账号（管部门、管人、管流程）→ 是组织成员，**有部门**

**本系统属于哪一类？** 看它的 8 个 `system:*`：`user / role / dept / flow / form / dict / audit / docType` ——
**全部是"组织管理"**，**没有任何"服务器/数据库/系统参数"权限**（那些在配置文件里，不在界面）。
⇒ **本系统的 admin 属于"组织管理型"，按主流应当有部门。**

**更根本的一条**：本系统**只有 `sys_user` 一种用户实体，没有独立的"后台账号"表** ⇒ 它**没有实现模型一的能力**，
天然只能走模型二（管理员 = 组织成员）。
⇒ 这比"因为保留了发单权"更本质：**即使拿掉发单权，admin 在模型二下仍然是组织成员，仍应有部门。**
（这也说明 §八补 D8 的"出口 B 不需要部门"只在**避免技术报错**这一层成立，从模型看 B 是**折中而非正解**。）

**结论（修正 D8 的权重）**：**出口 A 是主线** —— admin 挂一个组织内的根部门（公司本部 / 信息中心 / 总经办），
这正是泛微云版与钉钉的实际做法（管理员就是某个部门的员工）。**出口 B 只在"客户不接受 admin 有部门"时作为折中**，
且采用 B 就必须同时拿掉发单权（否则是自相矛盾态）。

#### 13.7补 ⚠ 上面这段结论的**前提被推翻了一半**（2026-09-23 查到表结构后修正）

上一段说"这正是泛微云版与钉钉的实际做法"——**这句话对，但它没说清另一半**：
**私有化阵营（泛微 e-cology、致远 A8）的管理员恰恰是没有部门的**，而**本项目就是私有化交付**。

**硬证据（查表结构，非厂商宣传）**：

| 系统 | 交付形态 | 管理员的表 | 人员主表 | 管理员在组织架构里吗 |
|---|---|---|---|---|
| 泛微 e-cology（E9） | 私有化 | **`HrmResourceManager`** | `HrmResource` | **❌ 不在** |
| 致远 A8 | 私有化 | `ORG_PRINCIPAL` 的预置账号 | `org_member` | **❌ 不在** |
| 泛微 eteams | 云 | 通讯录成员的**管理角色成员** | 同左（一套） | ✅ 在 |
| 钉钉 | 云 | 企业通讯录成员 + 管理组 | 同左（一套） | ✅ 在 |

- 泛微官方二次开发资料把 `HrmResourceManager` 定义为"**系统级管理员元数据表**，承载超级管理员、安全管理员、流程管理员等角色权限的底层映射"；
  密码重置语句是 `UPDATE HrmResourceManager SET password=... WHERE loginid='sysadmin'`。
  而组织模型是"组织-单位-部门-岗位-人员"五级，由 `HrmCompany`/`HrmSubCompany`/`HrmDepartment`/`hrmJobTitles`/`HrmResource` 承载
  ⇒ **管理员表不在那五级里**。
- 致远 A8：`system`（系统管理员）/`group-admin`（集团管理员）/`admin1`（单位管理员）/`audit-admin`（审计管理员）
  四个**预置账号**存于 `ORG_PRINCIPAL`，人员档案表是 `org_member` ⇒ 管理员与"四员分立"都在人员档案之外。
- 云阵营两条是官方文档原文：eteams「权限管理中心」写管理员在"角色设置"里配**角色成员**，
  且"系统管理员默认拥有所有模块的功能管理权限，**不可取消**"、"下级角色的功能权限只能在上级角色的权限范围内勾选"；
  钉钉子管理员路径是"成员 → 管理范围 → 分配权限"，成员一步写明"**从企业通讯录选择**"。

**所以分界线不是"哪个产品"，是"管理员是账号类型还是组织成员角色"。** 修正如上，导向一个新的表述：

> **本项目是私有化交付，却没有第二张账号表。**
> ⇒ 它**复制了模型一的"不给部门"，却复制不了模型一的"不进组织架构"** ——
> 因为 admin 就落在 `sys_user`（那张人员主表）里，必然被组织成员的规则约束（要部门、会被当审批人候选、单据必须有归属部门）。
> ⇒ 所以 **admin 挂根部门是"补偿性设计"，不是"模仿主流"**。这个定性比原来准确，也让长期路线变清楚：
> **要真正解决这个问题，得补模型一（独立运维账号表），而那时发单权可以光明正大地收回。**
> ⇒ D8 的本质因此改述为：不是"要不要给管理员发单权"，而是"**要不要承认它现在其实是组织成员**"（答案：现阶段必须承认）。

**D8 改成分期结论**（原表述"出口 A 是主线"过于绝对）：

| 阶段 | 选择 | 判据 |
|---|---|---|
| **交付前（现在）** | **A · 保留发单权 + 挂根部门** | 冷启动链**仅剩 B5 未修**（见下方 2026-09-24 复核），且它**只在真提交路径上暴露**，`/configs/{id}/preview` **恰好抓不到** ⇒ 此时收回 = 砍掉管理员唯一排障手段 |
| **交付后 / 补了模型一** | **B · 收回发单权** | 发单权的全部价值就是"临时排障"；冒烟验证完毕或有了独立账号表，它就没有存在理由 |

（设计说明 §3.3 / §3.8 已按此重写。）

> ### ⚠ 2026-09-24 读码复核：本文多处「B1~B5 未修」已过时，实际只剩 B5
> 逐条实读当前代码（不是凭文档）：
>
> | 项 | 判据 | 结论 |
> |---|---|---|
> | B1 预览 NPE | `AssigneeResolver.java` 里 `requireNonNullElse` **只剩一行注释**在解释"不能这么写" | ✅ 已修 |
> | B3 停用用户卡死 | `SysUserMapper` 已加「过滤出仍然可用的用户 ID」出口方法（注释写明"放在出口而不是逐个规则里"） | ✅ 已修 |
> | B4 幽灵单 | 已并入批次 0-4，且补了第二个写入点（`DocumentService.submit` 以数据库现值判定） | ✅ 已修 |
> | B2 无部门建单 500 | 走的是「admin 挂根部门」这一配置侧补偿，不是代码兜底 | ⚠ 靠配置，非空库上线要记得 |
> | **B5 自审只移除** | 2026-09-24 已落地**默认值 + 强制留痕**（用户拍板对齐钉钉/泛微）：`AssigneeTaskListener` 写任务变量 `__selfSkip_<nodeKey>`，`FlowRuntimeService#autoSkipUnassigned` 读回后写 `action=self_skip` + 意见「申请人即该节点审批人，系统自动通过（自审）」（非自审仍写「无匹配审批人，系统自动通过」）。新用例 `scripts/verify_self_approval_trace.py` 14 断言（含对照组）已挂进 `run_all_verify.sh`（17 套件） | ✅ 留痕已修 |
| **B5 剩缺口** | **策略仍是硬编码**（`AssigneeResolver:96-99` 无条件剔除申请人）。做成可配置需 DDL（`flow_config_node` 加列）+ `findLeaderAbove` 从 `EscalationService:254` 下沉为共用能力 | ⚠ **未做**（待排期） |

> **自审策略的行业取证（2026-09-24，用于支撑默认值）**
> - **钉钉**：管理后台 → 高级设置 → **流程高级设置 → 自动去重**，三种模式，其中一种是「**审批人和发起人同一人 ⇒ 自动通过**」。失效条件：表单可编辑 / 允许代他人提交 / 加签 / 转交 ⇒ 自动去重不生效。
> - **泛微**（eteams / E10 / e-office）：叫「**自动处理**」，分前置（相邻/后续节点，后者优先级更高）与后置；「**自动处理时在签字意见留痕**」是**独立开关**；「仅当操作者为本人一人时自动处理」；节点可改表单则关闭自动处理。
> - **两家共同三条**：自审 = **自动通过**（不是升上级）、**可配置开关**、**留痕独立**。
> - **反证**：等保三级要求"权限申请和审批应由不同人完成"；财务内控要求经办/审核/批准三分离 ⇒ 因此「允许自审」绝不能做成默认。

> ⇒ **默认值已定 = 自动通过 + 强制留痕**（已实现）。剩下的是「**可配置化**」，属产品可选项而非缺陷：**当前硬编码值本身就是安全的那一个**（自动通过 ≠ 允许自审）。

**三条可以直接抄过来的设计（本系统缺）**：
1. **顶级系统管理员权限不可取消**（泛微 eteams 明确写"系统管理员默认拥有所有模块的功能管理权限，**不可取消**"）
   → 对应本系统 C6 的"内置角色权限点不得降到 0"，是行业的通用护栏。
2. **下级权限只能在上级角色范围内勾选**（泛微 eteams）→ 对应本系统缺的**层级隔离**，即 D7 第二段。
3. **审计管理员独立成账号**（致远 `audit-admin`）→ 本系统 `system:audit` 现在挂在 ADMIN 身上，
   等于"系统管理员兼审计员"；等保 2.0 三员分立（系统管理员/安全保密管理员/安全审计员）里这是明确要分开的。
   **这条不需要动代码，拆角色即可**（与 C4 同批做）。

### 13.8 ⚠ 又一处需要更正：25 个权限点里 **15 个零引用**（2026-09-23 实测）

前面几节反复说"ADMIN 持 12 个纯业务动作，违反 SoD"。**方向对，但严重程度说反了。**
逐点统计"后端 `@RequirePerm` 引用次数 × 前端 `can()` 引用次数"后，实测结果是：

| 分组 | 有效（被引用） | 空转（0 引用） |
|---|---|---|
| 菜单 5 | — | `document:menu` `todo:menu` `ledger:menu` `dashboard:menu` `admin:menu` **全部 0** |
| 业务 12 | `document:export`(1/1)、`document:approve:seal`(4/1) | `document:create` `document:view:self` `:dept` `:company` `document:approve` `approve:leader` `:accountant` `:cashier` `document:supplement` `document:countersign` **全部 0** |
| 系统 8 | 8 个全部有效（`system:*`，后端 37 处 + 前端 28 处） | — |

**有效 10 / 空转 15（60%）。** 三条必须记住的推论：

1. **`document:create` 是装饰品** —— `POST /api/documents`（`DocumentController.java:52`）**只有 `@Audit`、没有 `@RequirePerm`**
   ⇒ 任何登录用户都能建单。所以"D8 把 `document:create` 从 admin 收回"**在功能上什么都不会发生**。
   **要收回，必须先在 `POST /api/documents` 上补门控**（工单新增动作项，见 §十二补 0-5）。
2. **`document:approve` 也是装饰品，但 `document:approve:seal` 不是** ⇒ 审批授权真实判定只在
   `FlowRuntimeService.assertAssignee()`（走 assignee/候选人/委托），用印反而走权限点。
   `/todos/approve`、`/batch-approve`、`/countersign` 三个端点**均无 `@RequirePerm`**（`TodoController.java:47/65/73`）。
3. **`document:view:*` 三兄弟是重复设计** ⇒ 行级可见性已由 `DataScopeHelper.buildClause()` 单一实现，
   权限点再来一遍只会产生"权限点给了但数据范围是 self"这种自相矛盾状态。**应删，而不是补门控。**

**对本次设计的影响**：§3.2 权限点处置表据此定稿（25 → 目标 17）。同时**新增原则 P2**
——"权限点必须有门控，否则不得入库"，并配 `scripts/check_perm_reachability.py` 红灯拦截，
**防止将来再堆装饰品**。这也是"给 admin 全部权限"这句话最直接的反例：
它给的一半是**没有任何地方在读的字符串**，却让审查者以为存在控制。

**另补两条实测**：
- 前端菜单 **14 项里只有 3 项带 `v-if="can(...)"`**（主数据/表单模板/审计日志，`HTML:24`）；
  **权限管理 / 流程管理 / 风险预警 三项无门控** ⇒ 普通员工能看见并点进去，然后吃 403。
- 前端 `can()` 一共只用了 **10 个 code**，与后端 `@RequirePerm` 的集合**完全一致** ——
  这说明"有效点"这一集合是自洽的，**问题纯粹出在另外 15 个点的存在本身**。

---

## 十四、阶段二 · 主体-成员分离（2026-09-23 新增，完整方案见设计说明 §10）

**这一节的性质**：本文正文（§一~§十三）是在**现有结构下**把已知错误行为清干净；
本节让**那类错误无法再被表达**。两者做的是同一件事的两层，不是两条可选路线。

### 14.1 根因一句话

> **`sys_user` 一张表同时承担了三个正交概念：登录主体 / 组织成员 / 权限持有者。**

由此派生出本文所有主要缺陷，且**都是同一个失效模式**——把不变量放在"每一处调用点的纪律"里：

| 症状 | 因为不变量没进结构 |
|---|---|
| 4 处 `hasRole("ADMIN")` | "运维账号"在数据模型里**没有位置**（`sys_role` 只表达"有哪些权限点"） |
| 需要逐个过滤 candidateIds（§十二补 0-3） | "审批人必须是组织成员"**从未被表达过**，只是"恰好所有人都在 `sys_user`"的副作用 |
| 15 个零引用权限点（§13.8） | 权限点是**自由声明的字符串**，没有任何机制保证被使用 |
| 自锁无护栏（§十二补 0-2） | "末位管理员"是不变量，但写在 service 里而不是守卫里 |

⇒ 本项目**自己的硬约束 4** 早就总结过这个教训（"行级权限只准一份实现；新增可见性判断禁止再写 switch，曾有 IDOR"）。同一个道理：**不变量必须进结构。**

### 14.2 设计要点（详见设计说明 §10.2~§10.5）

```
sys_principal (新增：登录主体)   id, account, password, principal_type('MEMBER'|'OPS'), status, ...
sys_user      (改造：人员档案)   principal_id PK, dept_id NOT NULL, job_no, real_name, ...
                                 ↑ account/password 移出；dept_id 由可空改 NOT NULL

运维账号 = 只有 sys_principal 行、没有 sys_user 行
⇒ 天然无部门（合法终态）、天然不进任何人员查询、天然不是审批人候选
```

**关键取舍：`dept_id` 改 `NOT NULL` 而不是"应用层记得判空"。**
现在 `document.dept_id` 是 NOT NULL 而 `sys_user.dept_id` 可空 —— **这个错配就是 B4 建单 500 的根因**。
改 NOT NULL 后非法状态**写不进去**，错误在"建员工时"暴露，而不是在"发单时 500"。

**配套三件（同源问题一起消灭）**：
1. `PermCode` 枚举 + 启动时双向校验 `sys_permission` ↔ 枚举 ⇒ 装饰品权限点变成**服务起不来**
2. **删掉 `LoginUser.hasRole(String)`**（收口 4 处后调用者归零）⇒ 再写硬编码**编译不过**。
   ⚠ 不用 ArchUnit：要加新依赖（离线构建），且**测试型护栏在本项目不执行**（所有构建入口都带 `-DskipTests`）
3. 新增 `AdminScope`（管哪些部门的人），与 `DataScope`（看多少数据）正交 ⇒ 分级管理员变成**加配置**

### 14.3 ⭐ 代价：**业务表零改动**（这是能下决心的关键）

因为 `sys_principal.id` **复用旧 `sys_user.id` 的值**，所有指向用户的列**数值不变**：

| 列 | 全库次数 | 迁移动作 |
|---|---|---|
| `created_by` / `updated_by` | 32 | **不动** |
| `user_role.user_id` / `user_post.user_id` / `department.leader_id` | 8 | **不动** |
| `applicant_id` / `assignee_id` / `receiver_id` / `delegator_id` / `delegate_id` / `uploader_id` / `operator_id` | 11 | **不动** |

⇒ 要动的只有：**1 张新表 + 1 张表改造 + 登录入口 + 3 个查询（`SysUserMapper`）**。
**业务表 DDL 与业务数据完全不动。** 这是"主体分离听起来大、实际耦合面很窄"的原因。

**唯一高风险处是登录链路**，用 **M1 双写 → M2 切读 → M3 映射 → M4 收尾 → M5 加固** 五步降风险，每步可回滚（设计说明 §10.6）。

### 14.4 阶段二能一次消掉什么

| 本文正文里的待办项 | 阶段二之后的状态 |
|---|---|
| B1 预览 NPE / B2·B4 建单 500 / B5 自审移除 | **触发路径不存在**（admin 不建单、不做审批人）⇒ 3 条从待办中消失 |
| B4 幽灵单（`#16②`） | **仍需单独修**（独立竞态 bug，与主体模型无关）—— 这条不能省 |
| `#16①` `leader_id` 不校验可用性 | 查询改为 `JOIN sys_user` ⇒ **结构级排除**（成员表里没有就不是人） |
| C6 自我提权 / 自锁（§十二补 0-2） | 收口到 `AdminGuard` 单入口，"末位管理员"成为守卫而非散落的 if |
| C7 干预能力方向反了 | 不受影响，仍需按 §3.4 收口 `:393` + 补转办/终止/催办 |
| §13.8 的 14 个装饰权限点 | 变成**启动失败**，不用逐个 grep |
| D7 分级管理员 | 加 `AdminScope` 配置即可（不再需要改架构） |
| D8 admin 发单权 | **自然消失** —— OPS 不是组织成员，发单权没有载体 |
| D2 `document.dept_id` | **不改**（改的是 `sys_user.dept_id` → NOT NULL，方向相反且更正确） |

### 14.5 ⚠ 顺序约束（与 §4.1 同源，不可违）

**必须先把 4 处 `hasRole` 收口完（本文批次 0 的 0-2~0-5 + §13.4），才允许回收/改名 admin 的角色编码。**
原因不变：旁路按字符串判定，先动编码会让它们**静默失效**。

### 14.6 待决策

| 项 | 选项 |
|---|---|
| 阶段二何时排进来 | 已设计、待触发。触发条件（设计说明 §10.7）：再交下一家客户 / 过等保 / 要部门级管理员 / 代审被审计追问 |
| 若做，从哪步起 | 建议 **M1（双写）与阶段一并行**：M1 不改任何现有行为，纯增量，且能把 M2 的切换风险提前暴露 |

---

## 十五、交付视角评审：接口交接成本 + 前端可理解性（2026-09-24）

**评审标准**（用户 2026-09-24 拍板，已入 `MEMORY.md`）：主体是后端；仓库里的前端只是**验证/可视化/交互演示工具**；**接口最终要交给公司其他前端人员对接** ⇒ 接口不能只看"功能对不对"，要看**接手成本**；前端不能只看代码优雅，要看**非技术人员（含客户）能不能看懂并操作得下去**。⇒ **技术正确 ≠ 交付合格。**

> 复核纪律：下表中标「实测」的条目已由我逐条读码或实跑复核（含行号）；未标注的来自两路侦察报告，尚未逐条复核。

### 15.1 接口侧

| 级 | 问题 | 证据（已实测复核） |
|---|---|---|
| 🔴 阻断 | **没有任何可访问的接口文档** | `application.yml:175-180` springdoc 两处 `enabled: ${OA_OPENAPI_ENABLED:false}`；全仓 `@Operation/@Tag/@Schema/@ApiResponse` 注解数 **= 0**（实测 `grep \| wc -l`）；本机 8080 实测 `GET /v3/api-docs` → **404**、`/swagger-ui/index.html` → **404**；README §5「接口清单」只列 **9 个 API 前缀**，而控制器实为 **19 个** ⇒ **缺 10 个**：`attachment / audit / delegation / document-types / escalation / notification / permission / post / risk / seal` |
| 🔴 高 | **`formData` 请求是对象、响应是字符串** | 请求 `DocumentCreateRequest.java:18` `Map<String,Object> formData`；响应 `Document.java:38` `String formData` ⇒ 对接人按请求形态解析响应会静默拿到 undefined |
| 🔴 高 | **`availableActions` 取值 ≠ `/approve` 的 `action`（未知值静默当"通过"）** | `FlowRuntimeService.java:153` 只判 `"reject"`，**其余任何值都落 `:157` 的"---- 通过 ----"分支**；`availableActions` 由 `DocumentService.java:415/446` 生成，前端按 `'approve'/'reject'/'withdraw'` 判断（`oa.html:1608-1610`）⇒ 传错/传空 ⇒ **静默批准** |
| 🔴 高 | **`rejectTo=PREV` 语义与文档不符** | `FlowRuntimeService.java:201-203` 把 `START` 与 `PREV` **一起**排除出 `backToSpecificNode`；`:198` 注释明写「默认终止实例退回发起人」⇒ **PREV 实为"退回发起人"，不是"上一节点"**。且前端 `REJECT_TARGETS`（`oa.html:196-202`）**根本没有 PREV 选项** |
| ⚪ 已澄清（**不是缺陷**） | ~~`/api/users` 权限口径自相矛盾~~ → **降级为「文档要显式声明」** | `UserController.java:41-45` 的注释写明是**故意**的：「选人下拉要在客户端对全量做模糊搜索，分页反而做不了」，与 `:55-56` `/page`（服务端分页、给表格用）是**刻意分工**。⚠ 但同文件 `:66 /{id}`、`:71 /roles` 仍无门控，**这两条没有被那条理由覆盖**（前端只调 `/api/users` 与 `/api/users/page`，`/{id}`、`/roles` 前端根本不调）⇒ **建议直接删掉这两个端点或补 `system:user`**（补了不伤人，因为没人调） |
| 🔴 高 | **接口文档缺失** | 见下 15.4 已处理 |
| 🔴 高 | **同一 `code:400` 对应两种 HTTP status** | `GlobalExceptionHandler.java:24` `BizException` **无 `@ResponseStatus`**（HTTP 200 + body `code:400`）；`:30-31` 校验异常 `@ResponseStatus(BAD_REQUEST)`（HTTP 400）⇒ 对接人无法用 HTTP status 统一判错 |
| 🔴 高 | **畸形 JSON / 参数类型错掉进兜底 500** | 已注册 handler 仅 7 个（`:24/30/39/45/61/74/87`），**无 `HttpMessageNotReadableException`、无 `MethodArgumentTypeMismatchException`** ⇒ 落入 `:87 Exception.class` → **500**（把"调用方传错"报成"服务故障"） |
| 🔴 高 | **单据类型有两个等价接口** | `GET /api/documents/types` vs `GET /api/document-types/enabled`，前端两个都在用（`oa.html:364-365` vs `:1127`）⇒ 对接人不知该用哪个 |
| 中 | 状态/枚举无查询接口 | 单据状态 0-6、节点状态、数据范围枚举均无字典接口（侦察报告，未复核） |
| 中 | 三套日期格式 | 响应 `yyyy-MM-dd HH:mm:ss` / 查询参数 ISO 带 `T` / 用印查询只要日期（侦察报告，未复核） |

### 15.2 前端侧

| 级 | 问题 | 证据（已实测复核） |
|---|---|---|
| 🔴 高危 | **加载失败被显示成"暂无数据"** | 全文 **38 处 `catch(function`**；`watch(page)` 内 **20 处 `.catch(function(){})`**（`:3040-3092`），另有 `:1219/1659/2233/2267/2302/2468/2524/2531` 空 catch；多处 `api.xxx().catch(function(){ return []; })`（`:1127-1140/1164/2427`）**把失败伪装成空列表** ⇒ 客户看到「暂无数据」，不会报障 |
| 🔴 高危 | **重复提交（无 loading、无 disabled）** | `oa.html:38` `<el-button type="primary" @click="submitRequest">提交审批</el-button>` **无 `:loading`、无 `:disabled`**；`ensureDraft` 防重有竞态（`:1680` 注释自述「否则每点一次「提交审批」就会多出一张草稿单」）⇒ 双击产生重复单据 |
| 🔴 高 | **`dictTypes` 全文件未定义** | 全文仅 **1 处**命中，在模板 `:33` `<el-option v-for="t in dictTypes">`；script 中**从未定义**（`:3145` setup 返回值里也没有）⇒ **数据字典下拉永远为空、永远选不中类型** |
| 🔴 高 | **首页"我的待办"自相矛盾** | `oa.html:26` 首页卡片标题「待办审批」，数据源却是 `documents.slice(0,4)`（= 全部可见单据前 4 条），而 hero 的 `todoCount` 来自 `/api/todos` ⇒ **同屏两个数字互相矛盾** |
| 高 | 死按钮 | 同 `:38` `<el-button>保存草稿</el-button>` **无 `@click`**（点不动也无提示） |
| 高 | 写死数据 | `DEMO_ACCOUNTS`（`:203-208`）把 9 个演示账号 + `123456` 印在**登录页**（空系统里这些账号不存在）；`REJECT_TARGETS`（`:196-202`）**「核算会计」与「内控合规」都映射到 `n3`**（重复缺陷），且整表只适用于日常付款一种流程；首页 hero（`:26`）写死 `2026年8月26日 · 星期三`、`早上好，林安然`、`96% 合规率`、`100% 留痕` |
| ⭐ | **零后端改动、收益最大** | `serverPreviewText`（`:1672`）与 `previewNodes`（`:700/1654/1659`）**已实现、已 export（`:3145`）但模板从未渲染**；对照 `currentFlowText` 是靠 `:77` 的 DOM 注入硬接上的 ⇒ **接上即让客户看见"卡在谁、接下来谁审"** |
| 中 | 流程可见性不足 | 详情抽屉只显示已走节点，**不显示后续待审节点** |
| 中 | 空状态缺"下一步" | 16 个 `el-table` 仅 6 个带 `empty-text` ⇒ 10 个只有默认「暂无数据」 |
| 中 | 术语泄漏 | 界面直接印 `schema`、`DAILY/BIZ/REIMBURSE/SEAL`、`system:dept`、`n1/n3`、`doc.amount >= 20000`、`oa.flow.escalation.enabled`、「指派规则/运行时/重新部署/兜底/留痕/穿透」 —— **2026-09-24 已修其中 4 类**（`schema` / `system:*` / `n1/n2/*` / `doc.amount >= 20000`），详见 **§15.7**；剩余 `DAILY/BIZ/…` 业务类型编码、`oa.flow.escalation.enabled` 配置键、「重新部署/兜底/留痕」等行话**未动** |
| 中 | 空系统无首登引导 | 引导必需数据可走 `bootstrap_via_api.py`，但客户自己面对空系统时没有任何"下一步做什么" |

### 15.3 建议优先级（**等拍板**，未动手）

- **P0 交付前必须**：A1 接口文档（开 `OA_OPENAPI_ENABLED` 生成 + 补 README 缺失的 10 组）· B1 静默失败（失败要说"加载失败"）· B3 重复提交（loading+disabled）· A4 `dictTypes` 未定义
- **P1 客户能直接感觉到**：接上 `serverPreviewText`（零后端改动）· 首页待办口径统一 · 空状态文案 —— **前 3 项 2026-09-24 已完成，见 §15.6**；A5/A6/A7 契约（`formData` 形态、`action` 映射、`rejectTo` 语义）**仍未写**（本轮只把 §15.6 里两条实测契约落进了文档）
- **P2 工程整洁**：A3/A4 错误语义与兜底 500 · 日期格式统一 · 术语人话化
- ~~A9 权限口径~~ → **已澄清，不是缺陷**（见 15.1 表首行）

### 15.4 本轮已修（2026-09-24，已构建 + 已实测验证）

**后端（纯增量，不改既有成功路径）**
- ✅ **补齐 2 个缺失异常处理器**（`oa-boot/.../handler/GlobalExceptionHandler.java`）：`HttpMessageNotReadableException`（畸形 JSON / 字段类型不符 / 空 body）+ `MethodArgumentTypeMismatchException`（路径参数类型错）→ **HTTP 400 + 可读提示**。此前落 `Exception.class` 兜底 ⇒ **500「服务异常，请联系管理员」**，对接方明明是自己拼错了 JSON 却去查服务端。写法与既有 `MaxUploadSizeExceededException`/`NoResourceFoundException` 两个处理器完全同构（那两段的注释已经把这个道理写清楚了）。
  **实测（本机 8080，改动前后对比）**：① 畸形 JSON → `HTTP 400 / code=400 /「请求体格式不正确…」`（原 500）；② `GET /api/users/abc` → `HTTP 400 / code=400 /「参数类型不正确：id（收到 abc）」`（原 500）；③ 空 body → 400（原 500）。**回归**：`GET /api/users`、`GET /api/depts` 仍 `200 / code=0`。
- ✅ **`/todos/approve` 的 action 白名单 fail-fast**（`FlowRuntimeService.approve`）：原实现只判 `"reject"`，**其余任何非空值都静默走「通过」分支** ⇒ 一个 typo（`aprove`、`agree`）或将来新增动作名 = **替审批人签字且不留痕**，与 P5「失败必须显式」正面冲突。现在只放行 `approve`/`reject`，其余抛 `BizException(400, "不支持的审批动作：…")`。已核对**全部调用方**（前端 3 处、用例 6 处）只传这两个值。
- ✅ **`ApprovalRequest.rejectTo` 的 javadoc 对齐实现**：原文写「PREV 上一节点」，实为与 `START` 等价（`doReject` 把两者一起排除出"跳回指定节点"）⇒ 改为显式写明「PREV 当前等同 START，需要退回上一节点请传具体 nodeKey」。

**前端**
- ✅ **`dictTypes` 未定义（真 bug）**：全文只在模板 `:33` 被引用、script 里**从未定义** ⇒ 数据字典类型下拉**永远为空** ⇒ 选不中类型 ⇒「＋ 新增字典项」**永远禁用**，客户在界面上根本进不了字典维护（后端接口一直是好的）。改为 `computed(() => Object.keys(dictMap))`（key 即 `/api/dicts` 返回的类型）。
- ✅ **防重复提交**：`submitRequest` 加在途标志（开头 `if (submitting.value) return`），并把原先 inline 的表单组装抽成 `isSealForm / collectFormData / buildCreateBody` 三个纯函数。原实现只靠 `ensureDraft` 的判空挡，而那是**竞态**：两次点击都在同一个 `await` 之前读到 `draftId == null` ⇒ **各建一张草稿单**，而重复单据在审批系统里是要人工清理的脏数据。
- ✅ **死按钮「保存草稿」接上**：与提交**共用** `buildCreateBody`（各写一份必然漂移，而漂移的表现是"草稿存的字段和提交的不一样"，不报错、只在后续节点按错误表单校验）。
- ✅ 三颗按钮都加 `:loading` / `:disabled`，对齐本仓既有的 `xxxDialog.saving` 模式。

**验证结果**
- `bash scripts/run_all_verify.sh` → **15/15 套件全绿**，且演示库基线**前后完全一致**（`document=46 / ACT_RU_TASK=5 / notify=84 / dt1_flow=131` …）。
- `verify_flow_interaction.py` 新增 **3 条负向断言**（31→34，全过）：拼错的 `action` 必须报错 · 非审批语义的 `withdraw` 同样报错 · **两次非法调用后节点状态未变、仍是原承办人的待办**（只看返回码不足以排除"报错了但也批了"）。⭐ 第 1 条靠**精确匹配新代码的报错文案**成立 ⇒ 旧实现永远不会产生这句话 ⇒ **不可能假绿**。
- `run_all_e2e.sh` → 3/5 套件绿（`dashboard` 44/44 · `final_gaps` 65/65 · `business_gaps` 79/79）。⭐ **`frontend_dashboard_e2e` 会点「提交审批」并核对 `POST /api/documents` 落库 + `document_link` 落关联行** ⇒ **本次前端重构被端到端覆盖**（不是只靠读码判断）。

### 15.5 ⚠ 2 个 E2E 套件红 = **演示库数据，不是代码回归**（证据链）

**结论**：`frontend_admin_e2e` 83/88、`frontend_attachment_e2e` 40/56 —— 全部由「日常付款申请」的**生效流程只有 2 个节点**引起。

| # | 证据 | 内容 |
|---|---|---|
| 1 | **改动前的日志**（最关键） | `/tmp/e2e_after_docfix.log` 时间戳 **今天 09:36**，早于我 11:47 的第一次改动，里面已经是 **83/88 + 40/56** —— 数字**完全相同** |
| 2 | 数据来源 | `flow_config` 130(V2)、131(V3) 均于 **2026-09-23 10:20:41 / 10:22:52** 由 `created_by=1`（admin，**界面操作**）创建 —— 早于本轮工作整整一天 |
| 3 | 生效指向 | `document_type#1.flow_config_id = 131`，其 `updated_at = **2026-09-24 09:45:26**` —— 早于我改动 **2 小时** |
| 4 | 结构差异（根因） | **V1(id=1，6 节点)**：`n1 发起 → n2 直属部门负责人 → n3 会计(按部门) → **gw1 金额分支(网关,node_type=3)** → n4 公司领导(大额) → n5 出纳付款(node_type=4)`；**V3(id=131，仅 2 节点)**：`n1 发起人 → n2 直属部门负责人` |
| 5 | 失败断言吻合 | admin：`{"rows":2,"gwRows":0,"branchRows":0,...}`（找不到网关行 / 分支行）；attachment：**首条**即「单据推进到出纳付款」失败（**流程里没有出纳节点**）⇒ 之后 16 条全级联 |

⇒ **修法（待拍板，本轮未动）**：让「日常付款申请」重新使用完整的 6 节点流程。两条路：
- **(a) 指针回退**：`document_type#1.flow_config_id` 指回 V1 并让 V1 生效 —— 1 行、可回滚；但 V1 是被 09-23 那两版**刻意废弃**的，回退等于撤销别人的发布。
- **(b) 走接口发布 V4**：复制 V1 的 6 节点 + 指派规则 + 分支条件（`POST /api/flows/configs` 建流程时**内部自动部署**）—— 更干净，但要先知道那两版是"有意测试"还是"误操作"。

⚠ **建议先问一句再改**：09-23 10:20 那两版是 admin 在界面上主动发布的、且先后发了两版（V2→V3），不像事故，更像在试流程编辑器。**不要默默覆盖别人的发布。**

---

### 15.6 P1 交付视角批次（2026-09-24 实施；含 E2E 反查出的 3 个真 bug）

用户指令：「你身为高级开发工程师，评估下分析下。然后开始动手」→「继续」。定序判据 = **改动风险 × 交付价值**；两条纪律：① 不动 load-bearing 契约（业务异常保持 HTTP 200 + `code≠0`）；② 不覆盖别人的发布。

**前端改动（3 处，都是 15.1/15.2 里点名的 P1）**

| # | 改动 | 为什么是"交付级"而不是"好看" |
|---|---|---|
| A | 「提交审批」弹窗接上**流程预览**（`serverPreviewText`） | 该函数与 `previewNodes` 早已实现并 export，**模板从未渲染** ⇒ 零后端改动、客户第一次能看见"这条单会经过谁"。⚠ 金额 `@change` 必须**重取**：网关按 `amount` 判分支，只取一次会让预览反过来误导人 |
| B | 首页「我的待办」卡片口径统一到 `/api/todos` | 原数据源是 `documents.slice(0,4)`（全部可见单据前 4 条），与同屏 hero 的 `todoCount` 口径不同 ⇒ 一屏两个数字互相矛盾 |
| C | 驳回弹窗「指定驳回层级」按**该单据实际在跑的那一版流程**动态生成 | 原为写死 5 项，且「核算会计」「内控合规」**都映射到 n3**（选内控、实际退回核算会计）；整表只适用于「日常付款」一种流程，换单据类型选谁都是错的 |

另：10 个 `el-table` 补 `empty-text` ⇒ **空状态 16/16 全覆盖**；登录页演示账号区加一行「这些账号只存在于本机演示库」。

**E2E 反查出的 3 个真 bug（全部是本次改动引入/暴露的，已修并回验）**

1. ⭐ **预览行每一站都显示"待解析"**：`serverPreviewText` 读的是 `nd.assignees`，而 `FlowPreviewVO.NodePreview` 给的字段是 **`candidateNames`**（`assignees` 是 `GET /configs/{id}` 里按 `nodeId` 索引的**规则表**，两者不是一回事）⇒ 把"解析出来了"显示成"没解析出来"，**比不显示更误导**。E2E 拿真实返回比对才发现。
2. ⭐ **待办卡片点不开**（`openDetail` 依赖 `documents` 列表）：`/api/todos` 与 `/api/documents` 的可见范围**本来就不是一套**（待办 = Flowable 派给我的任务；documents 走行级数据范围）。实测 **`zhouzh` 有 4 条待办、`/api/documents` 返回 0 条**（`total=0`）⇒ 卡片变成"看着能点、点了报未找到该单据"。`openDetail` 只用 `docId`，改为直接放行。（另实测：`GET /api/documents/38` 对 `zhouzh` 是 **200 + 带 approve/reject**，即**承办人身份本身能看**，所以这一个修复就够了。）
3. **驳回目标匹配用错了版本号**：`selected.flowVersion` 实际来自 `document.form_template_ver`，**是表单模板版本、不是流程版本** ⇒ 一旦"表单模板升版、流程没升"就会匹配失败、掉进 `docTypeId` 兜底拿**当前生效版本**去列旧单据的驳回目标（正是要避免的事）。改为**先按流程名匹配**，版本只做次级判据。

**新增契约发现（给前端对接方，已实测）**

- `DocumentDetailVO.flowVersion` ← `DocumentService:408` `vo.setFlowVersion(doc.getFormTemplateVer())` ⇒ **字段名与语义不符**，不能当流程版本使用。⚠ 这是接口契约问题，**建议后端改名或补 `flowConfigId`**（未动代码）。
- `POST /api/flows/configs/{id}/preview` 返回的是**完整配置链路**（网关两侧目标节点都在），**不按 `amount` 裁剪**；网关节点自带 `conditionExpr`（如 `[{"expr":"doc.amount >= 20000","target":"n4"},{"default":true,"target":"n5"}]`）。⇒ 前端不能把预览当成"这条单会走的路径"来显示（现在预览行把网关标成「按条件分支」，不会伪装成人审节点）。

**新增 E2E 套件 `oa-backend/scripts/e2e/frontend_p1_review_e2e.js`（44 条，已挂进 `run_all_e2e.sh` 的 `SUITES`）**

覆盖：① 首页待办口径三处一致（hero == 卡片条数 == 菜单角标）② 跨账号点开待办（`zhouzh`：待办 4 / documents 0 —— 直接锁住上面第 2 个 bug）③ 驳回目标 == 该单据在跑那一版的节点（**含**"不含当前节点""不含旧写死三项"两条负向断言）④ 提交弹窗预览 == 服务端真实节点链 + **真实审批人姓名**（`0` 处"待解析" —— 锁住第 1 个 bug）⑤ 金额变化会重新拉预览 ⑥ 基线前后一致 + 绝对不变量。全程只读，不提交、不驳回。

**验证结果（全部实测）**

- `run_all_e2e.sh` → **6 套件**：`admin` 83/88 · `attachment` 40/56（**两者仍是 §15.5 的演示库流程数据所致，非回归**）· `dashboard` 44/44 · `final_gaps` 65/65 · `business_gaps` 79/79 · `p1_review` 44/44。
- 演示库基线前后完全一致（`document=46 / attachment=1 / notification=84 / ACT_RU_TASK=5 / flow_config=5`）；`cleanup_demo_pollution.py --dry-run` 三项（无配置支撑的部署 / 孤儿实例 / 孤儿通知）均「无需清理」。

**本轮踩坑（写下来防复发）**

- ⚠ **源码级断言会被"注释"触发**：我在注释里引用了旧文案「今日有 5 项审批待处理」，直接把 `business_gaps` 的两条 `served.indexOf('5 项审批') < 0` 判红（**77/79**）。这不是功能回归。已改写注释；并让该断言**打印命中位置**——它原来只打印运行时文案，把排查指向了完全错误的方向（浪费了几分钟）。
- ⚠ **菜单文案必须用运行时的那份**：原型写 `index="approve"` → 「审批中心」，而注入脚本按 `pageNames.approve` 改成了**「待我审批」**。用原型文案点 = **点了个空且不报错**，后续 10 条断言集体失败，看着像功能坏了、其实是没走到那一页。
- `page.evaluate` 里读不到外层常量（函数会被序列化）⇒ 菜单名之类的参数必须**显式传进去**。

### 15.7 前端词人话化（2026-09-24 实施；用户选定为"下一件"）

**判据（用户的评审标准）**：本项目**主体是后端**，前端是**验证/可视化/交互演示工具**；
前端的评估标准不是代码优雅，而是**非技术人员（含客户）能不能看懂并操作得下去**。
**技术正确 ≠ 交付合格。**

**做法：人话在前、技术原值留在括号里** —— 不删技术信息。
理由：后端、日志、`ACT_*` 表、字段权限行**都按 nodeKey / 权限点编码说话**，排障必须对得上；
客户只需要看懂前半句。两边都不丢。

| # | 位置 | 改前 | 改后 |
|---|---|---|---|
| 1 | 表单模板页说明 | 「表单由模板 **schema** 驱动」 | 「表单长什么样，由模板里定义的**表单结构**决定」 |
| 2 | 模板编辑器字段标签 | `schema（JSON；…）` | `表单结构（JSON；…）` |
| 3 | 校验失败提示 | 「**schema** 不是合法 JSON」 | 「**表单结构**不是合法 JSON」 |
| 4 | 模板无字段提示 | 「该模板 **schema 没有 fields**，无字段可配置」 | 「这个模板的**表单结构里还没有字段**，暂时没法配置权限」 |
| 5 | 设计器按钮 / toast | 「生成 **schema**」/「已生成 **schema JSON**」 | 「生成**表单结构**」/「已生成**表单结构**」 |
| 6 | 设计器字段输入 | `标识(**key**)` / `字典类型(可留空)` | `标识（英文，如 **amount**）` / `选项来源（字典类型，可留空）` |
| 7 | 字段权限·节点下拉 | 裸 `n2`、`n3`（`*`/`n1` 有中文，其余没有） | 新增 `nodeKeyLabel()`：`所有节点（*）` / `发起节点（n1）` / `第 2 步（n2）` |
| 8 | 字段权限·说明 | 「节点选 \* 表示…具体节点（如 **n3**）…发起节点（**n1**）…全量覆盖」 | 「这里决定「**每个审批节点能看到哪些字段、能不能改**」。默认：发起节点（n1）能填能改，之后的节点只能看、不能改…」 |
| 9 | 字段权限·表格空态 | 「先在「表单模板」的 **schema** 里加字段」 | 「先到「表单模板」的**表单结构**里加字段」 |
| 10 | 分支表达式输入框 | 「如 `doc.amount >= 20000`（字段须以 `doc.` 开头）」 | 「例：`doc.amount >= 20000` —— 表示「**申请金额 ≥ 20000**」；金额等字段名前面要写 `doc.`」 |
| 11 | 权限不足提示（**6 处**） | 「无主数据维护权限（**system:dept**）」等 —— **权限点编码直接印给客户看** | 「**没有维护部门、岗位与人员的权限**」等；编码改为只进 `console.warn`（新增 `noPerm()` 帮手） |
| 12 | 未设审批人提示 | 「未配置审批人（该节点任务会进入**人工兜底**）」 | 「未设置审批人（走到这个节点会被**系统自动跳过**）」 |

**第 12 条是事实错误，不只是术语。** 后端无匹配审批人时走的是 `autoSkipUnassigned`
（自动通过并留痕），**根本没有"人工兜底"这条路径**。原文案会让配置人员以为
"反正有人兜着"，实际上这一环是**直接跳过、等于没审** —— 属于把风险说成了保障。

**本轮踩坑（值得写下来）**
- ⚠ **新增的模板函数必须在 setup 里 `return`，否则运行时报 `xxx is not a function`，页面局部渲染不出来但**不报错到界面**。
  本次 `nodeKeyLabel` 就是漏了 return ⇒ `frontend_admin_e2e` 的「**无控制台错误**」断言立刻红（87/88）。
  **这正是回归网该起的作用**：这类问题肉眼点页面很难发现（下拉只是少了几行），只有断言能抓住。
- ⚠ **改完源要重新 `cp` → 构建 → 重启**，否则 E2E 的「服务返回的 oa.html 与本地文件**字节一致**」会红
  （本次中途又改了 6 处权限提示，served=307200B vs local=307624B）。

**验证**：`run_all_e2e.sh` **376/376（6 套件）**；`run_all_verify.sh` **17/17**；
演示库基线前后一致；`cleanup_demo_pollution.py --dry-run` 无需清理。

**剩余未做（有意留口，非遗漏）**
- 审计日志的「**模块**」「**动作**」列直接印英文编码（`auth` / `document` / `login` / `approve`）。
  不做的原因：① 会碰 E2E「模块列 === 'auth'」的断言，需同步改用例；② 审计日志面向**审计员**而非客户日常操作；
  ③ 本轮已改 12 处，再扩大会稀释验证质量。⇒ 要做就是独立一小批（映射表 + 改断言）。
- `DAILY/BIZ/REIMBURSE/SEAL` 业务类型编码、`oa.flow.escalation.enabled` 配置键、「重新部署 / 兜底 / 留痕 / 穿透」等行话。

---

## 附、未核实

- [ ] 测试服务器 `hxj-oa` 库当前层状态（`deploy_status`、`document` 行数）—— 本地无服务器凭据
- [x] ~~审计日志是否覆盖高危操作~~ —— **2026-09-23 已核实：覆盖良好**。写接口（Post/Put/Delete）共 **52 个**，`@Audit` 共 **54 处**；未挂的只有 **4 个且全为无副作用**接口（`FormTemplateController:50 /validate`、`NotificationController:39 /{id}/read`、`:46 /read-all`、`FlowConfigController:79 /configs/{id}/preview`）。**真正的问题是"高危操作本身不存在"**（见 C7），不是"没留痕"
- [x] ~~**E2E 5 套件本轮未重跑**~~ —— **2026-09-24 已重跑**：3/5 绿（244/332 通过）。2 个红套件已定性为**演示库流程数据**所致（证据链见 §15.5），非代码回归；修法待拍板。**2026-09-24 晚补**：套件扩到 **6 个**（新增 `frontend_p1_review_e2e`，44 条），`business_gaps` 一度因**注释里引用了旧文案**被源码级断言判红（77/79），改写注释后回绿 79/79。当前 **4 绿 2 红**（红的两个仍是 §15.5 那两条），409/376 见 §15.6
- [x] ~~**演示库「日常付款申请」流程只剩 2 个节点**~~ —— **2026-09-24 已定性**：`flow_config` 130/131（各 2 节点）于 2026-09-23 10:20/10:22 由 admin 界面发布；`document_type#1.flow_config_id=131` 于 **2026-09-24 09:45:26** 指过去。详见 §15.5
- [ ] `escalation_api` 红灯确切根因
- [ ] nginx Basic Auth 是否已摘净
- [ ] 客户端浏览器/分辨率兼容清单（表单页在 1366×768 下的布局）

---

## 附二、2026-09-24 新发现（待决，不在原清单里）

### N-1 `flow_config_node.allow_reject` 是**零引用点**的声明字段（硬约束 18 的又一例）
查 V4/V1 等价性时发现。三条证据：
1. **接口上不可配置** —— `FlowSaveReq` 里根本没有该字段；`FlowConfigAdminService:527` 保存时**写死 `n.setAllowReject(1)`**（连同 DB 默认值也是 1）。
2. **服务端不校验** —— `FlowRuntimeService.approve` 的驳回分支不读它，一律放行。
3. **前端不读** —— `海峡金OA审批系统-联调版.html` 全文无 `allowReject`；驳回按钮按 `rejectTargets.length` 显隐，`openReject()` 只判 `pendingTaskId`。

唯一读它的地方是 `TodoService:159` → `TodoVO.allowReject`，而该字段同样无人消费。

- [ ] **待决**：要么让它生效（`approve` 里加校验 + 前端读它），要么从界面与数据库里删掉。**当前状态是"界面上能配、配了不管用"，比没有更危险**（同 §C 类问题）。
- 对照：`sla_hours` **是有引用点的**（`AssigneeTaskListener:91` 判 `compareTo(ZERO) > 0` 才设 `deadline`）⇒ 所以 `NULL` 与 `0.00` **行为完全相同**，V4 落 0.00 与 V1 的 NULL 等价。

### N-2 演示库「日常付款申请」流程已恢复
走接口发布 V4（id=237 / version=4 / `DAILY_PAYMENT_V4`）复刻 V1 六节点，V1/V2/V3 保持废弃。**等价性已逐节点核验**（节点/类型/顺序/指派规则/分支条件全一致；网关 `gw1`→`n4`，`form_field_permission` 无一行引用 `gw1`）。
两个红 E2E 套件随之转绿：**`frontend_admin_e2e` 88/88、`frontend_attachment_e2e` 56/56**，全量 **376/376**。
`run_all_verify.sh` 已把 `dt1_flow` 纳入基线 ⇒ 再有人发布新版本会立刻暴露。
