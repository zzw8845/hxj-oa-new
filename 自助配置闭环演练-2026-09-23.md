# 自助配置闭环演练报告 —— 「客户还没给表单和流程，系统还能不能配出来」

> **实测方式**：另起隔离实例（`127.0.0.1:8081`）+ 全新空库 `hxj_oa_drill_test`，
> 全程**只调 HTTP 接口**（等价于在界面上点），完全不碰演示库 `haixiajin_oa`。
> 实测环境**已回收**（实例已停、临时库已 DROP、临时文件已清），本报告是唯一产物。
>
> **姊妹报告**：
> - `零数据冷启动体检-2026-09-23.md` —— 「只有一个 admin」场景，暴露 Bug ①（负责人 SQL 不校验可用性）/ Bug ②（STARTED 覆盖状态，幽灵单）
> - `冷启动操作清单与200次验证方案-2026-09-23.md` —— 冷启动操作顺序 + 200 次验证怎么设计
>
> 本报告是**第三条线**：不争论「只有一个 admin 行不行」，而是直接回答
> **「临时给一张客户没提过的单据，能不能当场配出它的表单和流程并跑通」**。

---

## 一、先回答你的三个问题

### Q1：「没有客户真实表单和客户真实流程及节点，我们系统就没法做了吗？」

**不是。** 这次演练的全部意义就是证明这件事：**系统现在能做的，是给任何一张单据都配出它的
表单和流程 —— 这件事本身不依赖客户。**

演练里我临时虚构了一张客户从没提过的单据「采购申请」（含一个金额门槛分支网关），
在全空的库里、只用接口，**从零配到端到端办结**。全程没有一行代码改动、没有一次重启。

所以「客户没来」不是不能做，而是**做出来的东西是"空的壳"，要等客户把真实的字段和节点填进去**。
系统的价值是"壳可以随便造"，不是"壳里的内容我替你编"。

### Q2：「自定义动态设计体现在哪呢？」

体现在**三个可配点 + 一条闭环**，但它们都不是 100% 动态，边界必须说清楚（第五节）：

| 层 | 可配什么 | 配在哪 |
|---|---|---|
| 表单层 | 字段增删、类型、必填、选项、字段级节点权限 | 表单模板设计器（可视化，出 schema JSON） |
| 流程层 | 节点增删、顺序、条件分支（金额/字段）、办理节点是否强制上传凭证 | 流程管理 |
| 指派层 | 每个节点「谁审」：6 种规则（发起人上级 / 部门+角色 / 大类+角色 / 角色 / 指定人 / 条件） | 节点指派 |
| 枢纽 | 把「表单模板 + 流程」绑成一张单据类型 | 单据类型管理 |

**闭环**：主数据 → 表单模板 → 流程 → 节点指派 → 提单 → 审批 → 办结。四步全在界面上。

### Q3：「别人做 OA 是什么样，不是从零开始搭的吗？」

**不是从零，是"一个引擎 + 一堆配置"。** 行业里没有哪家 OA 是给客户现场写代码的，差别在
**配置能力的厚度**（能配多少、配完之后还改不改代码）。见第六节对照。

---

## 二、演练做了什么（配置闭环四步，全部走接口）

| 步 | 动作 | 接口 | 实测结果 |
|---|---|---|---|
| ① | 建单据类型「采购申请」 | `POST /api/document-types` | `id=1 code=PROCURE_REQ category=DAILY status=1` |
| ② | 建表单模板（8 字段，含 1 条联动规则） | `POST /api/forms/templates` | `id=1 doc_type_id=1 version=1 fields=8` → 激活 |
| ③ | 建流程（6 节点，含金额分支网关） | `POST /api/flows/configs` | `id=1 PROCURE_REQ_V1 deploy_status=1(已部署)` |
| ④ | 配节点指派（4 条规则） | `PUT /api/flows/configs/1/assignees` | 见下表 |

流程结构（实测落库）：

```
n1 发起申请(5) → n2 直属部门负责人(1) → n3 核算会计(1) → n4 金额分支(3 网关)
                                                            ├─ doc.amount>=50000 → n5 公司领导(1)
                                                            └─ 默认             → n6 入库办理(4, require_attachment=1)
```

节点指派（实测落库）：

| 节点 | 规则 | 参数 | 实际解析出的审批人 |
|---|---|---|---|
| n2 直属部门负责人 | `initiator_leader` | fallbackRoleCode=DEPT_HEAD | 张经理（采购部负责人） |
| n3 核算会计 | `dept_role` | roleCode=ACCOUNTANT, fallback=company | 陈会计 |
| n5 公司领导 | `role` | roleCode=GM | 李总 |
| n6 入库办理 | `user` | userIds=[7] | 刘采购 |

> n6 原本用节点名「入库办理」会命中关键词兜底（关键词库有"出纳"，没有"入库"）→ 解析为空、
> 会被静默跳过。**故显式改成 `user` 规则指定人**。这正是"节点名要按模板库填"那条硬约束的现实版。

---

## 三、端到端结果（含分支与凭证）

两张单据，唯一变量是金额：

| 单据 | 金额 | 实走节点（`ACT_HI_TASKINST` 实测） | 最终 `status` |
|---|---|---|---|
| **FK202609230002** | 60000 | 直属部门负责人 → 核算会计 → **公司领导** → 入库办理 | **3 已通过** |
| **FK202609230003** | 30000 | 直属部门负责人 → 核算会计 → 入库办理（**跳过公司领导**） | 2 审批中（停在 n6，等上传凭证） |

**结论**：
- **条件分支真实分流**：60000 命中 `doc.amount >= 50000` 走 n5；30000 走默认分支直接 n6。
- **办理节点强制凭证生效**：0002 在 n6 无凭证时被拦，补传 `bizType=receipt` 的附件
  （`attachment id=1 doc_id=1 node_key=n6`，上传人刘采购）后才放行办结。这是
  `TodoService` 的 `require_attachment` 校验在起作用。
- 另外用 admin 单独验了一次建单：**先失败后成功**（见 Bug ④）。

---

## 四、本轮冷启动才暴露的两个阻断 Bug（Bug ③ ④）

> 与体检报告的 Bug ① ② **不是同一条**。四条合起来，才是"全新系统冷启动"的完整坑谱。

### Bug ③：`initiator_leader` 两个入参都为 null 时抛 NPE（流程预览 500）

**位置**：`oa-backend/oa-flow/src/main/java/com/hxj/oa/flow/service/AssigneeResolver.java:121`

```java
Long deptId = Objects.requireNonNullElse(JsonColumn.toLong(param, "deptId"), ctx.getApplicantDeptId());
//                                                                  ↑ 两个都为 null 时：
//                    Objects.requireNonNullElse(null, null) → NullPointerException: defaultObj
```

**触发条件**：规则参数里没写 `deptId`，**且**发起人自己也没有部门。此时不是走下面的
`fallbackRoleCode` 兜底，而是**在兜底之前先炸**。

**实测**：对无部门的 admin 调 `POST /api/flows/configs/1/preview` → 500（NPE at 121）。
给 admin 补上部门后，同样的请求立即恢复正常。→ 确认根因。

**为什么演示库从没暴露**：`seed_data.sql` 给每个用户都配了部门，`minimal_seed.sql` 的 admin
却是无部门的。**只有真正从零开始才会踩到。**

**修法（把"兜底"提前到"取值"之前）**：

```java
Long deptId = JsonColumn.toLong(param, "deptId");
if (deptId == null) deptId = ctx.getApplicantDeptId();
// 后续逻辑不变：deptId 为 null 时直接走 fallbackRoleCode
```

（或用 `Optional` 链。要点是：**别用 `requireNonNullElse` 去表达"可空兜底"**，
它的语义是"我保证至少有一个非空"，与这里的意图相反。）

---

### Bug ④：`document.dept_id` NOT NULL 无默认值 → 无部门用户建单 500

**位置**：`oa-backend/sql/init_database.sql:412`

```sql
`dept_id` BIGINT UNSIGNED NOT NULL COMMENT '申请部门ID',
```

**实测**：无部门的 admin 建单 → `Field 'dept_id' doesn't have a default value`（500）。
补部门 + **重新登录**（部门是登录时写进 JWT/上下文快照的）后，建单成功（`FK202609230005`）。

**为什么演示库从没暴露**：同上 —— 演示库所有用户都有部门。这是**数据假设掩盖了代码缺陷**
的典型：代码假定"申请人必有部门"，而这个假定只在冷启动时被打破。

**修法（择一，建议前者）**：
1. **建单时回退到公司级**：`dept_id` 取不到申请人部门时，落 `company_id` 对应的根部门/默认部门，
   单据不至于建不出来；同时在返回里给出一次性提示"申请人未设置部门，已按公司级归档"。
2. 或把列改成 `NOT NULL DEFAULT 0`，语义上表示"未归属部门"，由后续环节补。

> **两条 bug 的共同教训**：它们都不是"多人在线的正常公司"会踩的，而是**"全新系统 + 数据没配全"
> 才会有**。共同根因是 `minimal_seed.sql` 的 admin 缺 `dept_id`，叠加代码里"申请人必有部门"的
> 隐含假设。**Bug ③ 是兜底逻辑写错位置，Bug ④ 是 NOT NULL 兜底缺失。**

---

## 五、半动态表单：客户真实表单来了，哪些**还得改代码**

这是本次最该让你心里有数的一条。动态表单是**半动态**：结构可配，但有几个地方被写死。
按"客户表单来了一定会撞"的概率排序：

| # | 缺口 | 现场表现 | 位置 |
|---|---|---|---|
| 1 | **18 个字段名被前端静默吞掉** | schema 里若用了 `title/amount/reason/payType/payeeName/sealType/fileName…` 等 18 个 key，会被过滤掉不渲染，且**不报错**（字段凭空消失） | `海峡金OA审批系统-联调版.html:2601` `FIXED_FORM_KEYS` |
| 2 | **`rules.require` 是死数据** | 设计器里给字段配的联动规则（如"选了固定资产才必填日期"）**保存了但不执行**；服务端 `validate()` 只读 `fields[].required` | `FormTemplateService.validate()` 只遍历 `fields` |
| 3 | **`invoiceGroup` / `attachment` 两种字段类型不渲染** | 类型白名单里有，但渲染层没实现 → 配了等于没配 | 字段类型白名单 8 种 vs 实际可渲染 |
| 4 | **服务端校验只有 4 类** | required / min·max / money 两位小数 / select 选项白名单。复杂校验（跨字段、正则、动态必填）没有 | `FormTemplateService.validate()` |
| 5 | **字段权限只有接口、没界面** | `form_field_permission`（节点×字段）接口有用例，但**没有编辑界面**，只能靠接口配 | 刻意推迟 |

**这意味着什么**：客户真实表单 80% 能配出来（文本/数字/金额/日期/下拉），
但**只要客户提"选了 A 就必须填 B""这个附件只有财务能改""要传发票明细表"**，
现在就一定要改代码 —— 改的是上面这 5 处，不是"配一下"。

> 建议：客户表单到手前，**先只承诺第 1 类能力**（基础字段 + 必填 + 下拉），
> 带逻辑的字段规则作为"二期"报价。否则第 1 条最坑 —— **字段静默消失**，客户会认定是 bug。

---

## 六、行业路径对照：别人做 OA 是从零搭吗

**不是从零，是"引擎 + 配置"。** 差别在配置能力厚度：

| 维度 | 我们目前 | 主流 OA（泛微/致远/钉钉宜搭一类） |
|---|---|---|
| 表单 | 8 种字段类型，可配，带版本 | 20~40 种控件 + 明细表 + 子表单 |
| 字段规则 | 存了不执行（第 5 节 #2） | 完整联动引擎（显隐/必填/计算/联动赋值） |
| 流程 | 节点 + 条件分支 + 6 种指派规则 | 分支 + 并行 + 会签 + 加签 + 自由流 + 超时 |
| 指派 | 6 种规则 | 组织矩阵 + 动态表达式 + 岗位代理人 |
| 组织 | 部门树/岗位/字典/角色/数据范围 | 多法人 + 虚拟组织 + 兼职 |
| 二次开发 | 改 Java + 重建 jar | 插件/脚本/低代码扩展点 |

**结论**：我们在**主干流程（审批、分支、指派、权限、台账）上是完整的**，
差的是**表单逻辑引擎的厚度**和**低代码扩展点**。这不是"要不要从零搭"的问题，
而是"配置能力的深度决定客户要改多少次代码"。

**当务之急不是补控件数量，而是把第 5 节 #1、#2 修掉** —— 那两条是"明明配了却不生效"，
比"没有这个功能"更伤信任。

---

## 七、建议的下一步（按性价比排序）

| 优先 | 动作 | 理由 |
|---|---|---|
| **P0** | 修 Bug ③（NPE 兜底写错位置）、Bug ④（dept_id 兜底） | 冷启动 100% 必踩，且都是几行代码 |
| **P1** | 修第 5 节 #1（固定 key 静默吞字段 → 改成明确报错或补齐渲染）、#2（`rules.require` 执行） | 「配了不生效」是最伤的体验 |
| P2 | `minimal_seed.sql` 的 admin 补 `dept_id`（或建单/预览双兜底后仍建议补） | 从数据侧消掉整类冷启动 bug |
| P3 | 字段权限编辑界面（第 5 节 #5） | 接口已就绪，补界面即可 |
| — | 等客户真实表单/流程资料 | 不必等；上面的修复与客户来不来无关 |

> **一句话建议**：先把"配了不生效"和"冷启动必踩"两类修掉，
> 然后**拿这张虚构的采购申请单当模板去和客户对话** —— 让客户在真实单据上改字段、改节点，
> 比拿空白需求表去问，能快得多。

---

## 八、环境与基线核实（本轮无污染）

- 隔离实例：`127.0.0.1:8081`，库 `hxj_oa_drill_test`，独立 JWT 密钥 / 存储目录 / 日志目录
  → **实例已停、库已 DROP、`/tmp` 临时文件已清**
- 演示库 `haixiajin_oa` 基线核对（演练全程未触碰）：

| 项 | 基线 | 实测 | 结论 |
|---|---|---|---|
| document | 46 | 46 | ✅ |
| post | 8 | 8 | ✅ |
| seal_apply | 9 | 9 | ✅ |
| seal_record | 0 | 0 | ✅ |
| delegation | 0 | 0 | ✅ |
| escalation | 0 | 0 | ✅ |
| ACT_RU_TASK | 5 | 5 | ✅ |
| notification | 84 | 84 | ✅ |
| form_template | 3 | 5 | ⚠️ 非本轮产物（见体检报告第五节：09:35~10:19 手工试的 4 条） |
| document_type | 3 | 5 | ⚠️ 同上 |

> `8080` 端口的演示库实例仍在运行（本轮未启动它，也未停它），不受影响。
