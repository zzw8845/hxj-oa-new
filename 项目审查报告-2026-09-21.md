# 海峡金 OA 审批系统 · 全面审查报告

> 审查日期：2026-09-21
> 审查范围：前端 `海峡金OA审批系统-联调版.html`、后端 `oa-backend/`（145 个 Java 文件 / 10,808 行）、验证体系、运行态与数据库基线、版本控制完整性
> 审查方法：全量实跑（非静态阅读）+ 源码逐点核实 + 未认证黑盒探测
> 审查基线：`main @ 2f14d09`

---

## 一、总体结论

**代码质量与工程纪律高于同类原型项目，功能面完整可用；阻塞上线的只有安全加固与流程自证两项，没有发现功能缺陷或数据一致性漏洞。**

| 维度 | 评价 | 说明 |
|---|---|---|
| 功能完整性 | ✅ 良好 | 全链路（发起→逐级审批→办结）、附件、行级权限、审计留痕均可用 |
| 测试体系 | ⚠️ 有效但缺闸门 | 437 条断言全绿，但**无预期数量自证**，静默跳过无法被发现 |
| 安全（越权/注入） | ✅ 良好 | IDOR 已修复且有证据链；SQL 注入面 0；CSV 公式注入有防护 |
| 安全（凭据/暴露面） | ❌ 需加固 | JWT 默认密钥已公开；API 契约未认证可读 |
| 数据一致性 | ✅ 优秀 | 全套测试前后数据库快照逐项一致，零污染 |
| 可观测性 | ⚠️ 待修 | 404 被报成 500，污染 error 日志 |

**判定：可作为内部联调环境继续使用；上生产前必须完成 P0 + P1 共 5 项。**

---

## 二、实测数据（与项目记忆基线逐项比对）

### 2.1 验证套件

| 套件 | 基线 | 实测 | 结论 |
|---|---|---|---|
| Java 单测 `mvn -o test` | 52/52 | **52/52** | ✅ 一致（0 失败 0 跳过） |
| `verify_admin_api.py` | 46/46 | **46/46** | ✅ 一致 |
| `verify_attachment_api.py` | 47/47 | **47/47** | ✅ 一致 |
| `verify_idor_fix.py` | 15/15 | **15/15** | ✅ 一致，漏洞确认已阻断 |
| `frontend_admin_e2e.js` | 38/38 | **38/38** | ✅ 一致 |
| `frontend_attachment_e2e.js` | 56/56 | **56/56** | ✅ 一致 |
| `frontend_dashboard_e2e.js` | 44/44 | **44/44** | ✅ 一致 |
| `frontend_final_gaps_e2e.js` | 60/60 | **60/60** | ✅ 一致 |
| `frontend_business_gaps_e2e.js` | 79/79 | **79/79** | ✅ 一致（含审计留痕全链路） |
| **合计** | — | **437 条断言，0 失败** | ✅ |

> **统计口径陷阱**：`target/surefire-reports/*.txt` 对 `@Nested` 类会重复报告，把 txt 相加得到 79 条，而实际 `@Test` 只有 52 条。**核对测试数请数 `@Test` 注解或读 XML**，不要累加 txt，否则会误判"测试变多了"。

### 2.2 运行态与数据库基线

```
实测：document=46 attachment=1 notification=84 notify_unread=84 document_link=0
      document_type=3 flow_config=3 ACT_RE_PROCDEF=3
      ACT_RU_TASK=5 ACT_RU_EXECUTION=10 ACT_HI_PROCINST=32
      orphan_hi_inst=0 orphan_notify=0
```
与基线**逐项完全吻合**。`cleanup_demo_pollution.py --dry-run` 报告：死部署 / 孤儿实例 / 孤儿通知 **三项均无需清理**。

跑完 5 个 E2E 后再次复核：快照前后一致，`~/Downloads` 可见文件零新增（跑前=跑后），通知已读态精确还原（now=84 before=84）。

### 2.3 硬约束一致性

| 约束 | 核查方式 | 结果 |
|---|---|---|
| ① 页面在 jar 里 | 源 HTML / static 副本 / jar 内副本三处 SHA-256 | ✅ 三处同为 `d1b5622...` |
| ② 行级权限单一实现 | 全仓搜 `buildClause` 调用点 | ✅ 唯一实现，详情/附件/台账/风险/统计五入口全复用 |
| ④ 逻辑删除释放唯一键 | 接口级用例回归 | ✅ 通过 |
| ⑩ 测试文件入库 | `git ls-files` × 磁盘文件双向差集 | ⚠️ **3 个探针脚本未入库**（见 P2-2） |

---

## 三、问题清单

### P0 — 上线前必须修复（1 项）

**P0-1 · JWT 默认签名密钥硬编码，且已随公开仓库公开**

- 位置：`oa-boot/.../config/SecurityBeanConfig.java:33`、`oa-boot/src/main/resources/application.yml:88`
- 值：`haixiajin-oa-default-secret-please-change-in-production`（54 字节，恰好通过 `JwtUtils.java:29` 的 ≥32 字节校验，因此**不会报错、静默启用**）
- 放大因素：`启动联调版.command` **不设置** `OA_JWT_SECRET`（本地演示依赖该默认值）；仓库 `zzw8845/hxj-oa-new` 为 **public**
- 影响：任何知道该密钥的人，可离线伪造携带 `roleCodes/permCodes/dataScope`（含 ADMIN）的令牌，**完全绕过认证与授权**。密钥已永久留在公开仓库历史中
- 缓解事实：本次核实 JWT 使用 HS384 + `verifyWith`/`parseSignedClaims`，**不存在 alg=none 或算法混淆**；缺陷纯粹是密钥来源问题
- 修复方案：
  1. 移除代码中的默认值，改为「未配置 `OA_JWT_SECRET` 则启动失败」（fail-fast 而非 fail-open）
  2. 部署环境注入随机密钥（≥32 字节）
  3. 如需彻底清除公开历史痕迹，只能 rewrite history 或将仓库转为私有

---

### P1 — 上线前应修复（4 项）

**P1-1 · `/v3/api-docs` 未认证对外暴露完整 API 契约（70 KB）**

- 实测：`curl /v3/api-docs` → **HTTP 200，70,638 字节**，含全部端点、路径参数、DTO 结构；`/v3/api-docs/swagger-config` → 200
- 来源：`oa-boot/pom.xml:44-46` 显式引入 `springdoc-openapi-starter-webmvc-ui:2.3.0`；`WebConfig.java:36-44` 白名单放行 `/v3/api-docs/**`、`/doc.html`、`/swagger-ui/**`
- 影响：未认证方获得完整攻击面地图（可据此精确构造请求、发现未覆盖校验的参数）
- **关键耦合（修之前必须知道）**：前端 `oa.html:208` 与 `oa.html:974` **正用 `/v3/api-docs` 做连通性探测**（登录页「已连接/未连接」指示 + 自动重连）。直接把它从白名单删掉，会让页面**永远显示「未连接」**
- 修复方案：
  1. 后端新增轻量白名单端点 `GET /api/ping`，返回 `{"code":0,"data":"pong"}`
  2. 前端上述**两处**探测改打 `/api/ping`
  3. 将 `/v3/api-docs/**`、`/doc.html`、`/swagger-ui/**` 移出白名单（或加认证）
  4. 若确需保留文档，用 `springdoc.api-docs.enabled=false` 在生产 profile 关闭

**P1-2 · 任何不存在的路径都返回 HTTP 500（而非 404）**

- 实测：`/totally-missing` → **500**；`/api/nonexistent-xyz` → 500；响应体为 `{"code":500,"msg":"服务异常，请联系管理员"}`
- 根因：`GlobalExceptionHandler.java:68-73` 的 `@ExceptionHandler(Exception.class)` 兜底接住了 Spring 6.1 抛出的 `NoResourceFoundException`，并把它当「未处理异常」记 `log.error`
- 影响：
  1. **日志污染**：任何爬虫/扫描器请求不存在路径，都会在 error 日志刷出堆栈，真实故障被淹没
  2. **监控误报**：500 率虚高（健康仪表盘失去意义）
  3. 语义错误：客户端错误被报成服务端错误，调试时误导方向
- 修复方案：在 `GlobalExceptionHandler` 中显式接住 `NoResourceFoundException` / `NoHandlerFoundException` → 返回 404（且不记 error）；兜底分支保留给真正未预期异常

**P1-3 · 测试套件缺少「预期断言总数」自证机制 —— 上次假绿灯事故的根因仍在**

- 现状：5 个 E2E 脚本收尾一律只打印 `pass + '/' + results.length`，**没有任何脚本声明预期总数**，也没有"少跑了 N 条"的比对
- 为什么这是 P1：项目记忆里已明文立过规矩 —— *「断言数量下降要当事故查；脚本应在开头声明预期总数、收尾比对」*，并记录了本项目**已中招一次**：`frontend_business_gaps_e2e.js` 曾在被回退的 `/api/users/page` 上抛 TypeError 中断，导致其后 **16 条断言（含整条审计留痕链路）从未执行**，却被当成"85/85 通过"
- 本轮修复的是"恢复可跑"，**没有装设闸门**。只要再次出现「某批 check 被删/被注释/上游中断」，脚本照样打印 `63/63 通过`，无人能发现
- 修复方案（建议直接补到现有脚本，不新建）：
  1. 每个脚本顶部声明 `const EXPECTED_TOTAL = 79;`（用实测值固化）
  2. 收尾比对 `results.length === EXPECTED_TOTAL`，不等则**判失败并打印差值**
  3. 增加断言：脚本正常走完（未走进 `.catch`）—— 或在 `.catch` 中显式标记"未跑完"
  4. 主动删除或显式 skip 掉依赖未实现能力的断言，**不要留下注定失败的断言**（它会连带废掉其后所有断言）

**P1-4 · CORS 配置 `allowedOriginPatterns("*")` + `allowCredentials(true)` 注册到 `/**`**

- 位置：`WebConfig.java:77`（通配 origin）、`:81`（允许凭据）、`:85`（注册到全部路径）
- 缓解事实：接口用 **Bearer token 而非 Cookie**，`allowCredentials` 对无 Cookie 的调用不产生直接的凭据自动携带风险；且源码注释已明确标注"生产请把 allowedOriginPatterns 收敛为白名单"
- 影响：属于演示期有意妥协，但**上线前必须收敛**；否则任意站点可读取 API 响应内容
- 修复方案：改为显式白名单（部署域名 + 本地联调地址），`allowCredentials` 视是否改用 Cookie 决定

---

### P2 — 建议修复（7 项）

**P2-1 · 异常消息直返前端，可能泄漏文件系统路径**
`GlobalExceptionHandler.java:43-47` 把 `IllegalArgumentException.getMessage()` 原样返回；`LocalStorageService.java:61/75`、`AttachmentService.java:114` 的异常消息含存储路径。
✅ 同时核实：兜底分支 `:70-72` 为通用文案，未设置 `include-stacktrace`（Boot 默认 never），**不存在堆栈泄漏**。
建议：对外统一返回通用文案，详细信息仅记日志。

**P2-2 · ~~3 个 E2E 探针脚本从未入库~~ → 经复核为误报，实际早已入库**

> ⚠️ **本条初判有误，保留在此用于对照。** 三个探针脚本**均已入库**，且自 `ab2a4a5` 基线提交起就在版本控制内：

| 文件 | 实际状态 |
|---|---|
| `oa-backend/scripts/e2e/frontend_file_mode_test.js` | ✅ 已入库（`ab2a4a5`） |
| `oa-backend/scripts/e2e/frontend_reconnect_test.js` | ✅ 已入库（`ab2a4a5`） |
| `oa-backend/scripts/e2e/frontend_same_origin_test.js` | ✅ 已入库（`ab2a4a5`） |

**误报根因**：构造「已入库」集合时用的正则是
`grep -E "Test\.java$|\.py$|_e2e\.js$|_hygiene\.js$|\.command$|\.html$|\.md$"` ——
**漏掉了 `_test.js` 后缀**；而磁盘侧用的是 `find -name "*.js"`（收录了它们）。两个集合口径不一致，
差集就把已入库文件误判成"从未入库"。
**流程教训**：审查里任何"文件缺失/从未入库"的结论，必须换第二种方式复算一次 ——
本例中 `git status` 立刻暴露矛盾（它们显示为 `M`，即已被跟踪）。

**P2-3 · 本地领先远端 1 个提交，交接说明已漂移**
`git log origin/main..HEAD` → `2f14d09 docs: 新增项目交接说明 + 忽略演示库快照` 未推送；而 `项目交接说明.md` 第六节写的是「HEAD：`d19193f`（本地与远端 main 一致）」。接手者会误判同步状态。
建议：推送该提交，并把交接说明的 HEAD 改为「由 `git log -1` 现取」或加一句"以 git 实际状态为准"。

**P2-4 · 认证侧缺少防爆破与登出失效**
`AuthService.java:43-53` 无登录失败限速/账号锁定；`AuthController.java:58-62` logout 为空实现（无黑名单），token 在 12 小时有效期内持续可用（`application.yml:89`）。
建议：加失败计数（按账号+IP 滑窗），或引入 token 版本号实现登出即失效。

**P2-5 · 附件仅做扩展名白名单，无真实类型（魔数）校验**
`AttachmentService.java:73-81` 只比对 `application.yml:103` 的扩展名清单。
缓解事实：下载强制 `application/octet-stream` + `X-Content-Type-Options: nosniff`，预览仅限图片/PDF，存储目录在 web 根之外 → 实际不可执行、不可直接访问。
建议：补魔数校验（如 `Files.probeContentType` 或 Tika），纵深防御。

**P2-6 · 免登录白名单含 4 个并不存在的端点**
实测：`/actuator/health` → 500、`/doc.html` → 500、`/swagger-ui.html` → 302（均因未引入 actuator、静态映射已关）。**攻击面清单与实际不符**，且这些 500 会持续污染日志（与 P1-2 同源）。
建议：白名单只保留真实存在的端点 + 新增的 `/api/ping`。

**P2-7 · 跨公司读未校验 `companyId`（当前单公司，风险低）**
`UserController.java:41/47/52`、`RoleController.java:33/39`、`DepartmentController.java:32/38`、`FlowConfigController.java:49/54/66/79/98/108`、`FormTemplateController.java:26/34/40` 接受 `companyId` 参数但未校验归属；`UserAdminService.java:94-101` 无归属过滤。
实测：`company=1`、`sys_user` 中 `distinct(company_id)=1` → **当前单公司部署，无实际越权影响**。但一旦多租户化即为横向越权。
建议：在 `DataScopeHelper` 同一层加入公司维度过滤（**不要**在各 Controller 各写一份）。

---

### 技术债与文档（3 项，可择机处理）

**T-1 · 前端存在「第二份模板」：挂载前 DOM 补丁（`oa.html:24-145`）**
逻辑层之外，UI 结构还由 JS 在 Vue mount 前用 `insertAdjacentHTML`/`innerHTML` 二次拼装（工作台快捷区、列表筛选器、提交表单字段、用印文案改写等）。
这是「原型文件不动、只替换逻辑层」约束下的**有意设计**，但代价是：改 UI 必须**同时**搜 HTML 模板与补丁段，否则出现「代码看着对、界面就是不工作」。
建议：在文件头补一份"补丁影响清单"注释（人员弹窗 / 角色面板 / 提交表单 / 工作台），降低后续踩坑概率。

**T-2 · Flowable `database-schema-update=true`**
`application.yml:75`。开发期方便，生产应改为 `false` + 显式 DDL 迁移；另有 `BpmnGenerator.java:176-182` 将配置条件串包成 `${...}` 写入 BPMN，构成 UEL 表达式注入面（受 `system:flow` 权限约束，仅高权限用户可达）。

**T-3 · `MEMORY.md` 已超出注入上限被截断（19,897 字节）**
导致本会话加载的记忆不完整。本次审查已将其压缩重写（详见变更记录）。

---

## 四、做得好的部分（建议保持）

1. **IDOR 修复的质量高于一般项目**：不只修了漏洞，而是把「详情可见 ⟺ 出现在列表里」做成了**构造性事实** —— 详情/附件判定改为把 `DataScopeHelper.buildClause()` 的片段套回本单据做一次主键 count，从结构上消除"两处语义漂移"的可能。并配 15 条证据链断言（列表是否含它 / 详情业务码 / 附件业务码三份证据必须自洽），且用**判据验证**证伪过一次（还原漏洞版本 → 用例变红）。
2. **数据范围 SQL 的注入防护到位**：`DataScopeHelper.java:57/64/67/72` 拼接值全为 `Long`；用户可控的 `deptPath` 在 `:92` 经 `PATH_SAFE = ^/[0-9/]*$` 白名单校验后才用于 LIKE。
3. **全仓 SQL 注入面为 0**：无 mapper XML、无 `${}` 字符串替换，`@Select` 全用 `#{}`；`orderBy`/`last()` 全为常量；`TodoService.java:115` 的 LIMIT 经 `min(limit,200)` 夹紧。
4. **CSV 公式注入防护**（`= + - @ \t \r` 前缀前置单引号）：这是台账导出最容易被忽略的 OWASP 项，本项目有 15 条单测覆盖公式前缀 + 结构转义 + 表头 9 列契约。
5. **审计留痕是真实落库的**：`AuditAspect.java:57-106` + `AuditLogService`（`REQUIRES_NEW` 独立事务、失败吞异常不影响主流程），并有 UI 级全链路断言与"读审计需 `system:audit`"的 403 断言。审计不记录请求体与明文密码。
6. **E2E 的数据卫生做得罕见地扎实**：共用 `_hygiene.js`（避免各脚本抄出漂移版本）；用**绝对不变量断言**补足 before/after 对比的盲区（"基线本身是脏的"时对比照样一致）；下载目录强制重定向到临时目录并有「`~/Downloads` 可见文件零新增」硬断言；改状态的用例（点"全部已读"）用快照精确还原。
7. **前端启动看门狗**（`oa.html:1896-1908`）：Vue 未完成挂载时给出可见提示而非白屏。

---

## 五、建议修复顺序

| 顺序 | 项 | 理由 |
|---|---|---|
| 1 | P1-3 测试预期总数自证 | 成本最低、收益最高：装一道闸门，防止后续所有改动被"假绿灯"掩盖 |
| 2 | P2-3 推送提交 | 一次 `push` 让本地领先的提交与远端同步（P2-2 经复核系误报，无需补入库） |
| 3 | P1-1 关闭 API 契约暴露（含前端探测改 `/api/ping`） | 涉及前后端联动，需一次改全 |
| 4 | P1-2 404 语义修正 + P2-6 白名单清理 | 同一处改动，一起做 |
| 5 | P0-1 JWT 密钥 fail-fast 化 | 上线前的硬门槛 |
| 6 | P1-4 CORS 收敛 | 与部署环境确定后一起做 |
| 其余 P2 | 防爆破 / 魔数校验 / 公司维度过滤 | 可以择机 |

---

## 六、未发现问题的项（已核查且干净）

- 未认证访问受保护接口：`/api/documents`、`/api/users`、`/api/attachments` 均返回 **401** ✅
- 附件路径穿越（`LocalStorageService.java:112-116` 的 `startsWith(root)` 校验）、响应头注入（`URLEncoder` + `safeName` 滤 CR/LF）、MIME 嗅探、web 根直访 ✅
- 可见性判断的"第二处实现"：已确认全仓唯一 ✅
- 审计切面落库、读审计鉴权 ✅
- 日志未打印 token/密码（全仓仅 `AuthInterceptor.java:46` debug 打异常 message）✅
- 跨环境配置漂移（`target/classes/application.yml` 与 `src` 一致）✅
- `.gitignore` 恰当：`run/`、`*.jar`、`data/attachments/`、`demo_snapshot_*.sql`（含真实数据与审计日志）均已排除 ✅

---

## 七、修复进展（2026-09-21 第二轮：已动手修复）

上面列出的问题中，**除 2 项有意推迟外，全部已修复并逐项实测**。

### 7.1 已修复（9 项）

| 编号 | 问题 | 修法 | 验证证据 |
|---|---|---|---|
| **P0-1** | JWT 默认密钥硬编码且已公开 | 删掉默认值，`SecurityBeanConfig` 改为**未配置即启动失败**；`application.yml` 的 `${OA_JWT_SECRET:...}` 改为空默认；`启动联调版.command` 首次运行自动生成 `oa-backend/.jwt_secret`（0600，已入 `.gitignore`）并复用 | 不带该变量启动 → **退出码 1**，日志明确输出「未配置 JWT 签名密钥，服务拒绝启动」；带脚本启动 → 密钥文件 64B/0600，服务正常 |
| **P1-1** | `/v3/api-docs` 未认证暴露 70KB 契约 | 新增免登录轻量端点 `/api/ping`；前端**两处**探测（`oa.html:208`、`:974`）改为打它；`springdoc.api-docs/swagger-ui.enabled=false` | `/v3/api-docs`、`/v3/api-docs/swagger-config`、`/doc.html`、`/swagger-ui.html` 全部 **404**（此前 200/70KB）；`/api/ping` → `{"code":0,...,"data":"pong"}` |
| **P1-2** | 不存在路径返回 500 而非 404 | `GlobalExceptionHandler` 显式接住 `NoResourceFoundException`/`NoHandlerFoundException` → 404，且只记 `debug`（不记 error） | `/totally-missing`、`/api/nonexistent-xyz`、`/actuator/health` 全部 **404**，响应体「请求的资源不存在」 |
| **P1-3** | E2E 无预期断言总数自证 | 5 个套件各加 `EXPECTED_TOTAL`（38/56/44/60/79）+ 收尾比对，条数不符即判失败并打印差值 | 全套跑完每行都带「（预期 N 条）」；**证伪一次**：把 admin 改成 999 → 输出「❌ 断言条数异常：实际 38 条，预期 999 条」且 **EXIT=1** |
| **P1-4** | CORS `*` + `allowCredentials(true)` | 默认只放行 `127.0.0.1/localhost/::1` 任意端口；`allowCredentials` 关闭（接口走 Bearer，无 Cookie 可带）；保留 `OA_CORS_PERMISSIVE=true` 逃生舱供 file:// 联调 | `Origin: http://evil.example.com` → **403 且无 ACAO**；`Origin: http://127.0.0.1:5180` → 200 + 回显 ACAO |
| **P2-1** | 异常消息泄漏文件系统路径 | `LocalStorageService` 保存/读取失败改为通用文案，细节（含绝对路径）只进日志 | 编译通过，附件相关 47 项用例全绿 |
| **P2-2** | ~~3 个探针脚本未入库~~ **经复核系误报**，实际早已入库 | 无需补入库；仍按新探测端点修正了内容（`api-docs` → `/api/ping`）并补充前置条件说明 | `git log -- <path>` 显示三个文件均由 `ab2a4a5` 收录 |
| **P2-4** | 无登录失败限速 | `AuthService` 按账号内存计数，5 分钟内失败 ≥10 次 → 429；成功即清零 | 用不存在账号连打 12 次：前 10 次 `401`，第 11 次起 **`429`** |
| **P2-6** | 白名单含 4 个不存在的端点 | 白名单收敛为 `/api/auth/login`、`/api/ping`、`/error` | 见 P1-1/P1-2 的 404 实测 |

> **顺带修掉一个连带的坑**：`启动联调版.command` 的健康检查原本 `grep '"openapi"' /v3/api-docs`。
> 关闭该端点后，健康检查会永远失败 → 守护进程会**每 5 秒无限重启后端**。已同步改为检查 `/api/ping` 的 `"code":0`。

### 7.2 复测结果（改动后全量）

| 套件 | 实测 | 退出码 |
|---|---|---|
| Java 单测 `mvn -o test` | **52/52** | 0 |
| `verify_admin_api` / `verify_attachment_api` / `verify_idor_fix` | **46 / 47 / 15** | 0 |
| `frontend_admin` / `attachment` / `dashboard` / `final_gaps` / `business_gaps` | **38 / 56 / 44 / 60 / 79** | 全部 0 |

数据库快照跑前跑后一致（`document=46 …orphan=0`），`cleanup_demo_pollution --dry-run` 三项「无需清理」，
`~/Downloads` 可见文件零新增。

### 7.3 有意推迟（2 项，均已记录在待办）

- **登出后 token 仍有效**：需要引入 token 版本号或黑名单（有状态、涉及表结构），单独一批做更安全。
- **附件魔数校验**：属纵深防御（当前已有 `octet-stream` + `nosniff` + 预览白名单 + web 根外存储四层缓解），
  而白名单含 `txt/csv` 等无魔数类型，误杀风险需要专门设计，不宜与本次混做。
