# 冷启动操作清单 与 200 次验证方案

> 配套文件：`oa-backend/sql/minimal_seed.sql`（已实测可用）
> 相关报告：`零数据冷启动体检-2026-09-23.md`（两个阻断性 Bug 的证据与根因）

---

## 一、先回答：是不是「只有一个 admin，其他全部新建」

**是。** 但"只有一个 admin"这句话要精确成 **7 张表有初始数据、其余全空**：

| 表 | 初始条数 | 说明 |
|---|---|---|
| `company` | 1 | admin 必须挂在某个 company_id 下 |
| `sys_user` | **1** | 只有 admin |
| `sys_role` | 1 | ADMIN 角色 |
| `sys_permission` | 25 | 权限点目录 |
| `role_permission` | 25 | ADMIN × 全部权限点 |
| `role_data_scope` | 1 | **最易漏，见坑①** |
| `user_role` | 1 | admin → ADMIN |
| `department` `post` `sys_dict` | 0 | 全部界面新建 |
| `document_type` `form_template` `flow_config` | 0 | 全部界面新建 |
| `document` `notification` `audit_log` | 0 | 运行时产生 |

### 为什么这 7 张表一张都不能少

- **权限点不能省**：admin 的超管权限**不是硬编码的**。`PermInterceptor` 只校验 JWT 里的 `permCodes`，
  而它来自 `role_permission × sys_permission`。只建用户不建权限点 → admin 能登录，但每个接口都 403。
  （实测：灌全这 7 张表后 `permCodes = 25 项`，全接口正常。）
- **数据范围不能省**：`role_data_scope` 缺失时，ADMIN 的数据范围会**静默降级为 `self`（本人单据）**。
  admin 将看不到别人提交的单据、看板只统计自己 —— 表现是"功能都能点，但数据对不上"，很难排查。

---

## 二、必须自建的内容（按顺序）

实测：**除第 0 步外全部可在界面上完成**。

| 步骤 | 建什么 | 在哪建 | 实测结果 |
|---|---|---|---|
| 0 | 库结构 + admin | SQL（`minimal_seed.sql`） | 唯一需要 SQL 的环节 |
| 1 | **部门树 + 每部门指定负责人** | 主数据管理 | ✅ 物化路径 `/1/` 服务端自动算 |
| 2 | 岗位 | 主数据管理 | ✅ |
| 3 | 角色（名称/部门/岗位/数据范围/权限点） | 角色管理 | ✅ 权限点带中文名回显 |
| 4 | 人员账号（挂部门+岗位+角色） | 用户管理 | ✅ `postName` 服务端自动映射 `post_id` |
| 5 | 字典 | 字典管理 | ⚠ 空库返回 `{}`，界面不报错，但部分业务下拉会是空的 |
| 6 | 表单模板（可视化设计器出 schema） | 表单模板 | ✅ |
| 7 | 流程（节点 + 条件分支 + 每节点「谁审」） | 流程管理 | ✅ 节点名建议从下拉里选（见下方「节点名怎么填」） |
| 8 | 单据类型（绑定表单模板 + 流程） | 单据类型管理 | ✅ 三层配置的枢纽 |
| 9 | 提单 → 审批 → 办结 | 工作台 | ⚠ **必须先修两个 Bug，见坑②③** |

> 第 1 步的「**每个部门都要设负责人**」是整条链路最容易漏、后果最严重的一步。
> 建议写进部署文档的强制检查项。

---

## 三、节点名怎么填（实测澄清）

节点这块分三层，搞清楚就不会配错。

**第 1 层 · 流程结构 = 纯数据。** 有几个节点、什么顺序、怎么分支，都存在 `flow_config_node` 表里，
界面自由增删排序；BPMN 是运行时按这些数据生成的，不是预先写死的 XML。

**第 2 层 · 节点名的语义 = 代码里的模板库（三档解析）。**

| 档 | 命中条件 | 结果 | 实测 |
|---|---|---|---|
| ① 精确匹配 | 名字在 15 个模板里 | 自动配好审批人 | `出纳付款` → `role{CASHIER}` |
| ② 关键词兜底 | 名字含 13 个关键词之一 | 自动配好审批人 | `会计复核` → `dept_role{ACCOUNTANT}` |
| ③ 都不命中 | 完全自创的名字 | **留空，需手动补配** | `王经理复核` → 该节点无规则 |

关键词 13 个：回传 / 办理 / 盖章 / 付款 / 出纳 / 发起 / 财务总监 / 会计 / 内控 / 领导 / 总经 / 负责人 / 章

**第 3 层 · 节点类型 = 代码常量。** 审批 / 抄送 / 条件分支 / 办理 / 发起 五种写死在代码里。
（注意：抄送有类型定义但模板库里没有对应模板，界面上配不出抄送节点。）

**实操建议**：

- 优先从下拉里选 —— 跨过①②档，零配置
- 用自己部门的叫法也行（"会计复核""财务经理审批"），只要名字里含上面那些关键词
- 真要完全自创（如"王经理复核"），**必须做两件事之一**：
  1. 建完流程后，去「节点指派」界面给该节点配上审批人；
  2. 或直接调接口时带上 `ruleType` / `ruleValue`（结构化形态支持）。
- **不补配的后果**：提单后该节点被系统自动跳过（`action=auto_skip`、备注"无匹配审批人，系统自动通过"），
  前端把它渲染成"已完成"，**不会提示你审批环节消失了** —— 这是最需要客户知道的一条。

> 想新增模板名（比如"安全环保部会签"）目前**只能改代码**（`FlowNodeTemplate` 里的模板列表）重新发版，
> 界面上加不了。如果客户的流程术语很特殊，这一点要提前评估。

---

## 四、三个必踩的坑（都是实测踩出来的）

### 坑① admin 的数据范围默认是「本人单据」

新建库只灌 `company/user/role/permission/user_role` 时，`GET /api/auth/me` 返回 `dataScope: "SELF"`。
必须显式设置（界面：角色管理 → 数据范围 → 全公司）：

```
PUT /api/roles/1/data-scope   {"scopeType":"company","scopeDeptIds":[]}   → dataScope 变 COMPANY ✅
```

⚠ 附带发现：这个接口**不校验枚举值**，传 `all` / `xyz` 都返回
`HTTP 200 {"code":0,"msg":"数据范围已保存"}`，但实际静默降级为 `SELF`。
合法值只有 5 个：`self` / `dept` / `center` / `custom_dept` / `company`（全小写）。
界面上是下拉选择所以不会传错，但**对接方直接调接口时容易中招**。

### 坑② 部门负责人不设 / 设成停用的人 → 单据永久卡死

`initiator_leader` 规则不校验负责人是否可用（详见体检报告 Bug①）。
实测：`leader_id=8`（已停用）→ 任务挂在停用账号上；`leader_id=0`（未设置）→ 指派给 id=0 的幽灵用户。
两种都会**静默卡死、零提示**，且 admin 待办里看不到。

### 坑③ 申请人 = 审批人 → 单据死在「审批中」

全节点被跳过时 `STARTED` 事件覆盖 `FINISHED`（详见体检报告 Bug②）。
单据状态停在"审批中"，但流程实例已结束、待办为零 —— 幽灵单。

> 在"只有一个 admin"的场景下坑②③是 100% 必踩：
> 首个审批节点是「直属部门负责人」，此时要么没设负责人（坑②），要么负责人就是 admin 自己（坑③）。

---

## 五、200 次验证怎么设计

### 前提

**先修坑②③**，否则任何需要审批的单据都会在第一个节点卡死，200 次跑不出有效结论。
坑① 通过 `minimal_seed.sql` 已规避。

### 第 1 轮：搭骨架（1 套，不计入 200）

建 1 公司 → 3~4 部门（**全部设负责人**）→ 3 岗位 → 3 角色 → 5~8 账号
→ 1 表单模板 → 1 流程（含 1 个条件分支）→ 1 单据类型 → 走通 提单→审批→办结。

### 第 2~N 轮：换变量（200 次拆成 5 组，每组 40）

| 组 | 变量轴 | 每次的断言重点 |
|---|---|---|
| 1 (40) | **金额档位 × 条件分支**：门槛下 / 门槛上 / 边界值（等号）/ 0 / 超额 | 分叉是否走对分支、`current_node_key` 是否为预期节点 |
| 2 (40) | **审批动作**：通过 / 驳回发起人 / 驳回到指定节点 / 撤回后重提 / 重复提交 | `document.status`(3/4/5)、节点记录 `action`、能否二次提交 |
| 3 (40) | **审批人形态**：单人 / 多人候选 / 会签 / 加签 / 委托代审 | `ACT_RU_TASK.ASSIGNEE_` 归属、候选人 `ACT_RU_IDENTITYLINK`、待办条数 |
| 4 (40) | **数据可见性**：不同部门 × 不同角色（self/dept/company） | 各账号看到的单据条数是否符合范围；越权应 403 |
| 5 (40) | **边界与异常**：审批人停用 / 删除 / 未设负责人 / 部门为空 / 岗位被删 | 应给出明确提示，**不允许静默卡死**（这组最能暴露坑②这类问题） |

### 每条的固定断言清单

```
document.status          → 期望值（0草稿/1待审/2审批中/3已通过/4驳回/5撤回/6归档）
document.current_node_key→ 期望节点
ACT_RU_TASK 中该单据的任务数 + ASSIGNEE_ 归属
申请人/审批人各自「待办」「已办」里的条数
notification 条数 + 接收人
audit_log 是否留下记录
```

### 收尾纪律（这几条踩过坑，必须遵守）

- 条数断言**只能实测后写死**，不能数源码推算（改一处会崩一片）。
- 不依赖起始状态、行序、排序位置，一律按**业务主键**（单据编号）定位。
- 动了界面状态的用例必须**自己收尾**；清理只允许「按创建时记下的 id 删」，
  **禁止按模式/范围/整表删**（历史上删过业务数据）。
- 验证状态推进必须自建夹具 + 物理清理（含 `ACT_*`，按 `BUSINESS_KEY_=单据编号` 反查）。

---

## 六、`minimal_seed.sql` 用法

```bash
# 1. 建库（仅结构，70 张表）
mysql -uroot < oa-backend/sql/init_database.sql

# 2. 灌最小初始数据（只留 admin）
mysql -uroot <库名> < oa-backend/sql/minimal_seed.sql

# 3. 启动后登录：admin / 123456（首次登录请立即改密）
```

灌完的核对基线：

```
company=1  sys_user=1  sys_role=1  sys_permission=25  role_permission=25
role_data_scope=1(type=company)  user_role=1
department=0  post=0  sys_dict=0  document_type=0  form_template=0  flow_config=0
```

**实测确认（已跑通）**：登录 ✅ / `dataScope=COMPANY` ✅ / 权限点 25 ✅ /
建部门自动算物化路径 ✅ / 建岗位 ✅ / 建角色 ✅ / 建员工自动映射岗位 ✅ /
新建账号可登录 ✅ / 空字典与空单据类型返回 `{}`、`[]` 不报错 ✅
