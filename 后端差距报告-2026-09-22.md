# 后端 vs 原型：能力差距报告（2026-09-22）

> 口径：**只看后端**。前端在本项目里的定位是"可视化 + 验证后端业务"，不是交付物。
> 每条结论都附证据；**判定方式以读代码/数据模型为准**，不用关键词搜索（理由见文末）。

---

## 一、结论总览

| 原型阶段 | 承诺范围 | 后端实际 | 判定 |
|---|---|---|---|
| **P0** 基座 | 用户权限（登录/角色/部门/数据范围）、流程引擎最小可用、单据提交与基础审批、附件上传下载 | 全部具备（75 个接口 / 28 个实体） | ✅ 完整 |
| **P1** 闭环 | 状态机完整化、驳回重提、台账档案与导出、看板统计、通知（站内信）、审计日志 | 全部具备。状态机 7 个动作齐全：`approve / reject / countersign / supplement / withdraw / edit / resubmit` | ✅ 完整 |
| **P2** 高级审批 | 条件分支、加签、补材料、超时与升级、批量审批、风险预警 | 条件分支（`TYPE_GATEWAY`）、加签、补材料、风险预警 ✅；**委托、超时升级、批量审批 ❌** | ⚠️ 4/7 |
| **P3** 用印与多公司 | 用印申请专项、印章台账、归还闭环、多公司隔离、流程版本治理 | 多公司隔离 ✅、流程版本治理 ✅、用印申请本身可用（作为单据类型走通用流程）✅；**印章台账与归还闭环本次补齐** | ✅（本次补齐） |
| **P4** 集成与移动 | 财务对接、银行回单、HR 同步、移动端审批、推送 | 未做 | ❌（预期之外） |

## 二、本次已补齐

### 用印台账与归还闭环（P3）

**补齐前的状态**：`seal_apply`、`seal_record` 两张表、两个实体、两个 Mapper 全都在，
但**没有任何 service / controller / 接口**，两张表 **0 行数据** —— 原型 P3 承诺的
「用印申请专项 / 印章台账 / 归还闭环」只有壳。

**补齐后**：新增 `SealService` + `SealController`（4 个接口）：

| 接口 | 作用 |
|---|---|
| `GET /api/seals` | 用印台账分页（按用章类型 / 归还状态 / 关键字 / 创建日期区间筛选） |
| `GET /api/seals/by-document/{id}` | 某单据的用印状态 + 动作台账（未登记时返回"待用印"视图而不是 404） |
| `POST /api/seals/use` | 登记用印：待用印 → 已用印 |
| `POST /api/seals/return` | 归还：已用印 → 已归还 |

写进代码的判断（每条都注明了理由）：
- **只有 SEAL 类单据能登记用印** —— 否则一张付款单也能写出一条用印记录，而台账是给审计看的；
- **草稿 / 已驳回 / 已撤回不许用印** —— 没走完审批就盖章是内控红线（已归档允许，归还常在归档后）；
- **状态流转用带条件的 UPDATE**（`WHERE return_status = 期望值`），影响行数为 0 即报"被他人变更" ——
  "先查再改"在并发下会让两个请求都成功，台账出现两条 use；
- **每次动作写 seal_record**（谁、何时、做什么、备注）—— "可查"的价值就在这条；
- 权限用**已存在**的 `document:approve:seal`（用印办理），不新设权限点。

**验证**：`scripts/verify_seal_api.py` **30 项断言全绿**（含权限 403、三种负例、
完整闭环、重复动作被拒、台账筛选、审计留痕、收尾物理清理），全量 E2E 287/287。

## 三、后端仍缺的能力（按建议优先级）

| # | 缺口 | 证据 | 影响 | 复杂度 |
|---|---|---|---|---|
| 1 | **表单模板维护接口** | `/api/forms/*` 只有 `schema`(读)、`templates`(读)、`validate`(校验)，**无 POST/PUT/DELETE**；`form_template` 3 行、`form_field_permission` 4 行只能改库 | 原型 §9 把「表单模板驱动」列为**关键架构决策**，现在模板无法通过接口维护 ⇒ 动态表单落不了地 | 中 |
| 2 | **委托（代理审批）** | 28 个实体中**无委托/代理实体**，无相关接口 | 原型 §7.2 approval-service 承诺「委托」；主管出差时无法授权他人代办 | 中 |
| 3 | **超时与升级** | 只有 `GET /api/risks` **展示**临期/超期节点，**没有任何升级动作**（无升级实体、无定时任务） | 原型 P2 承诺「超时与升级」；现在超时只是个提醒，不会真正升级到上级 | 中 |
| 4 | **批量审批** | 无批量接口（`/api/todos/approve` 一次一条） | 原型 P2 承诺「批量审批」 | 低 |
| 5 | **P4 集成**（财务/银行回单/HR 同步/推送） | 未做 | 明确分期在 P4，需确认是否在范围 | 高 |

## 四、判定方法（踩坑记录，避免后来者重犯）

本次一开始用**关键词 grep** 判"缺什么"，得出过**三个错误结论**：
- 「驳回重提缺失」→ 实际 `availableActions` 里有 `resubmit`；
- 「关联单据缺失」→ 实际有 `DocumentLink` 实体 + Mapper + VO + 详情装配；
- 「抄送缺失」→ 实际是流程节点类型 `TYPE_CC`（`BpmnGenerator` 里处理成不阻塞的 serviceTask）。

**结论：判定后端能力只能读代码/数据模型（实体、状态机、接口清单），
关键词搜索会给出方向相反的印象。** 本报告的每一条都按这个口径复核过。
