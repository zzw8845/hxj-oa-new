# 系统评估与审查 —— 海峡金 OA（2026-09-23 实测）

> **评估方式**：规模与构成实测（`git` / `wc` / 注解计数）+ **实跑接口回归全套件** + 验证资产的**结构性审查**。
> 与以往的清单式核查不同，本报告给**定级与判断**，并逐条标明「实测」还是「引用旧结论」。
> 配套：`修复与交付待办-2026-09-23.md`（执行用）；本报告是**评估用**。

---

## 一、总评

**这是一套「工程骨架相当扎实、代码卫生良好，但验证成色被高估、交付侧有真实硬伤」的系统。**
问题不在"写得好不好"，在**"你以为它被验证过了，其实有一部分从来没被真正验证"**。

| 维度 | 定级 | 依据（实测） |
|---|---|---|
| 架构与模块划分 | **A-** | 5 模块分层清晰（common/system/flow/document/boot），16,728 行 / 197 文件，规模与功能匹配，无臃肿 |
| 技术选型 | **A** | JDK 17 / Spring Boot 3.2.5 / Flowable 7.0.0 / MyBatis-Plus 3.5.9 / MySQL 8.3.0 / jjwt 0.12.5 —— 全部当代版本，**无 EOL 依赖** |
| 功能完整度 | **B+** | 98 端点 / 19 Controller，审批主干闭环完整；表单引擎是"半动态"（4 类缺口） |
| 代码卫生 | **A-** | `TODO` 仅 4 处，`FIXME`/`HACK`/`XXX` **零**，零残留标记 |
| **验证资产** | **B-（关键项）** | 631 条断言 / 25 个脚本，**设计理念属同类上游**；但存在**结构性假绿灯**（见第四节） |
| **交付就绪** | **C** | 3 处交付硬伤 + 4 个冷启动阻断 bug，脚本**不能直接交给客户** |
| 安全与合规 | **C+** | 鉴权 / 行级权限 / 限流 / fail-fast 齐备；但 SoD、分级管理员、弱口令敞口未解 |

**一句话结论**：
**代码质量可以交付；验证可信度不能交付；交付脚本不能直接交给实施的人。**

---

## 二、规模与构成（全部实测）

### 后端

| 模块 | 文件 | 行数 | 占比 | 职责 |
|---|---|---|---|---|
| oa-document | 77 | **6,903** | 41.3% | 单据、表单、附件、用印、台账 |
| oa-system | 63 | 4,590 | 27.4% | 用户/角色/权限/部门/字典/审计 |
| oa-flow | 33 | 3,682 | 22.0% | Flowable 封装、指派解析、BPMN 生成 |
| oa-common | 15 | 984 | 5.9% | 工具、上下文、行级权限 |
| oa-boot | 9 | 569 | 3.4% | 启动、安全、配置 |
| **合计** | **197** | **16,728** | 100% | |

- **HTTP 端点 98 个**，分布在 **19 个 Controller**
- 数据库 **70 张表**（29 业务 + 41 Flowable `ACT_*`/`FLW_*`）

### 前端

- 单文件 `海峡金OA审批系统-联调版.html`：**3,172 行 / 288 KB**（Vue3 + Element Plus）
- 组件规模：40 个菜单项 / 42 个对话框 / 22 个 tab 面板 / 约 20 张表格

### 文档与版本

- 原型分析 604 行 + 审查/交付报告 6 份（含本报告）
- **Git：41 次提交，时间跨度 3 天（09-21 → 09-23）**
  ⚠ 版本追溯起点很短 —— 09-21 之前的演进过程没有进 git，早期决策无法回溯

---

## 三、做得好的地方（具体到证据，不空夸）

1. **指派降级链设计是行业标准做法**：三层（组织解析 → 规则降级 → 运行期兜底），
   且有配套的预置节点模板与界面入口。这套设计不是拍脑袋，是认真想过的。
2. **验证体系的设计理念属同类项目上游** —— 三处体现：
   - `run_all_verify.sh`：**随机顺序**跑 + 前后基线比对。脚本注释里写明这是为了暴露
     "固定顺序掩盖的互相干扰"，并附了真实事故（某用例 `DELETE FROM seal_apply` 整表清空，
     固定顺序时恰好排在后面所以一直没被发现）
   - `run_all_e2e.sh`：**三重条数核对**（脚本实跑数 / 套件声明数 / 总入口预期数，三者必须一致）
     + 「没有打印自证结论就不能算通过」+ 残渣**主动报告而非静默清理**
   - 前置自检：HTML 模板配平、shell 变量 i18n（都是踩过坑后补的）
3. **代码卫生好**：`TODO` 仅 4 处，`FIXME`/`HACK`/`XXX` 零。
4. **清理纪律有效**（本次实跑验证）：跑前跑后演示库基线**完全一致**
   （`document=46 user=9 post=8 dept=8 template=5 seal_apply=9 delegation=0 escalation=0 act_task=5 notify=84`）
5. **安全基线正确**：swagger 默认关、JWT 密钥 fail-fast、行级权限单一实现、Redis 故障降级。

---

## 四、核心问题：验证成色被高估（本次最重发现）

### 4.1 【严重】两个套件在结构上**永远不会红**

`verify_admin_api.py`（46 条）与 `verify_attachment_api.py`（47 条）—— 这两个文件通篇
**没有任何 `sys.exit` / `SystemExit` / `raise`**：

```
$ grep -nE "sys.exit|SystemExit|raise " scripts/verify_admin_api.py scripts/verify_attachment_api.py
（无输出）
```

- 它们只**打印**失败清单，**从不设置退出码**
- 而 `run_all_verify.sh` 的判定标准就是"**退出码非 0**"
- ⇒ **这 93 条断言（占接口侧 299 条的 31%）无论实际对错，永远显示 ✓**

**本次实跑的现场证据**：

```
✓ verify_admin_api        结果：通过 45 项，失败 1 项     ← 有失败，却判绿
✓ verify_attachment_api   结果：通过 46 项，失败 1 项     ← 同上
```

对比 `run_all_e2e.sh` 的判定（靠三重条数核对）—— **接口侧的总入口判定明显弱于 E2E 侧**。

**修法（几行）**：两个脚本末尾补 `raise SystemExit(1 if FAIL else 0)`，并补 `EXPECTED_TOTAL` 闸门。

### 4.2 【中】有套件没挂进总入口

`verify_flow_assignee_admin.py`（18 条，**有** `EXPECTED_TOTAL` 闸门）
**不在 `run_all_verify.sh` 的用例清单里** ⇒ 标准回归**永远不跑它**。
而"节点指派"是整个审批流最核心的功能之一。**修法**：挂进去。

### 4.3 本次实跑结果：13 个套件 —— **10 绿 / 2 绿中有败 / 3 红**

| 结果 | 套件 |
|---|---|
| ✓ 全绿 | doc_type_admin(24) · idor_fix(15) · delegation_api(24) · form_template_api(32) · seal_api(30) · master_data_api(37) · logout_revoke(11) · batch_approve_api(18) |
| ✓ 但有失败 | **admin_api(45/1)** · **attachment_api(46/1)** ← 见 4.1，不会红 |
| ✗ 红 | **escalation_api** · **flow_branch_admin** · **flow_interaction** |

**三个红灯的定性 —— 都不是"被测功能坏了"，而是"测试资产自己的假设过期"：**

| 红灯 | 现场证据 | 定性 |
|---|---|---|
| `flow_branch_admin` | `[FAIL] 演示库基线：3 个演示单据类型齐全 且 流程配置数=3 -> 现值…共 5 条 流程 5` | 脚本**硬编码**了演示库绝对数字；演示库被手工建过 2 个单据类型 + 2 个流程 ⇒ **基线断言过期**。该套件其余 40+ 条全 PASS |
| `escalation_api` | `scanned=0` / `escalated=0`（夹具已备好超时节点） | **未定位到根因**。注意 `flow_interaction` **同样**卡在 `escalated=0` ⇒ 两处同源，疑似与超时升级默认 `enabled=false` 有关；需单独查 |
| `flow_interaction` | `夹具就绪…承办人=`（**空**）⇒ 后续 401 连锁 | 夹具造出的待办**没有承办人**，而脚本假设"待办必有承办人" ⇒ **夹具假设落空** |

> **这三条红灯本身就是结论**：测试资产里也堆着一批"隐含假设"，
> 和这次冷启动挖出的产品 bug **是同一类毛病** —— 假设成立时全绿，假设一破就误报或漏报。

---

## 五、功能面评估

| 层 | 状态 | 说明 |
|---|---|---|
| 审批主干 | **已交付** | 提单 → 逐节点审批 → 驳回/撤回 → 办结；批量审批、委托、加签、附件、台账、看板、通知、审计 |
| 自助配置 | **已交付** | 四步闭环（单据类型 → 表单模板 → 流程 → 节点指派）全部界面可配，**经冷启动演练实战验证** |
| 条件分支 | **已交付** | 结构化分支编辑器 + 表达式白名单（`6862e63` 级提交） |
| 超时升级 | **半成品** | 代码与用例在库，但**默认关闭**（需求未确认）；本次实测**两处套件都跑不通** ⇒ 至少在当前环境不可用 |
| 动态表单 | **半动态** | 基础字段 + 必填 + 下拉可用；**字段规则不执行、18 个 key 静默吞字段、2 种字段类型不渲染** |
| 分级管理员 | **未做** | 管理接口只按 `company_id` 隔离，无"管理范围" |
| 职责分离(SoD) | **未做** | ADMIN 兼持 `document:approve:*`，即"系统管理员兼业务审批人" |
| 财务/银行/HR/推送集成 | **本期不做** | 已明确立项边界 |

---

## 六、交付与合规风险（按敞口排序）

| # | 风险 | 敞口 |
|---|---|---|
| 1 | **演示账号弱口令 + 测试服务器挂公网域名** | 最大。一旦被扫，整个测试环境可登录 |
| 2 | `init_database.sql` 幂等为假（41 张 Flowable 表无 `IF NOT EXISTS`） | 二次执行 exit 1 中断；`set -e` 的自动化必挂 |
| 3 | `seed_data.sql` 流程未部署（`deploy_status=0`）且脚本不提示 | 漏第③步 ⇒ "能登录能建单、一提交就报错"，且报错不指向脚本 |
| 4 | **交付口径冲突**：交接文档 §9.2 让灌 `seed_data`，`minimal_seed` 自称从零起点 | 执行的人会灌错，且带来海峡金演示组织 |
| 5 | SoD / 分级管理员缺失 | 视客户性质（金融/国企 + 等保则必做） |
| 6 | 审计是否覆盖高危操作**未核实** | 代操作/改状态/重置密码/代审批是否留痕未知 |

---

## 七、结论与建议

### 可以对外说的
- 架构分层与模块边界、技术选型的当代性、审批主干的功能完整度、代码卫生（零 HACK/FIXME）
- 自助配置闭环**已实战验证**（有演练报告与落库证据）

### 不能对外说的（说了就是埋雷）
- ❌ **"测试全绿"** —— 现状是 10/13 绿、2 个绿中有败且**结构上永远不会红**、1 个套件从未被回归
- ❌ **"交付即用"** —— 有 3 处交付硬伤 + 4 个冷启动阻断 bug
- ❌ **"框架完整支持动态表单"** —— 是半动态，4 类缺口需改代码

### 建议的三步（顺序有依赖）

```
第 1 步  修"验证可信度"   ← 4.1 补退出码 + 4.2 挂套件（几行代码）
                           为什么不先修功能：现在**没有任何可信的回归网**，
                           改了功能也验证不了；而且这 93 条里可能还藏着真失败
第 2 步  修冷启动阻断     ← B1 · B3 · B4（各 1~2 处，纯代码）
第 3 步  修交付脚本与文档 ← P1 · P2 · P3（零代码风险）
之后：C1~C3（合规）→ E1/E2（表单，见客户前）→ C4/C5（按客户性质）
```

**为什么第 1 步是修测试而不是修功能**：一份"永远不会红"的回归网，
比没有回归网更危险 —— 没有会让人警惕，**假绿会让人放心**。
本次之所以能挖出 4 个冷启动 bug，恰恰是因为绕开了演示库、绕开了这些套件。
先把网补成真的会响，后面每一步才站得住。

---

## 附：本次未核实

- [ ] 测试服务器 `hxj-oa` 库当前层状态（`deploy_status`、`document` 行数）—— 本地无服务器凭据
- [ ] 审计日志是否覆盖高危操作
- [ ] `escalation_api` 红灯的确切根因（需单独定位）
- [ ] E2E 5 套件本轮**未重跑**（需 headless 浏览器 + 独立环境）；文中 E2E 条数
      （88/56/79/44/65 = 332）是**脚本声明的闸门值**，非本次实跑结果
