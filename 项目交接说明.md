# 海峡金 OA 审批系统 —— 项目交接说明

> **这份文件的作用**：换 WorkBuddy 账号后，新会话没有历史对话与云端画像，
> 请**先读这份**，再按需读 `.workbuddy/memory/MEMORY.md`（项目长期记忆）与
> `.workbuddy/memory/2026-09-*.md`（每日工作日志）。
>
> 最后更新：2026-09-21 11:15

---

## 一、这是什么

| 项 | 内容 |
|---|---|
| 项目 | 海峡金 OA 审批系统（单公司审批流，含流程引擎、附件、行级数据权限、审计留痕） |
| 前端 | `海峡金OA审批系统-联调版.html`（单文件 Vue3 + Element Plus，**由原型另存、只替换逻辑层**） |
| 原型 | `海峡金OA审批系统-原型分析与后端规划.md`（需求与方案，**原件不动**） |
| 后端 | `oa-backend/`（Spring Boot 3.2.5 多模块 + Flowable 7 + MyBatis-Plus + MySQL 8，**JDK 17**） |
| 远端仓库 | `git@github.com:zzw8845/hxj-oa-new.git`（**公开**；用 SSH，不依赖 token） |

## 二、跑起来

```bash
# 启动（自带守护、单实例、掉线自动拉起；NO_OPEN=1 不打开浏览器，自动化用）
NO_OPEN=1 bash 启动联调版.command

# 停止
bash 停止联调版.command
```

- 访问入口：**http://127.0.0.1:8080/oa.html**（后端同源提供，不要用 `file://`，也不要另起静态服务）
- 演示账号：密码统一 `123456`，页面点名字即可切换身份
- 数据库：`haixiajin_oa`（`mysql -uroot haixiajin_oa`，root 免密）
- 沙箱内 `ps` 不可用，取 PID 用：`lsof -nP -iTCP:8080 -sTCP:LISTEN -t`

> ⚠ **JWT 密钥已改为 fail-fast（2026-09-21）**：后端**未配置 `OA_JWT_SECRET` 时会拒绝启动**（退出码 1）。
> 因此**本地起服务请一律走 `启动联调版.command`** —— 它会首次自动生成 `oa-backend/.jwt_secret`（0600，已入 `.gitignore`）并复用。
> 直接 `java -jar oa-boot/target/oa-boot-1.0.0-SNAPSHOT.jar` 会报「未配置 JWT 签名密钥，服务拒绝启动」，
> 此时自行 `export OA_JWT_SECRET=<>=32字节随机串>` 即可。

**构建**（必须 JDK 17，离线可用的 `-o`）：

```bash
cd oa-backend && JAVA_HOME="$(/usr/libexec/java_home -v 17)" \
  PATH="$(/usr/libexec/java_home -v 17)/bin:$PATH" mvn -q -o -DskipTests package
```

> ⚠ **重建 jar 前必须先停服务**。在服务正从该 jar 运行时覆盖同一个 fat jar，JVM 惰性加载类会炸
> `NoClassDefFoundError: ...ThrowableProxy`，服务进入「TCP 能连上但 HTTP 永远超时」的假活状态，
> 极易误判成端口/防火墙问题。规范做法见 `MEMORY.md` 的「重建 jar 前必须先停服务」。

## 三、演示数据库（**当前状态无法从仓库重建**）

`oa-backend/sql/seed_data.sql` 只灌**用户 / 角色 / 单据类型 / 流程配置 / 流程节点**，
**不含 `document` 与 `notification`**。当前演示态（46 张单据、84 条通知、5 条在途任务）是长期跑出来的本地状态。

**恢复方式（同机一般用不到，防意外清库）**：

```bash
mysql -uroot < oa-backend/sql/demo_snapshot_20260921.sql   # 含建库语句，可直接整体还原
```

该快照已验证可完整还原（10 张关键表逐项吻合）。**它含真实数据与审计日志，已被 `.gitignore` 排除，不要提交到公开仓库。**

**附件实体文件不在仓库里**（`oa-backend/data/attachments/` 被忽略）：若只还原数据库，
现有 1 个附件（`2026/09/20/d56d30a5a927461687bf90107fc226d5.png`）会下载 404，需连目录一起拷。

## 四、验证体系（改动后必须全跑）

| 脚本 | 覆盖 | 基线 |
|---|---|---|
| `scripts/verify_admin_api.py` | 权限越权 403/401、管理页写接口、唯一键回归 | 46/46 |
| `scripts/verify_attachment_api.py` | 附件上传/下载/删除、扩展名与大小校验、越权、办理节点凭证必填 | 47/47 |
| `scripts/verify_idor_fix.py` | **横向越权（IDOR）证据链**；`--phase baseline` 可断言漏洞存在 | 15/15 |
| `oa-backend/scripts/e2e/frontend_admin_e2e.js` | 管理端三页字段契约、下拉来自后端、新增员工闭环 | 38/38 |
| `oa-backend/scripts/e2e/frontend_attachment_e2e.js` | 草稿删附件、提交上传、详情下载字节比对、凭证必填拦截 | 56/56 |
| `oa-backend/scripts/e2e/frontend_dashboard_e2e.js` | 看板三格=后端真值、菜单角标=真实未读/待办、关联前置单据全链路 | 44/44 |
| `oa-backend/scripts/e2e/frontend_final_gaps_e2e.js` | 首页近7天/流程状态=后端统计、通知中心、筛选项来自真实类型 | 60/60 |
| `oa-backend/scripts/e2e/frontend_business_gaps_e2e.js` | 风险预警、台账导出、首页真值、台账日期筛选、**审计留痕全链路** | 79/79 |
| Java 单测（`mvn -o test`） | 数据范围 SQL 片段、可见性判定、待办不过滤、CSV 公式注入与结构转义 | **52/52** |

运行 UI E2E：

```bash
NODE_PATH=/Users/zhouzewei/.workbuddy/binaries/node/workspace/node_modules \
/Users/zhouzewei/.workbuddy/binaries/node/versions/22.22.2-3/bin/node \
oa-backend/scripts/e2e/frontend_business_gaps_e2e.js
```

**演示库基线**（跑任何测试前/后都应一致）：

```
document=46  attachment=1  notification=84  notify_unread=84  document_link=0
ACT_RU_TASK=5  ACT_RU_EXECUTION=10  ACT_HI_PROCINST=32  ACT_RE_PROCDEF=3(=flow_config 3)
orphan_hi_inst=0  orphan_notify=0  document_type=3
```

跑完务必 `python3 scripts/cleanup_demo_pollution.py --dry-run` 复核一次
（E2E 的 before/after 快照会把**已有**污染当基线）。

## 五、改代码前必看的硬约束（节选，完整版在 `MEMORY.md`）

1. **页面在 jar 里**：改完 `海峡金OA审批系统-联调版.html` 必须 `cp` 到
   `oa-backend/oa-boot/src/main/resources/static/oa.html` → 重新打包 → 重启，否则线上还是旧页。
   用 `unzip -p oa-run.jar BOOT-INF/classes/static/oa.html | shasum` 与源文件比对哈希自证。
2. **行级权限判定只能有一份实现**：一律复用 `DataScopeHelper.buildClause()`。
   详情/附件曾用独立的 Java switch 复刻同一语义并写错，造成**横向越权（IDOR）**——
   已修为「把片段套回本单据做一次主键 count」，使「详情可见 ⟺ 出现在列表里」成为构造性事实。
   **新增任何「可见吗」的判断，禁止再写 switch。**
3. **权限点是登录时刻快照**（编码在 JWT `permCodes` 里，校验零查库）：
   **改角色权限后，该角色成员必须重新登录才生效。**
4. **流程改结构必须新建版本**：`AssigneeTaskListener` 按 `(flowConfigId, nodeKey)` 定位节点，
   就地改节点会让在途单据静默卡死。只改审批人规则才是安全的。
5. **逻辑删除前要释放唯一键字段**（用 `UniqueKeys.release`），关联表用物理删除。
6. **改文件串行改**：同一文件不要并行发多个编辑，后写者会基于旧版本落盘并抹掉前次改动（不报错）。
7. **测试有效性要用「判据验证」证伪一次**：把源码临时还原成漏洞版本再跑，用例必须变红才算数。

## 六、当前进度（2026-09-21）

**HEAD**：以 `git log -1 --oneline` **现取为准**，不要相信本文档里写死的哈希。
截至本次全面审查：本地 `2f14d09`（「新增项目交接说明 + 忽略演示库快照」）
**领先 `origin/main`（`d19193f`）1 个提交，尚未推送**。

已完成：
- **横向越权（IDOR）修复 + 同源收敛 + 单测**（`b746593`）——详情/附件可见性统一走 `DataScopeHelper`，
  配套 15 条可见性用例（含结构层断言「真正发给数据库的 SQL 含 buildClause 片段」）
- **收尾批**（`d19193f`）——① 修掉一个**假绿灯门禁**：`frontend_business_gaps_e2e.js` 原在被回退的
  `/api/users/page` 上抛 TypeError 中断，导致后面 **16 条断言（含整条审计链路）从未执行**，现已恢复全场可跑
  79/79；② 台账导出文件名丢日期；③ 普通员工白打 `/api/permissions` 403；④ 补回回退中丢失的
  `LedgerExportEscapeTest`（单测 37 → 52）；⑤ 两份控制器 Javadoc 与现状对齐

- **安全加固批 + 测试自证闸门**（2026-09-21 第二轮）：
  ① 修 **P0** —— JWT 默认密钥改 fail-fast（删默认值 + 未配置即拒启；启动脚本自动生成并复用本地密钥）；
  ② 关 **P1-1** —— `/v3/api-docs` 不再对外暴露完整契约，新增免登录 `/api/ping` 承接前端探测与守护健康检查；
  ③ 修 **P1-2** —— 不存在路径由 500 改 404；
  ④ 装 **P1-3** —— 5 个 E2E 加预期断言总数自证闸门（已证伪验证会吼）；
  ⑤ 收 **P1-4** —— CORS 收敛为本机白名单 + `allowCredentials=false`；
  ⑥ 顺带：异常消息脱敏、登录失败限速、白名单剔除 4 个不存在的端点、3 个探针脚本内容同步（**经复核它们本就已入库**，
  「未入库」是上一轮审查的正则漏配造成的误报，已在审查报告中更正）。
  复测：52 单测 + 108 接口 + 277 UI 全绿，库快照一致。

**已回退、仍是 pending**（用户明确选择暂不重做）：
- **#34 主数据（部门/岗位/字典）写接口**——`DepartmentController`/`DictController` 现为**只读**，
  `DepartmentService`/`DictService` 无写方法，`master_data_e2e.js` 亦丢失。重做时必须：
  挂 `system:dept` / `system:dict` 权限点 + `@Audit(module="system")` + 补接口级用例
- **#35 列表接口分页**——`UserController` 无 `/page` 端点，`patch_frontend_userpage.py` 丢失。
  重做时要连用例一起写（不要再留下必红的断言，否则整个套件会在那里断掉）
- **#42 补齐其余列表接口分页**

## 七、已知风险

**已修复（2026-09-21）**：代码里**不再有**硬编码的默认 JWT 签名密钥。
`SecurityBeanConfig` 改为 fail-fast —— 未配置 `OA_JWT_SECRET` 直接拒绝启动；
`启动联调版.command` 首次运行自动生成 `oa-backend/.jwt_secret`（0600，已入 `.gitignore`）并复用。
⇒ 现在**不可能**出现"忘记配置却静默用默认密钥对外发令牌"的情况。

**残留风险（需人工决策）**：
- 那串旧默认密钥（`haixiajin-oa-default-secret-...`）**仍永久留在公开仓库的历史提交里**。
  它能伪造的对象只剩「仍用旧默认值启动的、尚未更新的部署」—— 当前环境已不受影响。
  要彻底消除痕迹，只能 `git filter-repo` 重写历史，或把仓库转为私有。
- 数据库口令仍是 `${MYSQL_PASSWORD:}` 空默认 + `root` 用户（本地演示库免密）。
  上线前需改为独立账号 + 强口令并注入环境变量。

## 八、项目记忆在哪

| 位置 | 内容 |
|---|---|
| `.workbuddy/memory/MEMORY.md` | **项目长期记忆**（硬约束、验证基线、踩坑铁律）——会被自动加载 |
| `.workbuddy/memory/2026-09-18/20/21.md` | 每日工作日志（做了什么、为什么、踩了什么坑） |
| `~/.workbuddy/skills/` | 用户级技能：`flowable7-springboot3-oa`、`vue-prototype-backend-integration`、`github-push-from-sandbox` 等 |
| `scripts/patch_frontend_*.py` | 前端改动的**幂等**补丁脚本；再改页面请**追加**到既有脚本，不要新建 |

> `.workbuddy/` 被 `.gitignore` 排除（工具自管数据，不入版本控制），但它在磁盘上**不会丢**。

---

## 九、附录：细则（从 MEMORY.md 迁出，以保证其可完整自动注入）

### 9.1 Element Plus E2E 交互铁律（实测踩坑，别再猜）

1. **下拉只认真实点击**：`dispatchEvent(new MouseEvent('mousedown'))` 打不开 `el-select` 浮层且不报错
   （连锁假失败的典型源头）—— 一律用 puppeteer `elementHandle.click()`。
2. **daterange 键盘输入完全无效**：键入只改 DOM value，v-model 不更新（两个日期会拼在同一输入框）。
   必须在面板上真实点选日期格（`.el-date-range-picker__content.is-left/.is-right td.available`）。
3. **菜单/表格 textContent 带图标前缀**（「△风险预警」），断言用 `includes`，别全等。
4. **沙箱下载残留**：Chrome 临时文件 `.com.google.chrome.for.testing.*` 在沙箱内「能写不能 unlink」必然残留
   （沙箱外跑同样代码无残留）。puppeteer 25.x **没有 `prefs` 启动参数**（静默忽略）；
   预写 profile `Default/Preferences` 的 `download.default_directory` 有效。
   判定用「**可见文件零新增**」硬断言 + 隐藏残留 best-effort 清理并报告。
5. **netErrors 过滤行首状态码**用 `/^(401|403)\s/`（`indexOf(' 403 ')` 匹配不到行首）。
6. **拿不准交互方式先写一次性探针脚本实测**（登录→触发→dump DOM/落点），别按印象连跑整轮。
7. **测试有效性要用「判据验证」证伪一次**：只跑绿不算数 —— 把源码临时还原成**漏洞版本**再跑，
   受影响的用例必须变红。做法：`git stash push -- <只含源码的路径>`（untracked 测试文件留在原地），
   跑 `mvn test` 看失败，再 `git stash pop`。实测 15 条可见性用例中 14 条变红。
8. **沙箱命令坑**：shell CWD 每次重置 → 所有命令自带 `cd <绝对路径> &&`；
   `grep` 是 BSD 版**不支持 `\|` 与 `\b`**（静默无匹配会产生假发现），一律 `grep -E`；
   `git ls-files` 转义中文路径 → 用 `-c core.quotepath=false`；curl 必须 `--noproxy '*'`。

### 9.2 前端补丁脚本清单

| 脚本 | 说明 |
|---|---|
| `patch_frontend_admin.py` | 一次性（桩函数换真实后端调用），**非幂等**，已跑过 |
| `patch_frontend_admin_fixes.py` | E2E 暴露问题的**幂等**修正集。**后续修正追加到这里，不要新建** |
| `patch_frontend_voucher_fix.py` | 审批凭证 2 处（历史补丁误删 `el-upload`、`openDetail` 丢 `requireAttachment`）。幂等 |
| `patch_frontend_entry_fix.py` | 入口按钮名改为真实 `document_type` 名 + 删死代码块。幂等 |
| `patch_frontend_final_gaps.py` | 收口批 20 项（首页图表接后端统计 / 通知中心抽屉 / 清死代码 / 修写死筛选清单）。幂等 |
| `patch_frontend_business_gaps.py` | 业务缺口批 13 项（风险预警页 / 台账导出 / 首页真值 / 台账日期筛选）。幂等 |
| `patch_frontend_dashboard_fix.py`、`patch_frontend_attachment.py` | 看板与附件批（早期） |
| `cleanup_demo_pollution.py` | 清理演示库污染（`--dry-run` / `--apply`，幂等） |

- 幂等脚本的判据是「锚点还在就替换 / 目标串已在就 SKIP / 都无则整体不写入」。
  **锚点会过期**：已退役条目（2 risk 改名、5 index 留 risk）的锚点漂移后会让脚本进入
  「都不命中即整体不写入」而**静默失效** —— 改前先跑一次看自检输出。
- 退役脚本 `patch_frontend_perm_guard.py`、`patch_frontend_todate_fix.py` 修的是**真实缺陷**，
  其修正已回收进 `patch_frontend_admin_fixes.py` 的**条目 6、7**，勿按名字去找。
- `patch_frontend_userpage.py` 已随 #35 回退删除，重做 #35 时再写。

### 9.3 主数据写接口口径（#34，已回退，重做时参考）

- 部门+岗位共用 `system:dept`、字典 `system:dict`，全部 `@Audit(module=system)`。
- 部门 path 是**自包含物化路径**（`/2/3/` 含自身），新增靠「插行拿自增 ID → 回填 path」两步同一事务。
- **`parentId` 刻意不可改**（不做部门移动）。
- DTO 必填校验放 service 的 create 分支：同一个 DTO 服务新增+局部编辑，
  在 DTO 上打 `@NotBlank` 会把「只传 label 的 PUT」拦成 400。
- 现状（已核实）：`DepartmentController`/`DictController` **只有 GET**，
  `DepartmentService`/`DictService` 无任何写方法，`master_data_e2e.js` 亦丢失 —— 回退彻底且一致。
  两份控制器的 Javadoc 已注明「当前无写接口」+ 新增时必须满足的三件事（权限点 / `@Audit` / 用例）。

### 9.4 最近一次全面审查（2026-09-21）

结论与修复清单见 **`项目审查报告-2026-09-21.md`**（第六节=问题清单，第七节=修复进展）。

**审查实测**：**437 条断言全绿**（52 单测 + 108 接口 + 277 UI），数据库快照前后逐项一致，零污染。

**第二轮已修 9 项**：
- **P0**：JWT 默认密钥 → fail-fast（删默认值 + 未配置即拒启；启动脚本自动生成并复用本地密钥）
- **P1-1**：`/v3/api-docs` 关闭（此前未认证可读 70KB 完整契约）；新增免登录轻量端点 `/api/ping`，
  前端**两处**探测与启动脚本健康检查同步改用它
- **P1-2**：不存在路径由 500 改为 **404**（不再污染 error 日志）
- **P1-3**：5 个 E2E 加 `EXPECTED_TOTAL` 自证闸门（**已证伪验证**会吼）
- **P1-4**：CORS 收敛为本机白名单 + `allowCredentials=false`（留 `OA_CORS_PERMISSIVE=true` 逃生舱）
- **P2**：异常消息脱敏 / 登录失败限速（10 次/5 分钟 → 429）/ 白名单剔除 4 个不存在的端点 /
  3 个探针脚本内容同步（**其"未入库"系误报**，实际自 `ab2a4a5` 起已入库）

**有意推迟**：登出 token 失效（需 token 版本号）、附件魔数校验（误杀风险需专门设计）。
**仍待办**：跨公司读未校验 `companyId`（当前单公司，风险低）、`BpmnGenerator` UEL 注入面、Flowable `database-schema-update=true` 生产应关。
