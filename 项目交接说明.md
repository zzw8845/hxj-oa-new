# 海峡金 OA 审批系统 —— 项目交接说明

> **这份文件的作用**：换 WorkBuddy 账号后，新会话没有历史对话与云端画像，
> 请**先读这份**，再按需读 `.workbuddy/memory/MEMORY.md`（项目长期记忆）与
> `.workbuddy/memory/2026-09-*.md`（每日工作日志）。
>
> 最后更新：2026-09-21 11:15

---

## 一、这是什么

| 项 | 内容 |
|---|---|
| 项目 | 海峡金 OA 审批系统（单公司审批流，含流程引擎、附件、行级数据权限、审计留痕） |
| 前端 | `海峡金OA审批系统-联调版.html`（单文件 Vue3 + Element Plus，**由原型另存、只替换逻辑层**） |
| 原型 | `海峡金OA审批系统-原型分析与后端规划.md`（需求与方案，**原件不动**） |
| 后端 | `oa-backend/`（Spring Boot 3.2.5 多模块 + Flowable 7 + MyBatis-Plus + MySQL 8，**JDK 17**） |
| 远端仓库 | `git@github.com:zzw8845/hxj-oa-new.git`（**公开**；用 SSH，不依赖 token） |

## 二、跑起来

```bash
# 启动（自带守护、单实例、掉线自动拉起；NO_OPEN=1 不打开浏览器，自动化用）
NO_OPEN=1 bash 启动联调版.command

# 直接连测试环境的 MySQL / Redis / OSS 跑（凭据读 oa-backend/docker-test/.env）
OA_PROFILE=test NO_OPEN=1 bash 启动联调版.command

# 停止
bash 停止联调版.command
```

- 访问入口：**http://127.0.0.1:8080/oa.html**（后端同源提供，不要用 `file://`，也不要另起静态服务）
- 演示账号：密码统一 `123456`，页面点名字即可切换身份
- 默认（不带 `OA_PROFILE`）为 `local`：连本机 `haixiajin_oa`（`mysql -uroot haixiajin_oa`，root 免密），零外部依赖
- `OA_PROFILE=test`：连测试服务器（MySQL `47.115.133.58:3305/hxj-oa`、Redis `db3`、OSS `shenzhen-rlzx`），
  **同一份 `.env` 与容器部署共用**，从根上避免"本地跑得通、部署就报错"的配置漂移
- 沙箱内 `ps` 不可用，取 PID 用：`lsof -nP -iTCP:8080 -sTCP:LISTEN -t`

> ⚠ **JWT 密钥已改为 fail-fast（2026-09-21）**：后端**未配置 `OA_JWT_SECRET` 时会拒绝启动**（退出码 1）。
> 因此**本地起服务请一律走 `启动联调版.command`** —— 它会首次自动生成 `oa-backend/.jwt_secret`（0600，已入 `.gitignore`）并复用。
> 直接 `java -jar oa-boot/target/oa-boot-1.0.0-SNAPSHOT.jar` 会报「未配置 JWT 签名密钥，服务拒绝启动」，
> 此时自行 `export OA_JWT_SECRET=<>=32字节随机串>` 即可。

**构建**（必须 JDK 17，离线可用的 `-o`）：

```bash
cd oa-backend && JAVA_HOME="$(/usr/libexec/java_home -v 17)" \
  PATH="$(/usr/libexec/java_home -v 17)/bin:$PATH" mvn -q -o -DskipTests package
```

> ⚠ **重建 jar 前必须先停服务**。在服务正从该 jar 运行时覆盖同一个 fat jar，JVM 惰性加载类会炸
> `NoClassDefFoundError: ...ThrowableProxy`，服务进入「TCP 能连上但 HTTP 永远超时」的假活状态，
> 极易误判成端口/防火墙问题。规范做法见 `MEMORY.md` 的「重建 jar 前必须先停服务」。

## 三、演示数据库（**当前状态无法从仓库重建**）

`oa-backend/sql/seed_data.sql` 只灌**用户 / 角色 / 单据类型 / 流程配置 / 流程节点**，
**不含 `document` 与 `notification`**。当前演示态（46 张单据、84 条通知、5 条在途任务）是长期跑出来的本地状态。

**恢复方式（同机一般用不到，防意外清库）**：

```bash
mysql -uroot < oa-backend/sql/demo_snapshot_20260921.sql   # 含建库语句，可直接整体还原
```

该快照已验证可完整还原（10 张关键表逐项吻合）。**它含真实数据与审计日志，已被 `.gitignore` 排除，不要提交到公开仓库。**

**附件实体文件不在仓库里**（`oa-backend/data/attachments/` 被忽略）：若只还原数据库，
现有 1 个附件（`2026/09/20/d56d30a5a927461687bf90107fc226d5.png`）会下载 404，需连目录一起拷。

## 四、验证体系（改动后必须全跑）

| 脚本 | 覆盖 | 基线 |
|---|---|---|
| `scripts/verify_admin_api.py` | 权限越权 403/401、管理页写接口、唯一键回归 | 46/46 |
| `scripts/verify_attachment_api.py` | 附件上传/下载/删除、扩展名与大小校验、越权、办理节点凭证必填 | 47/47 |
| `scripts/verify_idor_fix.py` | **横向越权（IDOR）证据链**；`--phase baseline` 可断言漏洞存在 | 15/15 |
| `oa-backend/scripts/e2e/frontend_admin_e2e.js` | 管理端三页字段契约、下拉来自后端、新增员工闭环 | 38/38 |
| `oa-backend/scripts/e2e/frontend_attachment_e2e.js` | 草稿删附件、提交上传、详情下载字节比对、凭证必填拦截 | 56/56 |
| `oa-backend/scripts/e2e/frontend_dashboard_e2e.js` | 看板三格=后端真值、菜单角标=真实未读/待办、关联前置单据全链路 | 44/44 |
| `oa-backend/scripts/e2e/frontend_final_gaps_e2e.js` | 首页近7天/流程状态=后端统计、通知中心、筛选项来自真实类型 | 60/60 |
| `oa-backend/scripts/e2e/frontend_business_gaps_e2e.js` | 风险预警、台账导出、首页真值、台账日期筛选、**审计留痕全链路** | 79/79 |
| Java 单测（`mvn -o test`） | 数据范围 SQL 片段、可见性判定、待办不过滤、CSV 公式注入与结构转义、**登录限速与权限快照缓存的 Redis/进程内双实现**、**附件存储键生成** | **83/83** |

> 单测计数口径：**数 `@Test` 或读 `surefire-reports/*.xml`，不要累加 `*.txt`**（`@Nested` 会被重复报告，txt 相加会虚高）。
> 2026-09-21 新增 31 条：`oa-system` 25 条（限速 11 + 快照 14）+ `oa-document` 6 条（存储键）。

运行 UI E2E：

```bash
NODE_PATH=/Users/zhouzewei/.workbuddy/binaries/node/workspace/node_modules \
/Users/zhouzewei/.workbuddy/binaries/node/versions/22.22.2-3/bin/node \
oa-backend/scripts/e2e/frontend_business_gaps_e2e.js
```

**演示库基线**（跑任何测试前/后都应一致）：

```
document=46  attachment=1  notification=84  notify_unread=84  document_link=0
ACT_RU_TASK=5  ACT_RU_EXECUTION=10  ACT_HI_PROCINST=32  ACT_RE_PROCDEF=3(=flow_config 3)
orphan_hi_inst=0  orphan_notify=0  document_type=3
```

跑完务必 `python3 scripts/cleanup_demo_pollution.py --dry-run` 复核一次
（E2E 的 before/after 快照会把**已有**污染当基线）。

## 五、改代码前必看的硬约束（节选，完整版在 `MEMORY.md`）

1. **页面在 jar 里**：改完 `海峡金OA审批系统-联调版.html` 必须 `cp` 到
   `oa-backend/oa-boot/src/main/resources/static/oa.html` → 重新打包 → 重启，否则线上还是旧页。
   用 `unzip -p oa-run.jar BOOT-INF/classes/static/oa.html | shasum` 与源文件比对哈希自证。
2. **行级权限判定只能有一份实现**：一律复用 `DataScopeHelper.buildClause()`。
   详情/附件曾用独立的 Java switch 复刻同一语义并写错，造成**横向越权（IDOR）**——
   已修为「把片段套回本单据做一次主键 count」，使「详情可见 ⟺ 出现在列表里」成为构造性事实。
   **新增任何「可见吗」的判断，禁止再写 switch。**
3. **权限点是登录时刻快照**（编码在 JWT `permCodes` 里，校验零查库）：
   **改角色权限后，该角色成员必须重新登录才生效。**
4. **流程改结构必须新建版本**：`AssigneeTaskListener` 按 `(flowConfigId, nodeKey)` 定位节点，
   就地改节点会让在途单据静默卡死。只改审批人规则才是安全的。
5. **逻辑删除前要释放唯一键字段**（用 `UniqueKeys.release`），关联表用物理删除。
6. **改文件串行改**：同一文件不要并行发多个编辑，后写者会基于旧版本落盘并抹掉前次改动（不报错）。
7. **测试有效性要用「判据验证」证伪一次**：把源码临时还原成漏洞版本再跑，用例必须变红才算数。
8. **不在 `/api/` 前缀下的端点，认证拦截器拦不到**：`AuthInterceptor` 是 `addPathPatterns("/api/**")`，
   所以 `/v3/api-docs`、`/swagger-ui/**`、`/actuator/**` 这类路径**根本不进鉴权链** ——
   白名单里写没写它们都一样。因此"关掉接口文档"**只能靠 `springdoc.*.enabled=false`**，
   往白名单删条目是无效动作。**本项目已因此栽过两次**（默认 profile 一次、test profile 把默认值写成 `true` 又一次），
   现已统一为**默认 `false`**，需要排查时在 `.env` 里显式打开。新增任何非 `/api` 端点时必须单独确认鉴权来源。

## 六、当前进度（2026-09-21）

**HEAD**：以 `git log -1 --oneline` **现取为准**，不要相信本文档里写死的哈希。
本文件最后一次更新时本地为 `ad08298`（「测试环境接口契约默认关闭」）+ 本次部署批，**已推 `origin/main`**。

已完成：
- **横向越权（IDOR）修复 + 同源收敛 + 单测**（`b746593`）——详情/附件可见性统一走 `DataScopeHelper`，
  配套 15 条可见性用例（含结构层断言「真正发给数据库的 SQL 含 buildClause 片段」）
- **收尾批**（`d19193f`）——① 修掉一个**假绿灯门禁**：`frontend_business_gaps_e2e.js` 原在被回退的
  `/api/users/page` 上抛 TypeError 中断，导致后面 **16 条断言（含整条审计链路）从未执行**，现已恢复全场可跑
  79/79；② 台账导出文件名丢日期；③ 普通员工白打 `/api/permissions` 403；④ 补回回退中丢失的
  `LedgerExportEscapeTest`（单测 37 → 52）；⑤ 两份控制器 Javadoc 与现状对齐

- **安全加固批 + 测试自证闸门**（2026-09-21 第二轮）：
  ① 修 **P0** —— JWT 默认密钥改 fail-fast（删默认值 + 未配置即拒启；启动脚本自动生成并复用本地密钥）；
  ② 关 **P1-1** —— `/v3/api-docs` 不再对外暴露完整契约，新增免登录 `/api/ping` 承接前端探测与守护健康检查；
  ③ 修 **P1-2** —— 不存在路径由 500 改 404；
  ④ 装 **P1-3** —— 5 个 E2E 加预期断言总数自证闸门（已证伪验证会吼）；
  ⑤ 收 **P1-4** —— CORS 收敛为本机白名单 + `allowCredentials=false`；
  ⑥ 顺带：异常消息脱敏、登录失败限速、白名单剔除 4 个不存在的端点、3 个探针脚本内容同步（**经复核它们本就已入库**，
  「未入库」是上一轮审查的正则漏配造成的误报，已在审查报告中更正）。
  复测：52 单测 + 108 接口 + 277 UI 全绿，库快照一致。

- **部署准备批**（2026-09-21 第三轮，详见第九节）：
  ① Redis 化登录限速 + 权限快照缓存（双实例共享失效，Redis 故障降级进程内）；
  ② 附件存储由本地磁盘切到 OSS（`oa.storage.type=oss`，`StorageKeys` 两实现共用键规则）；
  ③ `logback-spring.xml`（全量 + 仅异常两份文件，异步不丢日志）；
  ④ `docker-test/` 重写为 OA 自有的蓝绿部署（8010/9010，华为 SWR）；
  ⑤ 新增 `init_database.sql`（仅结构）与**空库上线三层顺序**（结构 → 主数据 → 部署流程）；
  ⑥ 补 31 条单测（限速/快照的 Redis 与进程内双实现、存储键生成）⇒ **单测 83/83**；
  ⑦ 发现并修掉 **OSS 错误分支因缺 javax JAXB 实现而静默失效**的缺陷（见 9.4）。
  已在真实测试环境实测：限速计数落 `db3` 且不污染 `db0`、快照换代、OSS 上传下载、`NoSuchKey → 404`。

- **正式部署上线**（2026-09-21 第四轮，详见 9.6）：
  ① 应用配置按真实内存收紧（Tomcat 线程 400 → 100）；
  ② 部署资产改造为「服务器本地构建」（开发机 Docker 损坏 + 服务器拉不到 Docker Hub 双断）；
  ③ 服务器加 2GB swap 兜底；
  ④ compose 项目名显式定为 `hxj-oa`（躲开同机已有项目 `admin`，防 `down` 误删别人容器）；
  ⑤ 停 `wms-mng` 腾出内存（停 + 关自启，非删除，可 22s 恢复）；
  ⑥ 起 OA 双实例 + 装 nginx 配置（80 跳 443 + 证书）并 reload；
  ⑦ **蓝绿发布与回滚双向演练通过**；⑧ 域名 `hxj-oa-test.wgit123.com` 经真实 DNS 端到端验证全绿。

**已回退、仍是 pending**（用户明确选择暂不重做）：
- **#34 主数据（部门/岗位/字典）写接口**——`DepartmentController`/`DictController` 现为**只读**，
  `DepartmentService`/`DictService` 无写方法，`master_data_e2e.js` 亦丢失。重做时必须：
  挂 `system:dept` / `system:dict` 权限点 + `@Audit(module="system")` + 补接口级用例
- **#35 列表接口分页**——`UserController` 无 `/page` 端点，`patch_frontend_userpage.py` 丢失。
  重做时要连用例一起写（不要再留下必红的断言，否则整个套件会在那里断掉）
- **#42 补齐其余列表接口分页**

## 七、已知风险

**已修复（2026-09-21）**：代码里**不再有**硬编码的默认 JWT 签名密钥。
`SecurityBeanConfig` 改为 fail-fast —— 未配置 `OA_JWT_SECRET` 直接拒绝启动；
`启动联调版.command` 首次运行自动生成 `oa-backend/.jwt_secret`（0600，已入 `.gitignore`）并复用。
⇒ 现在**不可能**出现"忘记配置却静默用默认密钥对外发令牌"的情况。

**残留风险（需人工决策）**：
- 那串旧默认密钥（`haixiajin-oa-default-secret-...`）**仍永久留在公开仓库的历史提交里**。
  它能伪造的对象只剩「仍用旧默认值启动的、尚未更新的部署」—— 当前环境已不受影响。
  要彻底消除痕迹，只能 `git filter-repo` 重写历史，或把仓库转为私有。
- 数据库口令仍是 `${MYSQL_PASSWORD:}` 空默认 + `root` 用户（本地演示库免密）。
  上线前需改为独立账号 + 强口令并注入环境变量。
- **测试服务器上 `hxj-oa` 目前仍用 `root` 账号**（口令走 `.env` 注入，未入库）。
  正式上线前应新建仅对该库有权限的独立账号。
- **待人工完成**（本机条件不具备，未验证）：容器内实跑、`nginx -t` + 切换演练、推送镜像到 SWR。
  另：`nginx-oa.conf.template` 的 `@@UPSTREAM_HOST@@` 需人工填真实回源地址。
- **测试库是"干净可用"而非"演示态"**：无单据、无通知，且流程定义需在页面上手动部署。
  由此 `scripts/*.py` 与 `scripts/e2e/*.js` 这些依赖演示数据的套件**不能在测试库上跑**（会大面积失败）。

## 八、项目记忆在哪

| 位置 | 内容 |
|---|---|
| `.workbuddy/memory/MEMORY.md` | **项目长期记忆**（硬约束、验证基线、踩坑铁律）——会被自动加载 |
| `.workbuddy/memory/2026-09-18/20/21.md` | 每日工作日志（做了什么、为什么、踩了什么坑） |
| `~/.workbuddy/skills/` | 用户级技能：`flowable7-springboot3-oa`、`vue-prototype-backend-integration`、`github-push-from-sandbox` 等 |
| `scripts/patch_frontend_*.py` | 前端改动的**幂等**补丁脚本；再改页面请**追加**到既有脚本，不要新建 |

> `.workbuddy/` 被 `.gitignore` 排除（工具自管数据，不入版本控制），但它在磁盘上**不会丢**。

---

## 九、测试服务器部署（2026-09-21 新增）

### 9.1 部署资产一览（`oa-backend/docker-test/`）

| 文件 | 作用 |
|---|---|
| `Dockerfile` | 基础镜像 `eclipse-temurin:17-jre-jammy`，但**ARGR `BASE_IMAGE` 默认指向华为云公共镜像源**（Docker Hub 在这台服务器上拉不动）；`SPRING_PROFILES_ACTIVE=test` 默认值；`HEALTHCHECK` 打 `/api/ping`（`start-period=120s`） |
| `docker-compose.yml` | 蓝绿双实例 `oa` / `oa-1`，宿主机端口 **8010**（蓝）/ **9010**（绿），容器内固定 8080；`mem_limit: 700m`；`restart: unless-stopped`；接入 `docker-compose_wg-network`（用容器名连 Redis） |
| `.env.example` | 环境变量模板（全部凭据位为 `CHANGE_ME`），**唯一可入库的凭据说明** |
| `.env` | 真实凭据，**已入 `.gitignore`，禁止提交**（仓库是 public） |
| `deploy.sh` | 本机侧：检查 → 打包 → **同步 jar 与编排文件到服务器** → **在服务器上 `docker build`** |
| `run.sh` | 服务器侧：构建镜像 → 起"当前未服务"的那个实例 → 健康检查 → `nginx -t` → 切 nginx → 停旧实例 → **调 `verify_deployed.sh` 做部署自证** |
| `verify_deployed.sh` | 服务器侧：**部署自证**（22 项检查：实例 / 经 nginx 的端到端 / 安全项 / 业务链路 / Redis 是否真生效 / 磁盘内存证书）。采用「登记 → 执行 → 核对」三步，**漏执行会被判成事故（退出码 2）而不是通过** |
| `watchdog.sh` | 服务器侧：cron 每 15 分钟跑一次的**只读看门狗**（不自动改任何东西），异常写 `/var/log/oa-watchdog.log` |
| `cert-deploy-hook.sh` | 服务器侧：certbot 的 `--deploy-hook`，把新证书装到 nginx 实际读取的路径并 reload。装前四道校验，任一不过就沿用旧证书。见 9.8 |
| `patch-acme-location.py` | 幂等脚本：给三个 vhost 的 `:80` 块准备 ACME HTTP-01 验证路径（含把 server 级 `return 301` 收进 `location /`，理由见 9.8）。支持 `OA_NGINX_CONF_DIR` 覆盖，可先在本地演练 |
| `nginx-oa.conf.template` | nginx 站点模板：80 强制跳 443 + 443 用**三个站点共用的 Let's Encrypt 证书**（原为 `*.wgit123.com` 泛域名，已换，见 9.8）；含 `@@UPSTREAM_HOST@@` / `@@SERVER_NAME@@` 占位符 |

服务器 `root@47.107.79.42:10745`，部署目录 **`/opt/sh/oa`**（compose 项目名显式设为 **`hxj-oa`**，见 9.6）。
nginx 配置落在 **`/opt/docker/nginx/conf/include/hxj-oa-test.wgit123.com.conf`**（宿主机目录，容器重建不丢）。

**不再用 SWR 镜像仓库**：镜像改为在服务器上本地构建（`docker build -t oa:latest .`）。
原因见 9.6 第 3 条 —— 开发机 Docker 坏了、服务器又拉不到 Docker Hub，两条路一起断了。

> ⚠ **`nginx-oa.conf.template` 里的 `@@UPSTREAM_HOST@@` 仍是占位符**，但本机**实测值已确定为 `172.23.148.199`**
> （宿主 eth0 IP）。依据是同服务器上已验证可用的 `rlzx-wms.conf` / `trace-*.conf` 都写这个地址，
> 即「宿主 IP + 容器发布端口」而非容器名。
> **刻意不用容器名**：upstream 里的域名在 **nginx 启动时**解析，若某次开机 nginx 先于 oa 起来，
> 解析失败会让 **nginx 整个起不来**，代价是同机 trace-web / trace-mng / trace-api / rlzx-wms 四个站点一起挂；
> 用固定 IP 则是"只有 OA 这站 502"，爆炸半径小得多。
> 已知脆弱点：eth0 是 DHCP 地址（`ip addr` 显示 `dynamic`），地址变了这里的配置会失效 ——
> 但这是**本机既有**的脆弱点（wms/trace 同样依赖它），不是本次引入。

### 9.2 空库**不能**直接上线：必须按顺序灌三层

```bash
# ① 结构（68 张表 = 27 业务表 + 41 Flowable 表；只有结构，无任何数据）
mysql -uroot -h<host> -P<port> hxj-oa < oa-backend/sql/init_database.sql

# ② 主数据（公司/部门/岗位/用户/角色/权限点/字典/单据类型/表单模板/流程配置）
#    缺了这一步**没有任何账号能登录**，系统等于不可用
mysql -uroot -h<host> -P<port> hxj-oa < oa-backend/sql/seed_data.sql

# ③ 流程定义：**不会自动部署**（check-process-definitions: false），需在"流程管理"页逐个点部署
```

- `init_database.sql` 由 `sql/gen_init_database.sh` 生成（`schema.sql` + `flowable_schema_mysql.sql` 拼装并自检），
  改表结构后要**重新生成**，不要手改产物。
- `seed_data.sql` **是幂等的**（开头 TRUNCATE 主数据表），可重复执行；但它**不含 `document`/`notification`**，
  所以测试库是"干净可用"而非"演示态"。
- ⚠ 测试库与本地演示库是**两套独立数据**：`scripts/*.py` 与 `scripts/e2e/*.js` 这些验证套件依赖演示态的
  46 张单据 / 84 条通知，**在干净的测试库上跑会大量失败**（不是代码问题）。要跑套件请连本地 `local` profile。
- **第 ③ 步不做的话，系统"看起来正常但用不了"**：能登录、能建单，一提交就报流程相关错误。
  当前测试库（2026-09-21）**已完成第 ③ 步**，3 个流程均为 `deploy_status=1`，可直接提单。
  重建库之后必须重做这一步。

### 9.3 与蓝绿部署强相关的三处设计（别改回去）

1. **登录限速放 Redis**（`RedisLoginThrottle`）：两个容器同时在线，进程内计数会被"轮流打两个实例"绕过，
   10 次/5 分钟直接变成 20 次/5 分钟。Redis 故障时降级到进程内计数（仍拦，只是每实例 10 次）。
2. **权限快照缓存放 Redis**（`RedisAuthSnapshotCache`）：失效用**全局版本号 INCR**（键名含版本号），
   不做逐用户删除、更不用 `KEYS`/`SCAN` —— 这台 Redis 与其他项目共用，扫键会阻塞整个实例。
3. **每个 Redis 键都必须带 TTL**：该实例 `maxmemory-policy=noeviction`，内存写满时**不淘汰、直接对写入报错**。
   漏 TTL 的后果不是自己变慢，而是把同实例上别的项目写挂。

Redis 用的是 **`db3`**（`REDIS_DATABASE=3`）：`db0`/`db1`/`db2` 已被同服务器的 WMS 项目占用。

### 9.4 ⚠ 血泪坑：OSS SDK 的错误分支依赖 **javax 世代** JAXB，且必须在**根 pom** 里锁版本

症状：上传/下载/删除**全部正常**，唯独"读一个不存在的对象"抛出来的是 `OSSException(InvalidResponse)`
而不是 `OSSException(NoSuchKey)` ⇒ `OssStorageService` 里 `NoSuchKey → 404` 的映射**永远不触发**。
**只跑冒烟测试（传一个文件再删掉）永远发现不了**，必须专门测错误分支。

根因：`aliyun-sdk-oss` 只声明 JAXB **API** 不声明实现；Spring Boot 3.x 把 `jaxb-runtime` 管到 **4.x**
（实现 `jakarta.xml.bind`），而 OSS SDK 用的是 `javax.xml.bind`，两者不通用。

修复必须在 **`oa-backend/pom.xml`（根）的 `dependencyManagement`** 里钉住三件套：

```
org.glassfish.jaxb:jaxb-runtime:2.3.3
org.glassfish.jaxb:txw2:2.3.3
com.sun.istack:istack-commons-runtime:3.0.11
```

只在子模块 `oa-document` 写 `<version>` **不够**：压不住传递依赖 `txw2`/`istack`，也压不住
`oa-boot` 组装 fat jar 时另一条路径带入的 4.0.5（实测出现 `jaxb-runtime=2.3.3` 却配着 `txw2=4.0.5`）。

自证方法（**必须跑在打包后的真实 classpath 上**，编译通过说明不了问题）：

```bash
# fat jar 里的三件套必须是 2.3.x / 3.0.x
unzip -l oa-backend/oa-boot/target/oa-boot-1.0.0-SNAPSHOT.jar | grep -E "jaxb|txw2|istack"
# 期望：jaxb-runtime-2.3.3.jar  txw2-2.3.3.jar  istack-commons-runtime-3.0.11.jar
```

### 9.5 本次已实测通过（跑在真实测试环境上）

| 项 | 证据 |
|---|---|
| 空库建表 | `hxj-oa` 68 张表，全部为空 |
| 主数据灌入 | 9 用户 / 8 角色 / 24 权限点 / 3 单据类型 / 3 流程配置（18 节点） |
| 登录 + 权限 | `admin` / `huangxm` 均 `123456` 可登录，令牌正常 |
| **Redis 限速** | 11 次错误登录：10 次 401 + 第 11 次 429；`db3` 键 `oa:login:fail:<账号>` = `10`，TTL 291s；**`db0` 未被污染（仍 25 键）** |
| **快照换代** | 改角色权限后 `oa:auth:gen` 由空 → `1`，下次登录写入 `oa:auth:snap:v1:1:<uid>`，旧 `v1:0:*` 键被弃用并按 TTL 过期 |
| **OSS 上传/下载** | 建草稿 → 上传 → 下载字节比对一致（40 字节） |
| **NoSuchKey → 404** | 绕过应用删掉桶里对象后下载 → `{"code":404,"msg":"附件文件已丢失，请联系管理员"}`；未删对象的对照附件正常返回内容 |
| 日志落盘 | `oa-backend/logs/haixiajin-oa.log`（全量）+ `haixiajin-oa-error.log`（WARN 及以上，异步、双条件滚动） |
| **接口契约不暴露** | `/v3/api-docs` 与 `/swagger-ui/index.html` 均返回 **404**（`springdoc` 默认关闭）；`/api/ping` 仍正常 |
| **流程定义部署** | 3 个流程全部 `deploy_status=1`，`ACT_RE_PROCDEF=3` 与 `flow_config` **一一对应** |
| **完整审批闭环** | 黄小明（业务一部）提单 → 待办出现在**林经理**（其部门 leader，规则 `initiator_leader`）手上 → 林经理审批通过 → 流转到「会计（按部门）」，指派**王会计**。业务一部无会计，`dept_role` 的 `fallback: company` 正确回退到公司级会计 |

> 验证用的单据已按「子表 → 父表」顺序清理干净（含 `ACT_HI_*`/`ACT_RU_*`、`notification`、`code_sequence`），
> 库回到「仅主数据 + 流程已部署」状态。清理时的两个列名坑：`flow_instance_node.instance_id`（不是 `flow_instance_id`）、
> `notification.biz_id`（没有 `document_id`）；另外 **`mysql` CLI 遇错即中止**，脚本里别用 `2>/dev/null` 吞错，
> 否则会像本次一样"部分清理成功"却看不出原因。

上面这些是**本机连真实测试库**跑出来的结果。**容器内实跑 + nginx 切换 + 回滚**已于同日完成，见 9.6。

### 9.6 正式部署到测试服务器（2026-09-21 实测完成）

访问入口：**<https://hxj-oa-test.wgit123.com/oa.html>**（HTTP 会 301 跳 HTTPS）。
登录 `admin` / `123456`（见 9.7 的安全提示）。

实测证据（全部在本机经真实 DNS 直连复现，非 `--resolve` 伪造）：

| 项 | 结果 |
|---|---|
| `GET /api/ping` | **200** `{"code":0,...,"data":"pong"}`，`ssl_verify=0`（证书链有效，无需 `-k`） |
| `GET /oa.html` | **200**，181743 字节 |
| `POST /api/auth/login` | **200 登录成功**（打通 MySQL → 密码校验 → JWT → Redis 权限快照） |
| 未认证访问 `/api/documents` | **401**（拦截器生效） |
| `/v3/api-docs` | **404**（接口契约确认关闭） |
| nginx 80 → 443 | **301** |
| 其他站点回归 | trace-web **200**、trace-mng **200**（未受影响） |
| **蓝绿发布** | `run.sh` 全链跑通：构建镜像 → 起 `oa-1` → **15s 健康通过** → 切 nginx 8010→9010 → 停 `oa` |
| **回滚演练** | `docker start oa` + sed 回 8010 + `nginx -t` → reload，**14s 恢复**，经域名 200 |
| **wms 恢复演练** | `docker start wms-mng` → 22s 后 `rlzx-wms-test` 由 502 变 403（应用已应答）；期间 OA 不受影响 |

**四条必须知道的实现细节**

1. **compose 项目名必须显式指定为 `hxj-oa`**（`run.sh` / `deploy.sh` 里都带 `-p hxj-oa`）。
   compose 默认拿**目录名**当项目名，而这台服务器上已有一个项目就叫 **`admin`**
   （`trace-mng` / `wms-mng` 都属于它，`admin_wg-network` 就是它建的）。
   项目名撞车的后果不是"看着别扭"：`docker compose ps` 会把别人的容器一起列出来，
   `docker compose down` 会把 **trace-mng / wms-mng 一起停掉并删除**。
   部署目录也因此定为 `/opt/sh/oa`（而不是 `/opt/sh/oa/admin`）。

2. **内存账（实测，非估算）**：服务器总内存 3.48GB。
   常驻：mysql 910MB + trace-mng 692MB + rabbitmq 138MB + nginx 8MB + redis 7MB，加宿主开销约 0.44GB。
   停掉 `wms-mng` 后可用 **1476MB**。单实例 OA 实测只占 **272~319MB**（远低于 700MB 上限），
   所以"双实例 1400MB"的上限只会在**发布的那几十秒**被真正逼近。
   - `restart` 用 **`unless-stopped`** 而非 `always`：`always` 会把**手工停掉的**备用实例在重启后无条件拉回来，
     于是重启后两个 JVM 同时占内存，而内存预算是按"一个实例"算的。
   - 宿主**另加了 2GB swap**（`/swapfile`，写进 `/etc/fstab`，`vm.swappiness=10`）。
     理由：余量只有几十 MB，宁可在边缘少量换页，也好过被 OOM 直接杀掉。
     实测发布期间 swap 用到过 139~158MB。回退：`swapoff /swapfile && rm /swapfile` 并删掉 fstab 那行。
   - `application-test.yml` 的 Tomcat `threads.max` 由 400 压到 **100**（当并发上限用，给 700MB 上限上保险）。
     注意：Linux 线程栈是**惰性提交**的，400 线程并不会真的吃掉 400MB 常驻内存 —— 别用这个理由去"论证"内存够。

3. **镜像改在服务器本地构建**（`docker build`，不推 SWR）。两条路一起断了：
   - 开发机的 Docker **坏了**：`docker pull` 报
     `write /var/lib/desktop-containerd/.../meta.db: input/output error`（containerd 元数据库损坏）；
   - 服务器**拉不到 Docker Hub**：`registry-1.docker.io` 超时。`daemon.json` 里的阿里云加速器
     虽然 `docker info` 显示已生效、直连也返回 200，但**它已不代理 `eclipse-temurin` 这类镜像**，
     拉取会回落到 Docker Hub 然后超时。
   实测可用的是**华为云公共镜像源**：
   `swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/eclipse-temurin:17-jre-jammy`
   （已写成 `Dockerfile` 里 `ARG BASE_IMAGE` 的默认值，换环境用 `--build-arg` 覆盖）。
   代价：镜像仓库里没有历史版本，回滚靠"保留旧容器 `docker start`"+"本地时间戳 tag"两条路。

4. **wms 的处置是"停 + 关自启"，不是删除**：
   `docker update --restart=no wms-mng && docker stop wms-mng`。
   容器和镜像都还在，`docker start wms-mng` 22 秒即可恢复（已验证）。
   - 副作用：**`rlzx-wms-test.wgit123.com` 现在是 502**（因为它的后端停了）。若该站点还需在线，
     就得放弃 OA 的内存让位方案，改成别的办法。
   - 若确认不再需要 wms，可以 `docker rm wms-mng` 彻底删除（镜像在服务器上，仍可随时重建）。

### 9.7 ⚠ 上线前必须处理的安全项（本次未做）

| 项 | 说明 |
|---|---|
| **证书已自动续期（已解决，2026-09-21）** | 原为外购 Certum 通配符 `*.wgit123.com`，`notAfter = 2026-10-13`、**无任何自动续期**（certbot 账本为空、`certbot-renew.timer` 从未 active）。现已换成 **Let's Encrypt 三域名证书**（`hxj-oa-test` / `trace-mng-test` / `trace-api-test`），90 天有效、到期前 30 天由 `certbot-renew.timer` 自动续、`cert-deploy-hook.sh` 自动装并 reload。**以后不需要任何人操作**。旧证书留在同目录 `*.certum.bak` 可回退。细节见 9.8 |
| **测试库口令是 `123456`** | 9 个账号全部 `123456`（含 `admin`）。域名已公网可解析，**等于把管理员账号的弱口令挂在公网上**。至少先改 `admin` 的口令，或把访问限制到办公网 IP |
| **JWT 密钥** | 本次已在服务器 `.env` 里配了真实随机值（非仓库里的默认值），但**仓库历史里的默认密钥仍是公开的**，见 `项目审查报告-2026-09-21.md` P0 |

### 9.8 证书自动续期（2026-09-21 落地）

**结论：证书这块以后不需要任何人操作，也不需要去阿里云。**

> 别把两件事混起来：**域名注册**和**SSL 证书**是两码事。
> - **域名注册**（`wgit123.com` 本身）在阿里云（注册商 = Alibaba Cloud / 万网）控制台续费，
>   到期 **2035-05-09**，还有近 9 年，现在不用管。这一项确实"去阿里官网操作就好"。
> - **SSL 证书**是另一件事，也是原本 10-13 要出问题的那个。**不需要动阿里云。**

**为什么不用"去阿里云申请免费证书"这条路**：阿里云免费证书是 *DV 单域名、3 个月有效期、
不支持通配符*，且**不支持 ACME ⇒ 走不了自动续期**，每 3 个月都要人工去控制台重新申请、
下载、上传、覆盖、reload。本机 3 个站点是 3 个不同域名 ⇒ 每轮 3 次人工操作；
用免费证书的话等于把"忘记就全站挂"变成一个每 3 个月出现一次的风险。
（用付费证书则可买一张通配符，人工频率降到一年一次，但要花钱。）

**现状（已配好并实测）**：

| 项 | 值 |
|---|---|
| 证书 | Let's Encrypt，`CN=hxj-oa-test.wgit123.com`，SAN 含上述三个域名 |
| 有效期 | 90 天，当前 `notAfter = 2026-12-20` |
| 续期 | `certbot-renew.timer`（systemd，每 12 小时尝试一次，`Persistent=true`），剩余 30 天起自动续 |
| 安装 | `/opt/sh/oa/cert-deploy-hook.sh`（记在 `/etc/letsencrypt/renewal/wgit123.conf` 的 `renew_hook`） |
| 证书文件 | `/opt/docker/nginx/https/wgit123.com.pem` / `.key`（**三个站点共用**，路径没变） |
| 回退 | 同目录 `*.certum.bak`（旧的 Certum 通配符，可用到 2026-10-13） |

**验证方式**：`certbot renew --dry-run`（走 staging，会打印
`all simulated renewals succeeded`，不会动线上证书）。
⚠️ 它耗时约 **2.5 分钟**（HTTP-01 本身只要 10 秒，剩下的是 certbot 1.22 的启动开销），
用 `timeout` 包着跑容易被掐断并留下锁文件，下次会报
`Another instance of Certbot is already running` —— 那个报错**不代表有进程在跑**，
先 `pgrep -a certbot` 确认，别急着删锁。

**ACME 验证路径**：三个 vhost 的 `:80` 块各插了一段
`location ^~ /.well-known/acme-challenge/ { root /opt/html; }`，
由 `docker-test/patch-acme-location.py`（幂等）维护，备份在 `/opt/sh/oa/nginx-conf-backup/`。
改动前请先 `python3 patch-acme-location.py`（不带 `--apply`）看它会改什么。

**踩到的两个坑**（都只有实测才看得见，`nginx -t` 一律通过）：

1. `hxj-oa` 的 `:80` 原本是 **server 级 `return 301`**。`return` 属于 rewrite 模块，
   在 rewrite 阶段执行、**早于 location 选择** ⇒ 光插 location 抢不过它，
   验证路径实测仍返回 301。必须把这条 return 收进 `location /`。
2. 将来若新增子域名，**必须** `certbot certonly ... --cert-name wgit123 ... -d 新域名`
   把它加进同一张证书；否则新域名会拿到"域名不匹配"的证书。
   也可以用 `certbot certonly --expand`。

**已知代价**：不再覆盖 `*.wgit123.com` 通配符。若将来要恢复通配符自动续期，
需要在阿里云建一个**只能改 DNS 的 RAM 子账号**、把 AccessKey 放到服务器
（600 权限文件）并加 `certbot-dns-aliyun` 插件，走 DNS-01 —— 一次性配置，
之后同样无需人工。目前判断不值得引入这个长期凭据。

---

## 十、附录：细则（从 MEMORY.md 迁出，以保证其可完整自动注入）

### 9.1 Element Plus E2E 交互铁律（实测踩坑，别再猜）

1. **下拉只认真实点击**：`dispatchEvent(new MouseEvent('mousedown'))` 打不开 `el-select` 浮层且不报错
   （连锁假失败的典型源头）—— 一律用 puppeteer `elementHandle.click()`。
2. **daterange 键盘输入完全无效**：键入只改 DOM value，v-model 不更新（两个日期会拼在同一输入框）。
   必须在面板上真实点选日期格（`.el-date-range-picker__content.is-left/.is-right td.available`）。
3. **菜单/表格 textContent 带图标前缀**（「△风险预警」），断言用 `includes`，别全等。
4. **沙箱下载残留**：Chrome 临时文件 `.com.google.chrome.for.testing.*` 在沙箱内「能写不能 unlink」必然残留
   （沙箱外跑同样代码无残留）。puppeteer 25.x **没有 `prefs` 启动参数**（静默忽略）；
   预写 profile `Default/Preferences` 的 `download.default_directory` 有效。
   判定用「**可见文件零新增**」硬断言 + 隐藏残留 best-effort 清理并报告。
5. **netErrors 过滤行首状态码**用 `/^(401|403)\s/`（`indexOf(' 403 ')` 匹配不到行首）。
6. **拿不准交互方式先写一次性探针脚本实测**（登录→触发→dump DOM/落点），别按印象连跑整轮。
7. **测试有效性要用「判据验证」证伪一次**：只跑绿不算数 —— 把源码临时还原成**漏洞版本**再跑，
   受影响的用例必须变红。做法：`git stash push -- <只含源码的路径>`（untracked 测试文件留在原地），
   跑 `mvn test` 看失败，再 `git stash pop`。实测 15 条可见性用例中 14 条变红。
8. **沙箱命令坑**：shell CWD 每次重置 → 所有命令自带 `cd <绝对路径> &&`；
   `grep` 是 BSD 版**不支持 `\|` 与 `\b`**（静默无匹配会产生假发现），一律 `grep -E`；
   `git ls-files` 转义中文路径 → 用 `-c core.quotepath=false`；curl 必须 `--noproxy '*'`。

### 9.2 前端补丁脚本清单

| 脚本 | 说明 |
|---|---|
| `patch_frontend_admin.py` | 一次性（桩函数换真实后端调用），**非幂等**，已跑过 |
| `patch_frontend_admin_fixes.py` | E2E 暴露问题的**幂等**修正集。**后续修正追加到这里，不要新建** |
| `patch_frontend_voucher_fix.py` | 审批凭证 2 处（历史补丁误删 `el-upload`、`openDetail` 丢 `requireAttachment`）。幂等 |
| `patch_frontend_entry_fix.py` | 入口按钮名改为真实 `document_type` 名 + 删死代码块。幂等 |
| `patch_frontend_final_gaps.py` | 收口批 20 项（首页图表接后端统计 / 通知中心抽屉 / 清死代码 / 修写死筛选清单）。幂等 |
| `patch_frontend_business_gaps.py` | 业务缺口批 13 项（风险预警页 / 台账导出 / 首页真值 / 台账日期筛选）。幂等 |
| `patch_frontend_dashboard_fix.py`、`patch_frontend_attachment.py` | 看板与附件批（早期） |
| `cleanup_demo_pollution.py` | 清理演示库污染（`--dry-run` / `--apply`，幂等） |

- 幂等脚本的判据是「锚点还在就替换 / 目标串已在就 SKIP / 都无则整体不写入」。
  **锚点会过期**：已退役条目（2 risk 改名、5 index 留 risk）的锚点漂移后会让脚本进入
  「都不命中即整体不写入」而**静默失效** —— 改前先跑一次看自检输出。
- 退役脚本 `patch_frontend_perm_guard.py`、`patch_frontend_todate_fix.py` 修的是**真实缺陷**，
  其修正已回收进 `patch_frontend_admin_fixes.py` 的**条目 6、7**，勿按名字去找。
- `patch_frontend_userpage.py` 已随 #35 回退删除，重做 #35 时再写。

### 9.3 主数据写接口口径（#34，已回退，重做时参考）

- 部门+岗位共用 `system:dept`、字典 `system:dict`，全部 `@Audit(module=system)`。
- 部门 path 是**自包含物化路径**（`/2/3/` 含自身），新增靠「插行拿自增 ID → 回填 path」两步同一事务。
- **`parentId` 刻意不可改**（不做部门移动）。
- DTO 必填校验放 service 的 create 分支：同一个 DTO 服务新增+局部编辑，
  在 DTO 上打 `@NotBlank` 会把「只传 label 的 PUT」拦成 400。
- 现状（已核实）：`DepartmentController`/`DictController` **只有 GET**，
  `DepartmentService`/`DictService` 无任何写方法，`master_data_e2e.js` 亦丢失 —— 回退彻底且一致。
  两份控制器的 Javadoc 已注明「当前无写接口」+ 新增时必须满足的三件事（权限点 / `@Audit` / 用例）。

### 9.4 最近一次全面审查（2026-09-21）

结论与修复清单见 **`项目审查报告-2026-09-21.md`**（第六节=问题清单，第七节=修复进展）。

**审查实测**：**437 条断言全绿**（52 单测 + 108 接口 + 277 UI），数据库快照前后逐项一致，零污染。

**第二轮已修 9 项**：
- **P0**：JWT 默认密钥 → fail-fast（删默认值 + 未配置即拒启；启动脚本自动生成并复用本地密钥）
- **P1-1**：`/v3/api-docs` 关闭（此前未认证可读 70KB 完整契约）；新增免登录轻量端点 `/api/ping`，
  前端**两处**探测与启动脚本健康检查同步改用它
- **P1-2**：不存在路径由 500 改为 **404**（不再污染 error 日志）
- **P1-3**：5 个 E2E 加 `EXPECTED_TOTAL` 自证闸门（**已证伪验证**会吼）
- **P1-4**：CORS 收敛为本机白名单 + `allowCredentials=false`（留 `OA_CORS_PERMISSIVE=true` 逃生舱）
- **P2**：异常消息脱敏 / 登录失败限速（10 次/5 分钟 → 429）/ 白名单剔除 4 个不存在的端点 /
  3 个探针脚本内容同步（**其"未入库"系误报**，实际自 `ab2a4a5` 起已入库）

**有意推迟**：登出 token 失效（需 token 版本号）、附件魔数校验（误杀风险需专门设计）。
**仍待办**：跨公司读未校验 `companyId`（当前单公司，风险低）、`BpmnGenerator` UEL 注入面、Flowable `database-schema-update=true` 生产应关。
